# SimplifyModifier安定化・曲率ベース間引き計画書

## 背景・現状の課題

3D生成パイプライン(`js/carving.js`の`decimateMesh`)は、間引き(頂点削減)に
2つの実装を使い分けている。

- **`simplifyModifierDecimate`**: `js/vendor/SimplifyModifier.js`(three.js付属の
  quadric error簡略化、edge collapse実装)を使う。辺の長さ×曲率で「潰しても
  影響が小さい辺」を優先的に選ぶため、平坦な面は大胆に、複雑な形状は繊細に
  間引く、本来あるべき曲率感度の高い間引きができる。
- **`gridClusterDecimate`**: バウンディングボックスを格子(グリッド)に分割し、
  同じセルに落ちる頂点をまとめて1点にする方式。edge collapseのような複雑な
  半エッジ構造を持たないため頂点数に関わらず必ず動作するが、**曲率を一切見ない
  (どんな複雑な形状でも同じセルサイズで機械的に間引く)**、質の低い方式。

`SimplifyModifier`は頂点数が`SIMPLIFY_SAFE_LIMIT`(60000)を超えると
`gridClusterDecimate`にフォールバックする。理由は実機ベンチマークで確認済みの
不安定性: 27,818頂点は成功するが280,386頂点は
`"Cannot read properties of undefined (reading 'hasVertex')"`で例外を投げる。
閾値を200,000まで上げても同じ例外で失敗することを2026-07-11に確認済み
(`js/carving.js`のSIMPLIFY_SAFE_LIMIT付近のコメント参照)。

体+全アクセサリー統合間引き(2026-07-11のリファクタ)により、実サンプル
(7アクセサリー全部乗せ)は合計163,982頂点で**必ず**`gridClusterDecimate`
経路を通る。このため、体よりずっと細いアクセサリー(房状の髪飾り・
ツインテール等)の断面が数セルに丸ごと吸収され、粗い/穴の空いた形状になる
不具合が発生した(2026-07-12、ユーザー指摘「ツインテールの造形が粗い」)。

### 暫定対応(実施済み、コミット33feffd)

`gridClusterDecimate`に以下を追加して急場をしのいだ:
1. パーツの境目の頂点(隣接面が別ownerを含む頂点)は絶対にクラスタリングせず
   位置を凍結する。
2. それ以外の頂点は、各ownerが自分自身のbbox表面積と目標頂点数(全体の
   targetVertsを頂点数比で配分)から求めた専用のセルサイズで間引く。

この暫定対応で「扇状に歪む」「穴が空く」実害は解消したが、**曲率を見ない
機械的な間引きである点は変わっていない**(ユーザー指摘: 「パーツのサイズで
間引くんじゃなくて法線とか強さで決めるべきでは」)。ファイルサイズも
816,608→1,406,364バイトに増えている。

## 今回の方針

`gridClusterDecimate`側の小手先の改善を続けるのではなく、**`SimplifyModifier`
(曲率を考慮したquadric error簡略化)を大きいメッシュでも安定して使えるように
根本修正し、これを唯一の間引き経路にする**。あわせて、UIの間引き設定を
「目標頂点数を指定する」方式から「間引きの強さ(許容誤差)を指定し、結果の
頂点数は表示するだけ」という、誤差ベースの本来あるべき方式に変更する。

### 1. クラッシュの原因調査(着手済み、要検証)

`js/vendor/SimplifyModifier.js`の`collapse(vertices, faces, u, v)`関数を
確認したところ、以下の配列変更中反復(mutate-while-iterating)のハザードが
疑わしい:

```js
for ( let i = u.faces.length - 1; i >= 0; i -- ) {
  u.faces[ i ].replaceVertex( u, v );
}
```

`replaceVertex`内部で`removeFromArray(oldv.faces, this)`(=`u.faces`から
自分自身をsplice除去)を呼んでおり、**まさにこの`for`ループが反復している
配列自体を、ループの内側から書き換えている**。後ろから前へ回るループ自体は
「今の要素を消して1つ前に進む」パターンとしては一般的に安全とされるが、
`u`が複数の三角形で共有されている場合の`faces`配列の実際の状態遷移を
1つずつ手で追って確認できていない(理論的な疑いの段階、実証はまだ)。

**次にやること**: 実際に163,982頂点クラスの巨大メッシュ(サンプル2、7
アクセサリー全部乗せ)を使って、`collapse()`が呼ばれるたびに`u.faces`の
状態をログ出力するなどして、クラッシュの直接的な引き金を特定する
(疑いの箇所が正しいかどうかも含めて要検証)。

### 2. 安定化の実装方針(調査結果次第で変わりうる)

- 疑いが正しければ、`for`ループ開始前に`u.faces`を`slice()`でスナップ
  ショットしてから反復するよう修正する(mutate-while-iterating回避の
  定石)。
- 修正後、実際に163,982頂点のサンプルで`SimplifyModifier`が最後まで完走
  するか確認する。完走しても**結果のメッシュが破損していないか
  (縮退三角形・非多様体・法線反転等)を別途検証する**必要がある
  (クラッシュしないことと正しい結果であることは別問題)。

### 3. パフォーマンスの懸念(クラッシュとは別に検討が必要)

`minimumCostEdge(vertices)`は毎回vertices配列を線形走査する
(`for`ループでO(n))実装で、これを間引きたい頂点の数だけ繰り返すため
全体でO(n×削除数)。163,982頂点から10,000頂点まで約154,000回削除する
場合、154,000×163,982 ≈ 250億回の比較が発生し得る。クラッシュが直っても
**実用的な時間で終わらない可能性が高い**。優先度付きキュー(二分ヒープ等)
に置き換えて`minimumCostEdge`をO(log n)にする改修も合わせて必要になる
見込み(実測してから判断)。

### 4. UIの再設計: 目標頂点数 → 間引きの強さ(誤差閾値)

現在`target_verts`(ユーザーが直接指定する目標頂点数)になっている設定を、
「間引きの強さ」(0〜1、または任意のスケールの誤差閾値)に置き換える。

- `SimplifyModifier.modify(geometry, count)`のAPIを、`count`(削除数)
  指定に加えて`maxCost`(許容誤差の上限)指定でも止められるように拡張する
  (`minimumCostEdge`が返す最小コストが閾値を超えた時点でループを打ち切る)。
- ジェネレータ/ビューアのUIは「強さ」スライダー1本にし、間引き実行後の
  結果頂点数を(編集不可の)表示欄に出す(ユーザーが頂点数を勘で決め打ち
  する必要がなくなる)。
- 「強さ」→「maxCost」の対応づけは、モデル全体のスケール(SCALE等の
  キャリブレーション値)に対する相対値にする(絶対値だとキャラクターの
  体格差で同じ強さ設定の効き方が変わってしまうため)。

### 5. `gridClusterDecimate`の扱い

`SimplifyModifier`が大規模メッシュで安定動作することを実機確認できたら、
`gridClusterDecimate`は「万一`SimplifyModifier`が失敗した場合の最終フォール
バック」という元々の役割に戻る。今回追加した境目凍結・パーツ別セルサイズの
複雑化(コミット33feffdの内容)は、`gridClusterDecimate`がフォールバックと
してもほぼ発動しなくなるなら、**複雑さに見合わなくなる可能性が高い**。
ただし完全に発動しなくなる保証はない(`SimplifyModifier`が別の理由で
失敗する可能性は残る)ため、以下のいずれかを選ぶ判断が必要:

- (a) 境目凍結・パーツ別セルサイズのロジックごと削除し、シンプルな旧来の
  単一セルサイズ方式に戻す(フォールバック発動時の質は今回の修正前に
  逆戻りするが、SimplifyModifierが安定していれば発動自体が稀になるため
  実害は小さいと考えられる)。
- (b) 複雑化したロジックはそのまま残す(コードの複雑さは増えるが、
  フォールバック発動時でも今回改善した質を維持できる)。

**この計画に着手するセッションで、実機確認の結果を踏まえて判断すること。**
(現時点でのユーザーとの合意: 「進める」= (a)寄りだが、SimplifyModifier側の
安定性が実証できるまでは(b)のまま様子見でもよい)。

## 検証方法

1. サンプル2(7アクセサリー全部乗せ、生彫刻163,982頂点)で、修正後の
   `SimplifyModifier`が実際に完走することを確認する(タイムアウト・
   クラッシュ・メモリ不足が起きないか)。
2. 完走した場合の結果メッシュを目視確認する(特にツインテール等の細い
   アクセサリーが滑らかで穴が無いこと、パーツの境目に扇状の歪みが
   無いこと)。
3. `npm test`(golden-masterハッシュは彫刻結果が変わるため更新が必要)。
4. 「強さ」スライダーのUIをジェネレータ・ビューア両方で実装し、
   `generator-ui.test.js`/`viewer-ui.test.js`がPASSすることを確認する。
5. 間引き所要時間を実測し、体感で許容できる範囲(数秒〜十数秒程度)に
   収まっているか確認する(優先度付きキュー化が本当に必要かどうかの
   判断材料にする)。

## 今回やらないこと(スコープ外、別途検討)

- `gridClusterDecimate`自体をより高品質な曲率考慮アルゴリズムに作り替える
  こと(SimplifyModifier側の安定化で解決する前提のため、gridCluster自体の
  高度化は不要になる見込み)。
- 間引き以外の品質課題(前後の境界の継ぎ目の縞模様、パーツ間の45度視点での
  隙間等、本セッションの別の調査で指摘済みの未解決事項)は本計画のスコープ
  外。
