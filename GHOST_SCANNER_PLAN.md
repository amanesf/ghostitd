# ゴーストスキャナー(AI前処理ツール)計画書

## 目的
1枚の任意イラスト(ポーズ・アングル不問)を入力し、Gemini APIを使って
①front(Tポーズ正面)/side/backの3面図を段階的に生成、②各面のランドマーク・
生成パラメータを自動推定、③既存のジェネレータ(`landmark_tool.html`)へ
そのまま引き継げる形で出力する、半自動の前処理ツール。既存の3ツール
(ゴーストジェネレータ/ゴーストビューア/ゴーストコントローラー)と並ぶ
4つ目のツールとして`index.html`に追加する。

## 名称
**ゴーストスキャナー**(Ghost Scanner)。1枚の絵から"本当の姿"(3面図+骨格情報)
を読み取る機能に合わせた、既存ツール群と同じ「ゴースト+平易な英単語」の命名規則。

## APIキーの取り扱い
- `geminiAPIkey`リポジトリ(このリポジトリとは別)にキーを保管(user管理、期限付きで
  ローテーションする前提)。
- ゴーストスキャナー自体はキーをコードに埋め込まない。初回起動時に入力欄へ
  手動で貼り付け、`localStorage`に保存して以降は自動読込する(GitHubからの
  自動取得はしない=「鍵を取ってくるための鍵」問題を避けるため)。

## 画像生成の課金化に伴う手動フォールバック(重要)
画像生成(front/side/back、プロンプトA/B/C)は無料枠が使えなくなったため、
**API経由の自動生成と、外部ツールで手動生成した画像のアップロードの両方に
対応する**。画像認識・JSON推定系(ランドマーク推定=プロンプトD、パラメータ
提案=プロンプトE、body/accessory分類=プロンプトF)は引き続きGemini 3.5 Flash
で低コストに利用できるため、こちらは従来どおりAPI呼び出しのみでよい。

- **画像生成を伴う各フェーズ(タブ1入力後の2/3/4)には以下を用意する**:
  - **プロンプトコピペボタン**: 表示中のプロンプト全文(参照画像込みの指示文)を
    クリップボードにコピーする。ユーザーが外部のGemini/他ツールへ手動で
    貼り付けて生成できるようにする。
  - **画像アップロードボタン**: 手動生成した画像ファイルをそのタブの確定画像
    として読み込む(API生成結果を待つ代わりに、この画像を次フェーズの
    参照画像として使う)。
  - 「APIで生成」ボタンは残す(課金前提で使いたい場合向け)。3つの手段
    (API生成/コピペ手動生成後にアップロード/直接ファイル選択)を並べて、
    どちらを使ってもタブの確定状態(front/side/back画像)に同じ形で入る
    ようにする。
- ランドマーク推定(タブ5)・パラメータ/アクセサリー提案(タブ6)は画像認識
  なので、これまでどおりAPI呼び出し(Gemini 3.5 Flash)のみで実装してよい
  (手動フォールバック不要)。

## 実装方針(コード構造)

このリポジトリでは直近、`carving.js`/`skeleton.js`/`pipeline.js`の巨大関数
(200〜350行規模)を責務ごとの内部関数に分割するリファクタが行われている。
ゴーストスキャナーは新規実装なので、同じ問題を後から作り込まないよう、
**最初から以下を意識して書く**:

- **関数は単一責務にする**: 「画像をリサイズする」「Gemini APIを呼ぶ」
  「レスポンスJSONを検証する」「canvasに点を描画する」「タブの状態を
  切り替える」等、役割ごとに関数を分ける。1つの関数が「APIを叩いて、
  結果をパースして、DOMも書き換えて、次のタブに進める」のように複数の
  責務を一度に持たないようにする。
- **層で分離する**: (1)Gemini API呼び出し層(プロンプト構築+fetch+
  レスポンスパース、新規セッション/会話継続の切り替えもここに閉じ込める)、
  (2)データ変換層(JSON⇔ツール内部の状態、accessory領域→exclude_maskの
  ラスタライズ等)、(3)UI層(タブ切り替え、プレビュー描画、チャットUI)を
  分け、UI層からAPI層の実装詳細(fetchのURL組み立てや認証ヘッダー等)が
  直接見えないようにする。
- **ファイルを分割する**: 既存ツールのように1ファイル完結のHTMLでもよいが、
  上記の層はできれば別ファイルに分ける(例: `js/scanner_api.js`(API呼び出し
  層)、`js/scanner_render.js`(プレビュー描画、Phase 0で`js/common.js`に
  切り出す共通描画関数を呼ぶ側)、`ghost_scanner.html`(タブUI・状態管理の
  み)。1ファイルに全部書き始めると、後から分割し直すコストの方が高くつく。
- **状態管理を1箇所にまとめる**: タブごとの確定状態(front画像・side画像・
  ランドマークJSON等)を素朴なグローバル変数の乱立にせず、1つの状態
  オブジェクトにまとめ、各タブの描画/次へ進む処理はその状態オブジェクトを
  読み書きする形にする(タブ間でどの値が何に依存しているか追いやすくする)。

## UI構成: フェーズごとのタブ(左→右に進行するウィザード形式)

```
[1.画像投入] → [2.正面生成] → [3.側面生成] → [4.背面生成] → [5.ランドマーク] → [6.パラメータ(任意)] → [7.出力/ジェネレータへ]
```

- 各タブに、実際にAPIへ送信する**プロンプト全文を表示**するパネルを設置(隠さない)
- 前のタブの結果が確定するまで次のタブには進めない直列進行(後戻りしてやり直すことは可能)
- 画像生成タブ(2/3/4)は上記「画像生成の課金化に伴う手動フォールバック」の
  とおり、プロンプトコピペボタン+画像アップロードボタンを併設する

## 3面図生成: 逐次生成方式(同時3枚生成ではない)

1. 元イラストを**先に1024×1024へリサイズ**してからGeminiに渡す(Geminiのリサイズは使わない)
2. **front生成**: リサイズ画像を参照にTポーズ正面を生成。輪郭線を出さない指示、
   人体の正中線を画像中心に揃える指示をプロンプトに含める。確認・必要ならリトライ
3. **side生成**: 確定した**frontを参照画像**として使用(元イラストではなくfront基準にすることで
   キャラクターの一貫性を担保)。確認・必要ならリトライ
4. **back生成**: 同様にfrontを参照。確認・必要ならリトライ

front/side/back生成(タブ2〜4)は**Geminiのセッションを毎回新規にする**(会話履歴を
持ち越さない)。誤った生成結果に後続のリクエストが引きずられるのを防ぐため。

## ランドマーク/パラメータ: チャットによる反復微調整

タブ2〜4とは逆に、タブ5(ランドマーク)・タブ6(パラメータ)は**会話を継続してよい**:
- 初回は検証済みプロンプト(後述)で自動推定
- 推定結果を、front画像上に点/領域をオーバーレイした**簡易プレビュー**で確認
- 誤りがあれば、下部のチャット欄で「肘をもう少し下に」のように自然文で指示すると、
  直前のJSON+画像+会話履歴を踏まえてGeminiが修正後のJSONを返す(タブ内でのみ会話を保持)
- 「この内容で確定」で次のタブへ進み、以降は会話を持ち越さない

## 検証済みのランドマーク推定プロンプト(要点)
実際に試して精度を検証済み。得られた知見:
- **キャリブレーション手順が有効**: 点を打つ前に「シルエットの最上端・最下端・肩幅・
  ウエストのくびれ」を画像から直接読み取らせることで、頭身比率の思い込みによる
  下半身の圧縮(誤差3〜5%程度)を軽減できた
- **左右の定義は「画面に向かって左=_L」で明記する**: 「本人視点の左右(=画面上で反転)」
  という指示は誤って理解されやすく、実際に左右反転という重大な誤りを引き起こした。
  このツールの実データ(`landmarks_ai.json`)で"screen-left = _L"であることを確認済み
- 誤差が大きくなりやすい点(head_top・elbow・knee・ankle・toe)には、失敗しやすい
  傾向を明記した具体的な判断基準を与えると改善する
- セルフチェック手順(シルエット最上端/最下端との整合性確認、左右の再確認)を
  プロンプト末尾に含めると精度が上がる

## 共通化(先行タスク、フェーズ0)
簡易プレビューの描画は、既存の`landmark_tool.html`が持つ点描画(`drawCross`/`drawLabel`)・
領域描画(`drawRegionOutlines`)・色定義(`GCOL`/`LM`)と見た目を揃えたいが、これらは
現状`landmark_tool.html`にベタ書きされていて`js/common.js`(全ツール共通読み込み)には
入っていない。**ゴーストスキャナー着手前に、これらをcanvasコンテキストと座標だけを
受け取る汎用関数として`js/common.js`に切り出すリファクタを先に行う**(landmark_tool.html
側もそちらを呼ぶように変更)。これにより見た目が完全に統一され、将来の変更も1箇所で済む。

## ジェネレータへの引き継ぎ
出力(front/side/back画像+`points_px`+任意で`gen_params`/`accessories`)を
IndexedDB経由で`landmark_tool.html`に渡し、遷移後すぐ編集画面が開くようにする
(ダウンロード→手動アップロードより滑らかな導線)。

**技術的な注意(2026-07-07のリファクタで判明)**: `js/idb.js`の
`saveGeneratedModel`/`loadGeneratedModel`(キー`generated_model`)は
「ジェネレータ→ビューア」専用で、契約が「完成GLB」から「彫刻後の中間
パッケージ(生メッシュ・焼き込み済みcanvas・pivot等)」に変更済みのため、
**この引き継ぎには使えない**。代わりに、`landmark_tool.html`が「前回の
続きから」再開する際に使っている`saveNormalSessionImages`/
`loadNormalSessionImages`(キー`normal_session_images`、`{front,side,back}`
のBlob形式)と**同じ形のBlobを新しい専用キーで保存**し、
`landmark_tool.html`起動時に`?source=scanner`のようなクエリパラメータを
見て、①そのBlobを画像として読み込み、②`landmarks_ai.json`相当のJSONを
`applyLoadedJson`にそのまま渡す、という2段の読み込み分岐を追加する
(`applyLoadedJson`自体は無改修で動作する)。

## フェーズ一覧(効果/コスト/実現性)

| # | フェーズ | 内容 | 効果 | コスト | 実現性 | 状態 |
|---|---|---|---|---|---|---|
| 0 | プレビュー描画ロジックの共通化(先行リファクタ) | `landmark_tool.html`内の`LM`/`GCOL`/`drawCross`/`drawLabel`/`drawRegionOutlines`を`js/common.js`に切り出し | 中(保守性・見た目統一) | S〜M | 高 | 完了 |
| 1 | タブUI骨格+APIキー入力/保存+index.htmlへのカード追加 | 7タブのウィザード枠組み、localStorage管理 | - | M | 高 | 完了 |
| 2 | 画像リサイズ処理(1024×1024) | canvas APIでの前処理(タブ1) | 高 | S | 高 | 完了 |
| 3 | front生成(タブ2)+プロンプト可視化+新規セッション必須 | 単発リクエスト(履歴を持たない) | 高 | M | 中(要プロンプト調整) | 完了 |
| 4 | side生成(タブ3)/back生成(タブ4)+新規セッション必須 | 確定frontを参照に都度新規セッション、個別リトライ | 高 | M | 中(frontの質に依存) | 完了 |
| 5 | ランドマーク自動配置+簡易プレビュー+チャット微調整(タブ5) | 検証済みプロンプトで初回推定→チャットで反復修正→点オーバーレイで確認 | 高 | M | 高 | 完了 |
| 6 | パラメータ/アクセサリー自動提案+除外マスク導出+簡易プレビュー+チャット微調整(タブ6、任意スキップ可) | body/accessory二分類(front+side+back同時入力)でaccessories JSONを取得(髪は房ごとに分割)、`exclude_from_body_silhouette===true`のものだけをexclude_masksにcanvas描画(頭を覆う前髪/後ろ髪は対象外)。チャットで反復修正、領域オーバーレイ表示 | 中 | M〜L | 中 | 完了 |
| 7 | 出力/ジェネレータへの受け渡し(タブ7) | IndexedDB経由で`landmark_tool.html`へ遷移、読み込み分岐を軽微追加 | 高 | M | 高 | 完了 |
| 8 | index.htmlへのカード追加(導線) | 4枚目のカード | 中 | S | 高 | 完了 |
| 9 | front/side/back生成タブへの手動フォールバック追加(プロンプトコピペ+画像アップロード) | フェーズ3・4完了後、画像生成の課金化を受けて追加。詳細は上記「画像生成の課金化に伴う手動フォールバック」参照 | 高(コスト回避) | M | 高 | 未着手 |

## body/accessory の定義(アクセサリー自動検出の判断基準)

「アクセサリー」は、体に密着したものではなく、**通常人体の輪郭からはみ出るもの全般**と定義する
(スカート・マント・広がった袖・大きなリボン・髪の房・帽子・獣耳・尻尾・翼・武器・鞄なども含む)。
判断基準: 「その部分を取り除いたときに、下の体のシルエットが単純な円柱/紡錘形に近い形で
残るか」。体に密着した服(通常のシャツ・ズボン・靴等、体のラインに沿うもの)はbody扱いとし
アクセサリー検出の対象外とする。

## 除外マスク(ジェネレータの「除外」タブ)への対応
`landmark_tool.html`の除外タブは、毛先など「シルエット測定(体の輪郭検出)を狂わせる部分」を
塗って除外する機能(`exclude_masks`、front/side/backごとにcanvas)。**これは新規のAI呼び出しを
追加せず、アクセサリー検出(プロンプトF)の結果を流用して機械的に生成する**:
プロンプトFをfront/side/back**3枚同時入力**に拡張し、各accessoryについて実際に写っている面
ごとの多角形領域を返させ、その多角形をcanvas上に赤で塗りつぶして`exclude_masks[view]`
(PNG dataURL)を合成する(AI呼び出しは1回のまま、accessories出力とexclude_masks出力を
同時に導出する)。

**注意(重要)**: 除外マスク化は**全accessoryに一律適用しない**。`js/pipeline.js`の
`buildDerivedLandmarks`は`analysis.ytop`/`ybot`(除外マスク適用後のシルエット実測値)を
モデル全体のスケール基準として使っているため、**頭部を覆う前髪・後ろ髪のような
"体の地肌そのものを覆っている髪"を除外マスクに含めると、頭部の実測データが画像上に
一切残らなくなり、モデル全体のスケール基準が崩れるリスクがある**(スカートに隠れた脚の
ように「下に本来の輪郭が続くと仮定して補間できる」ケースとは性質が異なる)。そのため
プロンプトFでは、3Dパーツとして分けるかどうか(髪は基本的に全てaccessory化)と、
除外マスクに含めるかどうか(`exclude_from_body_silhouette`)を**別基準**として
判定させ、体の基本輪郭を定義している髪(前髪・後ろ髪等)は除外マスクの対象外とする。

## 最優先の不確実要素
フェーズ3・4(front確定→side/back生成の一貫性)。3面図生成自体は動作実績があるが、
「安定しない」という申告どおりプロンプトの工夫が必須。フェーズ0・1・2(共通化+骨格+
リサイズ)を先に作ってから、このツール上でフェーズ3のプロンプトを繰り返しテストする
順番が効率的。

## 運用ルール
1. 着手するときはこのファイルの状態列を「進行中」に、完了したら「完了」に更新する。
2. フェーズ0は他フェーズの前提(先行タスク)。フェーズ1・2は並行着手可。
3. 実施中に追加の課題が見つかったら、末尾に追記してよい。
4. 各フェーズの実装時は「実装方針(コード構造)」の単一責務・層分離・状態一元化を
   守る。1つの関数/コミットが肥大化してきたら、その場で責務ごとに分割する
   (後回しにして後から一括リファクタしない)。
5. ゴーストスキャナー実装中は、`tests/generation-pipeline.test.js`(golden master
   のGLBハッシュ比較)のような重いテストの実行・追加は省略してよい(既存3ツールの
   彫刻/スキニング処理には触れない機能のため)。既存テストを壊していないことの
   確認は必要だが、本機能向けに新規の重いE2Eテストを追加する必要はない。

## プロンプト集(実装時にそのまま使用する)

### プロンプトA: front(正面Tポーズ)生成

```
入力画像は、ある2Dアニメ調キャラクターの原画です(ポーズ・アングルは不定)。
この画像に描かれたキャラクターのデザイン(顔立ち・髪型・髪色・瞳の色・服装・
配色・アクセサリー)を完全に維持したまま、以下の条件を満たす新しい1枚の画像を
生成してください。

## 生成条件
1. **ポーズ**: 直立・Tポーズ(両腕を真横に一直線に伸ばす、脚は肩幅程度に
   自然に開くか揃える。膝は曲げない)
2. **アングル**: 完全な正面(カメラ方向を向く。体・顔とも横向き成分を含まない)
3. **構図**: キャラクターの**人体の正中線(頭頂から股間を通る中心線)**を、
   画像の水平方向の中心(x=画像幅の50%)に一致させる。イラストの見た目上の
   重心(髪の広がりや小物の位置)ではなく、あくまで骨格の中心線を基準にすること
4. **輪郭線**: 黒い縁取り線(アウトライン)を入れない。面と面の境界は色の差だけで
   表現し、線画的な黒縁は一切使わないこと
5. **背景**: 完全な透過(アルファチャンネル)、または均一な単色背景
6. **余白**: キャラクター全体(頭頂からつま先まで)が画像内に収まるよう、
   上下左右に適度な余白を残すこと(画像端でトリミングされないこと)
7. **画風**: 元画像と同じ画風(線の太さ、塗りの質感、色調)を維持すること

出力は画像のみとしてください。
```

### プロンプトB/C: side(側面)/back(背面)生成

`front`確定後、その画像を入力にして実行(sideの場合の例)。**新規セッションで実行**する。
backは「真横」→「完全に背後(180度)」、「側面図」→「背面図」に置換するだけで流用できる。

```
入力画像は、あるキャラクターの正面Tポーズの立ち絵です。この画像と全く同じ
キャラクターデザイン(髪型・髪色・瞳の色・服装・配色・アクセサリーの形状や
位置)を維持したまま、**真横から見た側面図**を生成してください。

## 生成条件
1. **アングル**: 完全な真横(90度)。前後どちら向きでも構いませんが、体の
   向きが左右で一貫していること(横向きの顔・体で、正面成分を含まない)
2. **ポーズ**: 入力画像と同じTポーズ(直立、腕を体の前後どちらかに真横に
   伸ばした状態、膝は曲げない)
3. **構図**: キャラクターの**人体の正中線**を画像の水平方向の中心に一致させる
4. **輪郭線**: 黒い縁取り線を入れない(入力画像も輪郭線が無い状態のはずなので、
   その画風を維持する)
5. **背景**: 完全な透過、または入力画像と同じ単色背景
6. **余白**: 頭頂からつま先まで画像内に収まるよう適度な余白を残す
7. **一貫性**: 服のシワ・アクセサリーの位置・体型プロポーションが正面図と
   矛盾しないこと(正面で見えていた要素が側面から見て不自然に消えたり、
   別のデザインに変わったりしないこと)

出力は画像のみとしてください。
```

### プロンプトD: ランドマーク推定(検証済み・最終版)

```
あなたは2Dアニメ調キャラクター立ち絵の画像1枚から、3Dモデル生成用の骨格関節点
(合計18点)をピクセル座標で推定する専門家です。添付画像は、キャラクターが
Tポーズ(直立・両腕を真横に一直線に伸ばした姿勢)で描かれた正面(front)の
透過PNGです。

## 手順0: 必ず最初に行うキャリブレーション(内部処理・出力不要)
点を打つ前に、必ず以下を画像から直接読み取ってください(一般的な体型比率の
知識だけで推測せず、この画像自身のシルエット輪郭を見て判断すること):
- キャラクターのシルエット(髪を含む)の最上端・最下端のy座標
- 胴体の左右幅が最も広い場所(肩)と最も狭い場所(ウエスト)のx座標
- 脚の分岐点(股)のy座標
このキャリブレーションを基準にして、以降の各点を「画像全体に対する一般的な
比率」ではなく「このキャラクター自身のシルエット輪郭のどこに実際に線・面の
境目があるか」で判断してください。よくある失敗は、頭身比率の思い込みで
下半身(膝から下)を実際より短く推定してしまうことです。膝・足首・つま先は
必ずシルエットの輪郭線(脚と靴の境目、靴の先端)を直接見て置いてください。

## 左右の定義(重要)
_L/_Rは「画面に向かって左側にあるものが_L、右側にあるものが_R」です
(本人から見た左右ではありません。画像上でそのまま左にある方が_L、
右にある方が_Rと覚えてください)。服・髪で隠れていても、実際の体の
構造上の位置を透視して推定してください。

## 手順1: 各点の詳細な定義

- **head_top**: 髪型を含めた輪郭全体の最上端の1点。つむじや髪飾りの先端では
  なく、シルエット全体で最もyが小さい点。
- **chin**: 顔の輪郭(髪ではなく肌の輪郭)で最もyが大きい点。前髪や後ろ髪の
  影に惑わされず、顔の肌色パーツの下端を見ること。
- **clavicle_L/R**: 首の付け根と肩関節の中間あたりの、体幹の輪郭線上の点。
  肩関節(shoulder)よりも中心寄り・やや上。服の首周りの縫い目があれば目安になる。
- **shoulder_L/R**: 腕を体幹から分離する回転軸の位置。Tポーズで腕が真横に
  伸びている場合、「体幹の輪郭線が終わり、腕の輪郭線が始まる境目のx座標」を
  軸に、その高さでの体幹側面のyと一致させる。腕の一番外側の輪郭(二の腕の
  盛り上がり)と混同しないこと。
- **elbow_L/R**: Tポーズで腕が完全に伸びている場合、肩と手首を結ぶ直線の
  ほぼ中間点(腕の長さの45〜55%あたり)。腕の輪郭線に実際にわずかな
  くびれ・曲がりが描かれていればそこを優先する。腕が真横一直線でなく
  少し下がって描かれている場合は、その角度に沿って肩から中間の位置を取る。
- **wrist_L/R**: 前腕と手のひらの境目(手首の骨のくびれ、または袖口・
  リストバンド等の装飾の位置)。指先まで数えないこと。
- **waist_L/R**: 体幹の左右輪郭のうち、胸から腰にかけて最もx方向にくびれて
  いる(中心線に最も近づいている)高さの左右の点。服のベルト・リボンの
  位置が目安になることが多い。
- **hip**: 両脚のシルエットが分岐を始める、股間に相当するy座標の中心
  (x=シルエット中心線)。スカート等で脚の分岐が見えない場合は、脚の
  付け根の輪郭が左右に開き始める高さを股の位置として推定する。
- **knee_L/R**: 脚のシルエット輪郭で、太もも(上)とすね(下)の境目に
  あたるくびれ・曲がりの位置。ズボン等のシワや服の切り替え線があれば
  目安にする。**過去の失敗傾向として、膝・足首・つま先を実際より上
  (体に近い側)に置きすぎることが多いため、シルエット輪郭の下端付近まで
  しっかり目を配ること。**
- **ankle_L/R**: すねと足(靴)の境目。靴を履いている場合は、靴の甲の
  盛り上がりが始まる位置。
- **toe_L/R**: 足/靴の輪郭で最もyが大きい(=シルエット全体の下端に最も
  近い)点のうち、つま先側の点。**この点はキャラクター全体のシルエット
  最下端とほぼ同じ高さになることが多い**(踵より前に出ている場合)。

## 手順2: セルフチェック
JSON出力前に、以下を自己確認してください:
1. head_topのyは、あなたが手順0で読み取ったシルエット最上端のyと一致しているか
2. toe_L/toe_Rのyは、シルエット最下端のyに近いか(大きく上に外れていないか)
3. _Lと付く点がすべて画像の左半分側、_Rと付く点がすべて画像の右半分側に
   あるか(中心線を挟んで正しい側にあるか)を再確認する
4. shoulder→elbow→wristが概ね直線(Tポーズ)上に並んでいるか

## 出力形式
このAPIレスポンスは自動処理されるため、説明文やMarkdownのコードフェンスは
付けず、有効なJSONオブジェクトのみを出力してください。

{
  "image": {"w": <画像幅>, "h": <画像高さ>},
  "points_px": {
    "head_top": {"x": 0, "y": 0},
    "chin": {"x": 0, "y": 0},
    "clavicle_L": {"x": 0, "y": 0},
    "clavicle_R": {"x": 0, "y": 0},
    "shoulder_L": {"x": 0, "y": 0},
    "shoulder_R": {"x": 0, "y": 0},
    "elbow_L": {"x": 0, "y": 0},
    "elbow_R": {"x": 0, "y": 0},
    "wrist_L": {"x": 0, "y": 0},
    "wrist_R": {"x": 0, "y": 0},
    "waist_L": {"x": 0, "y": 0},
    "waist_R": {"x": 0, "y": 0},
    "hip": {"x": 0, "y": 0},
    "knee_L": {"x": 0, "y": 0},
    "knee_R": {"x": 0, "y": 0},
    "ankle_L": {"x": 0, "y": 0},
    "ankle_R": {"x": 0, "y": 0},
    "toe_L": {"x": 0, "y": 0},
    "toe_R": {"x": 0, "y": 0}
  }
}
```

### プロンプトE: 生成パラメータ提案(参考、精度は中程度)

技術的なメッシュパラメータ(`body_vox`/`psq_*`/`arm_circle`等)は画像から厳密に
読み取れる性質のものではないため、「妥当な既定値からの微調整」を提案させる
程度に留める。

```
添付のキャラクター正面画像を見て、3Dモデル生成時のパラメータについて
簡単な提案をしてください。以下の既定値からの変更が必要と思われる項目のみ、
理由と共に挙げてください(変更不要な項目は既定値のままでよい、無理に
全項目を変えようとしないこと):

- psq_torso/psq_legs/psq_arms(断面の丸み、1.2〜6、2に近いほど丸く自然、
  大きいほど角ばる): キャラクターの体型・衣装が丸みのあるシルエットか、
  ローブや鎧のような角ばった衣装かで判断
- arm_circle(腕を円筒形にする、true/false): 腕が単純な円筒に近い形状か、
  袖や籠手で複雑な形状かで判断
- hand_extrude(手を厚み付き平面として押し出す、true/false): 手が画像内で
  明確に確認できるかどうかで判断

出力はJSON形式のみ({"suggestions": [{"key": "...", "value": ..., "reason": "..."}]})
としてください。判断材料が無い項目は含めないでください。
```

### プロンプトF: body/accessory二分類+アクセサリー領域抽出+除外マスク用データ(改訂版)

front/side/back**3枚同時入力**。exclude_masksもこの結果から機械的に導出する。

```
添付の3枚の画像(front=正面、side=側面、back=背面)は、同一キャラクターの
Tポーズ立ち絵です。これらを横断的に見て、以下の2種類に分類してください。

## 分類基準
- **body(通常人体)**: 頭・首・胴・腕・脚そのもののシルエット、および
  肌や体に密着してその輪郭に沿う衣服(体のラインをそのままなぞる
  シャツ・タイツ・手袋・靴下など)。**このカテゴリの領域は出力不要です**
  (デフォルトで全てbody扱いにするため)。
- **accessory(通常人体の輪郭からはみ出るもの)**: 体そのものの輪郭を
  超えて外側に広がる・突き出るすべてのもの。判断基準は**「その部分を
  取り除いたときに、下の体のシルエットがそのまま単純な円柱/紡錘形に
  近い形で残るか」**です。例:
  - スカート・マント・広がった袖・大きなリボン(体の輪郭より外側に
    張り出す衣服)
  - 髪の房・ツインテール・アホ毛など、頭部の基本シルエットを超えて
    伸びる髪
  - 帽子・ヘッドホン・眼鏡・獣耳・尻尾・翼・武器・鞄などの装着物

体に密着した服(通常のTシャツ・ズボン・靴等、体のラインに沿うもの)は
accessoryに含めないでください。迷った場合は「取り除いても体の基本
シルエットが崩れないか」で判断してください。

## 髪の扱い(重要)
**髪は、地肌に短く密着した坊主・刈り上げのような例外を除き、基本的に
全体をaccessory扱いにしてください**(頭部の基本シルエットからはみ出る
部分だけでなく、髪全体を対象とする)。理由: 髪は体の動きと異なる揺れ方を
する(3Dモデルとして独立して動かしたい)ため。

さらに、**視覚的にひとまとまりに見える房(ふさ)ごとに、別々のaccessoryとして
分割してください**(1つの巨大な「髪」としてまとめないこと)。分割の目安:
- 前髪(顔にかかる部分)と後ろ髪(後頭部から背中側に流れる部分)は別に分ける
- ツインテール・お団子・三つ編みなど、左右や複数に分かれている房はそれぞれ
  個別のaccessoryにする(例: 「左ツインテール」「右ツインテール」)
- アホ毛・後れ毛など、他の髪の塊から明確に独立して飛び出している一房も
  個別のaccessoryにする
- 房の見分けが付かないほど一体化した髪型(お椀型のボブ等)の場合のみ、
  「前髪」「後ろ髪」程度の粗い分割にとどめてよい(無理に房を捏造しない)

髪のaccessoryの取付ボーンは、頭部に根本が近いものは`head`(rigid)、
背中まで長く流れる後ろ髪やツインテールのように体の動きに緩やかに追従
させたいものは`head`+`neck`や`head`+`chest`のような複数ボーン(soft)を
検討してください。

## 除外マスク用フラグ(重要、body/accessory分類とは別基準)
各accessoryについて、**シルエット測定(体の輪郭検出)から除外すべきかどうか**
を別途判定してください。これは「3Dパーツとして分けるかどうか」とは異なる
基準です。判断基準: **その部分の下に、隠れた体の輪郭を推測する手がかりが
無いか**。
- `true`(除外すべき)の例: ツインテール・アホ毛・後れ毛など体の輪郭の外側に
  完全に飛び出している髪、体の輪郭より外側に大きく張り出すスカート・マント
  ・広がった袖(隠れている脚/腕はその下に実在するので推測で構わない)
- `false`(除外すべきでない)の例: **前髪・後ろ髪など、頭部の地肌そのものを
  覆っている髪**(除外すると頭部の輪郭を測定する手がかりが画像上に一切
  残らなくなるため)。体に密着し輪郭をそのまま表す衣服の一部(既にbody
  扱いのものは対象外)

特に注意: 前髪・後ろ髪は3Dパーツとしては分離してaccessory化しますが、
**除外マスクには含めないでください**(`exclude_from_body_silhouette: false`)。
頭部の地肌が画像に描かれていないキャラクターがほとんどのため、ここを
除外すると頭部の基準サイズが求められなくなります。

## 出力手順
1. 検出したaccessoryそれぞれに、以下のパレットから重複しないよう
   順に色を割り当ててください: #7aa2ff, #ff6ad5, #52e0c4, #ffb454,
   #ff8a8a, #b48cff, #8cffb4, #5be6ff
2. 各accessoryについて、取付ボーンを以下から1つ以上選んでください:
   hips, torso, chest, neck, head, clavicle_L, upperarm_L, forearm_L,
   wrist_L, hand_L, clavicle_R, upperarm_R, forearm_R, wrist_R, hand_R,
   thigh_L, shin_L, foot_L, toe_L, thigh_R, shin_R, foot_R, toe_R
   (1つのボーンに固定なら"rigid"、複数ボーンにまたがり体の動きに追従
   するなら"soft"。例: スカート→hips+thigh_L+thigh_R、soft。
   ヘッドホン→head、rigid。尻尾→hips、rigid)
3. 各accessoryについて、上記の「除外マスク用フラグ」を判定してください
4. 各accessoryについて、front/side/backそれぞれの画像に実際に写っている
   場合はその面での多角形領域も、画像のピクセル座標で時計回りに6〜12点
   程度で近似して返してください(写っていない、または隠れて見えない
   面は省略してよい)

## 出力形式(JSONのみ、説明文・コードフェンス不要)
{
  "accessories": [
    {
      "name": "例: スカート",
      "mode": "soft",
      "bones": ["hips", "thigh_L", "thigh_R"],
      "color": "#7aa2ff",
      "reason": "腰から広がり、脚のシルエットの外側に張り出しているため",
      "exclude_from_body_silhouette": true,
      "regions": {
        "front": {"points": [[0,0], [0,0], [0,0]]},
        "side": {"points": [[0,0], [0,0], [0,0]]},
        "back": {"points": [[0,0], [0,0], [0,0]]}
      }
    }
  ]
}
```

**Ghost Scanner側の処理**: このJSONを受け取ったら、①`accessories`フィールドとして
そのまま`landmarks_ai.json`に格納(`exclude_from_body_silhouette`は内部処理用の
一時フィールドなのでこのキー自体は出力JSONから除いてよい)、②`exclude_from_body_silhouette
===true`のaccessoryについてのみ、`regions[view].points`をcanvas上に赤で塗りつぶし
`exclude_masks[view]`(PNG dataURL)として合成する(`false`のもの、特に前髪・後ろ髪は
除外マスクに含めない)。

## 実装時の追記(2026-07-07、フェーズ0〜8実装完了)

- フェーズ0〜8を一通り実装(`js/common.js`への切り出し、`js/scanner_api.js`、
  `js/scanner_render.js`、`ghost_scanner.html`、`index.html`のカード追加、
  `landmark_tool.html`の`?source=scanner`読み込み分岐、`js/idb.js`の
  `SCANNER_IMAGES_KEY`専用キー追加)。
- **不確実要素**: 実際のGemini APIキーでの動作検証ができていない。特に
  `js/scanner_api.js`のエンドポイントURL形式(`v1beta/models/{model}:generateContent`)、
  レスポンスの`inline_data`/`inlineData`キー名、`responseModalities`パラメータの
  厳密な指定方法は公開ドキュメントに基づくbest-effort実装であり、実際に叩いて
  形が違えば調整が必要(エラー時は本文をそのままUIに出すようにしてあるので、
  そこから原因を特定できる設計にしてある)。
- ジェネレータへの引き継ぎは`js/idb.js`の`saveScannerHandoffImages`/
  `loadScannerHandoffImages`/`clearScannerHandoffImages`(専用キー
  `scanner_handoff_images`)+ `sessionStorage`の`scanner_handoff_json`
  (ページ遷移一回きりのJSON受け渡し)という形で実装した。

## 完了: アクセサリー抽出方式を「色分けマップ」方式に変更

### 経緯
タブ6b-2(アクセサリー領域指定)は当初、1件のaccessoryごとにGeminiへ
「front/side/back上のこの部分を25点の多角形で答えよ」と座標を数値で
当てさせる方式で実装した(実装時の追記の直前の版)。しかし実地検証で
精度が低いことが判明した。座標の数値回帰はNano Banana系(画像生成/編集
モデル)が本来得意な作業ではなく、逆に「指定した部分を指定した色で
塗り分けさせる」という**画像編集**タスクにすると、実際に検証用プロンプトを
手動でAI Studioに投げたところ、境界が明瞭でにじみもほぼ無い高精度な結果が
得られた(体=黒・背景=白・各accessory=パレット色の塗り分けで、複雑な
重なり=ツインテールが体の後ろを通る形状も含めて正確に塗り分けられた)。

### 新方式の概要
1. **タブ6b-1(名称・分類・色割当)は現状維持**。ここでaccessoryごとに
   名前・mode・bones・パレット色・exclude_from_body_silhouetteを確定する
   (既存のプロンプトF、変更不要)。
2. **タブ6b-2を全面置き換え**: 「1件ずつ座標を当てさせる」現行方式
   (`promptAccessoryRegion`、`runAccessoryRegions`、1件ずつのGemini呼び出し)
   を廃止し、代わりに以下を行う:
   - front/side/backそれぞれについて、**1回の画像編集呼び出し**で
     「各accessoryを確定済みのパレット色でベタ塗り、体(肌・体に密着した
     衣服)は黒(#000000)、背景は白(#FFFFFF)のまま」という色分けマップ画像を
     生成する(呼び出し回数がaccessory数に依存せず**view数=3回で固定**になり、
     従来より画像生成APIコストが下がるケースが多い)
   - 生成 or **手動アップロードした**色分けマップ画像(front/side/back
     計3枚)を、こちら側のcanvas処理で色ごとに走査し、連結成分抽出で
     accessoryごとの**マスク**(ラスタ、多角形化はしない)を機械的に算出する。
     決定論的なJS処理であり、追加のAPI呼び出しは発生しない
3. **副産物として体シルエットマスクも得られる**: 色分けマップの「黒=体」
   領域は、閾値処理に頼らないピクセル精度の体シルエットマスクとして
   そのまま使える。除外マスク(exclude_from_body_silhouette===trueの
   accessory分)もこのマスク抽出と同じ仕組みで導出する。

### データモデル: 「多角形(手動)」と「マスク(自動)」は統合せず並存させる
検討の結果、**既存の多角形手動編集機能(パーツタブの点ドラッグ、除外タブの
ブラシ)は無改修でそのまま残し**、色分けマップ由来のマスクは**それとは別の、
新しい並行した機能**として追加する(データ形式を1つに統一しようとしたが、
「マスクは自動抽出のみで手動編集はしない」「既存の手動編集は今まで通り
使いたい」という要件から、無理に統合せず2つの独立した仕組みとして共存
させる方が単純で安全と判断した)。
- accessory1件は、**多角形(`regions[view].points`、既存・手動編集可)**か
  **マスク(色分けマップ由来、新規・手動編集不可=再生成/再アップロードでのみ
  更新)**のどちらかの形式でデータを持つ(1accessoryにつきどちらか一方)。
- 既存の多角形形式の保存済みJSON/セッションとの互換性は**維持しない**
  (仕様変更として割り切る。読み込みエラーへの特別な救済処理は作らない)。
- `js/accessories.js`(3Dメッシュ生成本体)は、多角形なら現行通り
  `bboxOf()`で範囲を出し、マスクならマスクの外接矩形(true画素のmin/max)
  で同様に範囲を出す形に分岐対応する(実際の彫刻自体はどちらの場合も
  bbox範囲内のアルファ検出で行われる=既存ロジックのため、この分岐追加は
  小規模で済む見込み)。

### UI変更(landmark_tool.htmlのタブ構成再編)
現行の「領域」(ex、除外の手動ブラシ)と「パーツ＋」(ac、アクセサリーの
手動多角形編集)という並列タブを廃止し、以下の5タブ構成に再編する:

1. **マーク**(lm、既存・無改修)
2. **自動マスク**(新規): front/side/backの色分けマップ画像3枚を並べて
   表示し、そこから抽出されたパーツ一覧(除外フラグ含む)を同じ画面内に
   同時表示する。色分けマップの生成(Gemini画像編集、3回)・手動
   アップロード(3枚)もここで行う。マスクはこのタブでのみ更新され、
   手動での書き換えはできない(再生成/再アップロードのみ)。
3. **手動マスク**(新規、既存2機能の入れ物): タブを開いた直後は編集
   ロック状態で、**明示的なチェックボックスをオンにして初めて編集できる**
   ようにする(誤操作防止、自動マスクと役割が違うことを意識させるため)。
   有効化後、内部のサブ切り替えで従来の「領域」(除外、ブラシ手動編集)と
   「パーツ」(アクセサリー、多角形手動編集)を行き来できる。中身の実装は
   既存のex/acタブのロジックをそのまま流用する(無改修に近い)。
4. **設定値**(params、既存・無改修)
5. **生成**(gen、既存・無改修)

- **`ghost_scanner.html`(タブ6b-2)**: 既存の「各アクセサリーの領域を検出
  (1件ずつ)」ボタン・進捗表示を撤去。代わりに:
  - 「色分けマップを生成(front/side/back)」ボタン(内部で3回の画像編集
    API呼び出し)
  - 「手動でアップロード」ボタン×3(front/side/back、既存のfront/side/back
    生成タブと同じ手動フォールバックパターンを踏襲。ユーザーが外部の
    AI Studio等で生成した色分けマップをそのまま登録できるようにする)
  - 3枚(生成 or アップロード)が揃ったら「領域を抽出」ボタンで色抽出処理を
    実行、結果をプレビュー表示(このマスクデータは手動編集不可)

### 新規実装が必要な処理(色抽出コア)
- 汎用処理(スキャナー・ジェネレータ両方から使うため`js/common.js`に置く):
  1. 対象色との近似判定(色距離が許容誤差以内かどうか。アンチエイリアス
     境界のにじみを吸収するため)
  2. 二値マスク化→連結成分抽出(flood fill/union-find)。**多角形化(輪郭
     追跡)は行わず、ラスタマスクのまま保持する**(データモデルの節の通り、
     多角形とマスクは別形式として並存させるため)
- 想定関数シグネチャ(仮): `P3D.extractMaskFromColormap(ctx, w, h,
  targetColorHex, toleranceOpt)` → `{maskDataUrl, bbox:[x0,y0,x1,y1]} | null`

### 影響ファイル(見込み)
- `js/common.js`: 色抽出コアロジック新規追加
- `js/scanner_api.js`: 色分けマップ生成用プロンプト追加(前セクションで
  検証済みの内容をベースに、accessory一覧から動的にパーツ/色のテーブルを
  組み立てる関数)
- `ghost_scanner.html`: タブ6b-2のUI・JSを置き換え(`promptAccessoryRegion`/
  `runAccessoryRegions`/関連ボタンを削除し、色分けマップ生成+アップロード+
  抽出のフローに差し替え)
- `landmark_tool.html`: タブ構成を5タブ(マーク/自動マスク/手動マスク/
  設定値/生成)に再編。「自動マスク」タブを新規実装し、既存の「領域」
  「パーツ＋」は「手動マスク」タブの内部サブ切り替えとしてロジックほぼ
  無改修のまま移設(チェックボックスによる編集ロックのみ追加)
- `js/accessories.js`: accessoryが多角形かマスクかで範囲計算を分岐
  (`bboxOf(points)` / マスクのtrue画素からのbbox算出)
- スキャナー→ジェネレータの引き継ぎJSON契約: accessoryごとに`regions`
  (多角形、既存)または`mask`(新規フィールド、色分けマップ由来)の
  どちらかを持つ形に**拡張**(既存の`regions`自体の形は変更しない)

### リスク・不確実要素
- 画像編集API呼び出しが増える(view数分=3回)ため、以前遭遇した無料枠
  クォータ超過(HTTP 429 "limit: 0")が起きやすくなる可能性がある
  (ただし従来のaccessory数分の呼び出しよりは合計回数が少なくなるケースが多い)
- 色境界のアンチエイリアス/にじみへの色距離許容誤差のチューニングが必要
  (許容誤差が狭すぎると輪郭が欠ける、広すぎると隣接色を誤って含む)
- 1つのaccessoryが同一面内で複数の分離した領域になるケース(例: 服の柄が
  途中で別パーツに隠れて分断される等)は、現時点では最大連結成分のみを
  採用する想定(複数成分対応は必要になれば追って拡張)
- 実際のGemini画像編集APIでの検証は、ユーザーが手動でAI Studioに投げた
  1回のみ(front面・1キャラクターのみ)。side/back面や複雑な重なりを含む
  別キャラクターでの追加検証はまだ行っていない
- `landmark_tool.html`のタブ構成変更は影響範囲が広い(既存の「領域」
  「パーツ＋」を直接触るユーザーがいる前提のUI変更)。ロジック自体は
  極力無改修で移設する方針だが、CLAUDE.md記載の「単一の巨大IIFEスコープ
  での変数衝突リスク」に注意し、タブ切り替え・表示条件まわりの変数
  (`curTab`等)を編集する際は全体grepを徹底すること

### 状態
完了(2026-07-08)。実装内容:
- `js/common.js`: `P3D.extractMaskFromColormap(ctx,w,h,targetColorHex,tolerance)`
  (色距離判定→二値化→最大連結成分抽出→ラスタマスクdataURL+bbox)を追加。
  `P3D.hexToRgb`もヘルパーとして追加。
- `js/scanner_api.js`: `P3D.scannerBuildColormapPrompt(accessories, viewLabel)`
  で色分けマップ生成プロンプトを動的に組み立てる関数を追加。
- `ghost_scanner.html`: タブ6-2を「色分けマップを生成(front/side/back)」+
  手動アップロード×3+「領域を抽出」ボタンに置き換え(`promptAccessoryRegion`/
  `runAccessoryRegions`は削除)。抽出結果は`acc.mask={front,side,back}`
  (各`{maskDataUrl,bbox}`)に格納し、ジェネレータへの引き継ぎJSON
  (`landmarks_ai.json`相当)の`accessories[].mask`にそのまま乗せる
  (`regions`と排他)。除外マスク合成(`scannerBuildExcludeMaskDataUrl`)も
  マスク形式のaccessoryに対応。
- `js/accessories.js`: `stageAccessories()`のregions/mask分岐を追加。マスクの
  場合はbboxの4隅をfrontPointsToModel等に通してモデル座標のbboxを求めるのみで、
  多角形(frontPolygon等)は生成しない(carveRegion側は素のbbox+アルファ検出の
  みで彫刻する=計画通り)。
- `landmark_tool.html`: タブを「マーク/自動マスク/手動マスク/設定値/生成」の
  5タブに再編。「自動マスク」タブは新規(マスク由来accessoryのプレビュー3枚+
  一覧表示、読み込み専用。再抽出はゴーストスキャナー側で行う設計)。
  「手動マスク」タブは既存の「領域」(ex)「パーツ＋」(ac)をサブタブとして
  無改修に近い形で格納し、チェックボックス(`#manualMaskUnlock`)で編集ロック
  (既定でロック)。`curTab`の値自体は"ex"/"ac"のまま維持し(全体grep済み、
  衝突なし)、トップレベルのタブボタン/パネル表示だけ`MANUAL_MASK_SUBTABS`で
  特別扱いする形にした。
- スキャナー→ジェネレータの引き継ぎJSON契約: `accessories[]`の各要素が
  `regions`(多角形)または`mask`(色分けマップ由来)のどちらか一方を持つ形に
  拡張(`js/accessories.js`の分岐に合わせ、両方揃っていればregions優先)。
- `tests/generator-ui.test.js`/`tests/generation-pipeline.test.js`: タブ構成
  変更に合わせてセレクタを更新(`.tabbtn[data-tab="ex"/"ac"]`→
  `.tabbtn[data-tab="manualmask"]`+`#manualMaskUnlock`+`[data-subtab="ex"/"ac"]`)。
- `npm test`: `generation-pipeline`(golden hash、既存のregions経路は変更なし
  のためハッシュ不変)・`generator-ui`・`viewer-ui`はPASS。`controller-ui`は
  本変更と無関係のファイル(`controller.html`は無変更)での既存の失敗
  (PIPカメラ表示のアサーション)で、この作業による影響ではない。
- 実機でのGemini画像編集API検証は未実施(APIキーなし、計画書の既知の
  不確実要素を参照)。

## 完了: APIキーUIをボタン化+パラメータ提案機能の削除

### 1. APIキー設定UIをボタン化
現状、`ghost_scanner.html`のAPIキー入力欄・モデル選択欄は、ページ上部の
カードとして**どのタブを開いていても常に表示**されている。これを右上の
「APIキー」ボタン1つに集約し、押したときだけ設定パネル(APIキー入力/保存/
削除・モデル選択・モデル一覧取得)を開閉するUIに変更する。

- 配置: `<h1>ゴーストスキャナー`と同じ行の右端(既存の`.back`リンクと
  被らない位置)に「⚙ APIキー」のようなボタンを追加
- 開閉: ボタン押下でパネルの表示/非表示をトグル(既存の`<details>`要素
  流用、またはクラス切り替えのどちらでも可。実装時に既存のプロンプト
  パネル(`details.promptPanel`)と同じ`<details>`パターンに寄せると
  スタイルの再利用ができて楽)
- 中身(APIキー入力・保存・削除、モデル選択、モデル一覧取得ボタン)は
  **既存のHTML構造・IDをそのまま**移設するだけで、ロジック(`refreshApiKeyStatus`
  等)の変更は不要な見込み
- APIキー未設定時に生成/推定ボタンを押すと、`callGemini`側で
  「APIキーが設定されていません」というエラーが返る既存動作はそのまま
  活かせる(必要ならエラーメッセージ内で「右上のAPIキーボタンから設定して
  ください」と誘導文言を足す程度の変更に留める)

### 2. パラメータ提案機能(タブ6a)の削除
`GHOST_SCANNER_PLAN.md`のフェーズ一覧では「パラメータ/アクセサリー自動提案」
として1つの任意フェーズだったが、実装後の所感として**パラメータ提案
(psq_torso等の数値サジェスト)は精度・有用性ともに低く不要**と判断。
以下を削除する:

- UI: タブ「6.パラメータ」内の「6a. 生成パラメータ提案(任意)」カード
  一式(プロンプト表示パネル・提案取得ボタン・提案一覧表示・チャットUI)
- プロンプト: `PROMPT_E`の削除
- JS: `runParamsInitial`/`sendParamsChat`/`renderParamSuggestions`、
  `state.params`(`{suggestions, history}`)の削除
- 出力: `buildLandmarksAiJson()`内の`gen_params`組み立てブロックを削除
  (`state.params.suggestions`参照ごと除去。ジェネレータ側
  `landmark_tool.html`の`applyLoadedJson`は`d.gen_params||{}`という
  デフォルト値付きの読み方をしているため、`gen_params`キー自体が
  無いJSONを渡しても**無改修で動作する**、既存の後方互換性あり)
- タブ構成: 「6b. アクセサリー検出」カード群(6b-1/6b-2、色分けマップ方式
  計画と統合される予定)だけが残るので、タブラベルは`TAB_LABEL.params`の
  表示名を「6.パラメータ」→「6.アクセサリー」に変更する。ただし
  **内部のタブキー自体(`"params"`という文字列)は変更しない**(`TAB_ORDER`・
  `state.curTab`・各所の`switchTab("params")`呼び出し等、参照箇所が多く
  リネームの実益が薄いため、表示名だけ変えて済ませる)

### 影響ファイル(見込み)
- `ghost_scanner.html`のみ(HTML構造・JS両方。他ファイルへの影響なし)

### リスク・不確実要素
- 低リスク。パラメータ提案機能は他機能から参照されておらず、削除しても
  ランドマーク推定・アクセサリー検出・出力処理のいずれにも影響しない
  (`gen_params`が無いJSONは`landmark_tool.html`側で問題なく読める)
- APIキーボタン化は純粋なUI配置変更で、ロジックはHTML移設のみ

### 状態
完了(2026-07-08)。`ghost_scanner.html`に実装済み、mainへpush済み。

## 計画(未着手): 色分けマップ方式・運用面の修正6点

色分けマップ方式の実装後、実際にモデルを生成してみたユーザーからの
フィードバックをもとに調査し、原因を特定した。以下6点、状態は全て未着手。

### ①色分けマップ生成プロンプトのプレビュー表示が正面(front)固定になっている
**実際の生成処理(`generateColormaps()`)自体は正しく面ごとに
`VIEW_LABEL_JP[v]`("正面(front)"/"側面(side)"/"背面(back)")を渡しており、
プロンプト本文(`P3D.scannerBuildColormapPrompt`)も`viewLabel`引数を使う
共通実装になっている(front専用にハードコードされてはいない)。**
バグがあるのは表示専用の`refreshColormapPromptText()`で、常に
`VIEW_LABEL_JP.front`を渡しているため、タブ6-2の「送信プロンプト全文を
表示」パネルが常に「正面(front)」表記になり、あたかもfront専用のように
見えてしまっていた。
- 修正方針: プレビューの文言を「各面(front/side/back)共通」のような
  view非依存の表現に変える(またはfront/side/back3つ分のプロンプトを
  タブ内に並べて見せる)。実際の生成ロジックは変更不要。

### ②マスク抽出が最大連結成分のみで、1パーツが複数領域に分かれる場合に対応していない
`js/common.js`の`extractMaskFromColormap()`は`largestComponent()`で
**最大の連結成分1つだけ**を採用しており、同一色で塗られた領域が画面内で
複数の孤立した塊に分かれるケース(例: 独立した左右の房が同色指定に
なっている、体の別部位に隠れて視覚的に分断されている等)では、小さい方の
塊が失われる。計画時点で把握していたリスクだったが(GHOST_SCANNER_PLAN.md
「リスク・不確実要素」に記載済み)、実際に問題として顕在化した。
- 修正方針: `largestComponent`を「一定面積以上の全連結成分」を採用する
  版に変更し、複数成分をマスクとして合成(OR)する。1accessory1viewにつき
  複数の分離した塊があってもすべて拾えるようにする。

### ③モデル生成後にアクセサリーの位置がずれる(添付画像で確認)
**根本原因を特定(2026-07-08訂正: Gemini側ではなく、このツール自身の
既存バグ)**。当初「色分けマップ画像と元画像のピクセル寸法が食い違う
(Gemini起因)」という仮説を立てたが、指摘を受けて`js/pipeline.js`を
確認したところ、**マスク方式かどうかに関係なく、アクセサリー全般に
影響する既存の構造的バグ**であることが判明した。

`js/pipeline.js`の`runCarvingStages()`(仮称)内、体本体の彫刻
(`stageVisualHull`)は面ごとに個別のサイズを正しく渡している:
```js
var body = P3D.stageVisualHull({
  faW:sizes.front.w, faH:sizes.front.h, saW:sizes.side.w, saH:sizes.side.h, ...
});
```
一方、アクセサリーの彫刻(`stageAccessories`)は**front基準の1組の
W,Hだけ**を全ビューに使い回している:
```js
acc = P3D.stageAccessories({
  frontRgba: rgbaFull.front, backRgba: rgbaFull.back, sideRgba: rgbaFull.side,
  W: sizes.front.w, H: sizes.front.h,   // ← back/sideにもこれを使い回す
  ...
});
```
`js/accessories.js`側もこの単一`W,H`を、back画像のalpha検出範囲切り出し
(`localAlpha(opts.backRgba, W, H, ...)`)やbackの左右反転計算
(`backPointsToModel`内の`W-p[0]-CX`)にそのまま使っている。つまり
**front/side/back画像のピクセル寸法が1pxでも食い違うと、back/side側の
アクセサリー切り出し位置・スケールがズレる**構造になっている。

スキャナー経由のfront/side/backはそれぞれ別のGemini生成呼び出しの
結果であり、寸法が完全一致する保証は無い(この意味でGeminiの挙動も
遠因ではあるが)、**「サイズが違っても正しく変換できる処理がツール側に
そもそも無い」のが直接の原因**であり、マスクの抽出方法(色分けマップ
経由かどうか)とは無関係。多角形方式でも本来同じバグを踏む可能性が
あったが、色分けマップ方式で実際にfront/side/backのサイズがずれやすい
運用が増えたことで初めて表面化したと考えられる。

ユーザー指摘の「相対ではなく絶対位置でよい」は、`back_offset_x/y`等の
手動オフセット調整の話ではなく、そもそも**面ごとのサイズを正しく扱えば
ズレは発生しない**、という理解で一致。

- 修正方針: `js/pipeline.js`の`stageAccessories`呼び出しを
  `stageVisualHull`と同様に、`sizes.front`/`sizes.side`/`sizes.back`を
  個別に渡す形に変更する。`js/accessories.js`(`stageAccessories`内の
  `localAlpha`呼び出し・`backPointsToModel`/`sidePointsToModel`)も、
  front用のW,Hとback/side用のW,Hを区別して使うように修正する
  (`stageVisualHull`/`carveRegion`側が既に面ごとのサイズを扱っている
  実装を参考にする)。
- 以前の仮説(色分けマップ画像自身のサイズを基準にbboxを計算している点)
  も、マスクのbbox自体がどの面のものか(front/side/back)に応じて正しい
  面サイズと突き合わせる必要がある、という点では上記の修正に包含される。
  独立した追加のリスケール処理は不要と判断する(このバグ修正で解消する
  見込みのため)。

### ④ゴーストスキャナー側でマスク抽出結果のプレビューが無い
`ghost_scanner.html`の`renderMaskList()`は「マスク取得済みの面=
front,side,back」というテキスト表示のみで、抽出したマスクの形状を
画像上に重ねて確認する手段が無い(旧・多角形方式にあった`accPreview`
canvas+`drawAccessoryRegionsPreview`相当の仕組みが、色分けマップ方式へ
移行した際に作られていなかった)。なお`landmark_tool.html`側の
「自動マスク」タブには視覚的なオーバーレイプレビュー(後述⑤)が
既にあるため、確認自体は不可能ではないが、生成元であるスキャナー側で
確認できないのは手戻り(ジェネレータまで進んでから初めて誤りに気づく)
の原因になる。
- 修正方針: タブ6-2に、`landmark_tool.html`の「自動マスク」タブと同様の
  プレビュー(front/side/back画像+抽出マスクを半透明重ね描き)を追加する。

### ⑤ジェネレータの「自動マスク」タブでマスク画像が画面サイズに対して大きすぎる
原因はCSSセレクタの取りこぼし。`#automaskColormaps img{max-width:110px;...}`
というルールが定義されているが、実際に`renderAutomaskTab()`が生成して
いるのは`<img>`ではなく`<canvas>`要素(`c.width=im.w; c.height=im.h`と
元画像の実寸そのまま)であり、このCSSルールは`canvas`には一切適用されない。
そのため各viewのプレビューが実寸(数百〜1024px超)のまま表示され、画面から
はみ出るほど大きくなっていた。
- 修正方針: CSSセレクタを`#automaskColormaps canvas`に修正する(または
  `img`と両対応にする)。合わせてスマホ幅でも収まるよう、コンテナの
  `flex-wrap`列でのサイズ配分(3枚横並びなら1枚あたり計算幅を指定する等)
  も見直す。

### ⑥ゴーストスキャナーにも自動保存/前回の続きから機能がほしい
`landmark_tool.html`は`NORMAL_SESSION_KEY`(localStorage)への
`autosaveInterval`定期保存+起動時の「新規作成」/「前回の続きから」選択
(`checkContinueAvailable`)という仕組みを持つが、`ghost_scanner.html`は
状態がすべてメモリ上の`state`オブジェクトのみで、ページを閉じる/
リロードすると生成途中の内容(front/side/back画像、ランドマーク、
アクセサリー一覧、色分けマップ、マスク等)がすべて失われる。
- 修正方針: `landmark_tool.html`と同じパターンを踏襲する:
  - `state`のシリアライズ可能な部分(dataURL/JSON、画像はBlobとして
    IndexedDB、それ以外はlocalStorageのJSON)を一定間隔で自動保存する
  - 起動時に保存済みセッションの有無を検出し、「新規作成」/「前回の
    続きから」を選ばせる導線を追加する(現状のタブ1直行の初期表示を
    見直す必要がある)
  - 保存キーは`landmark_tool.html`の`SCANNER_IMAGES_KEY`
    (`scanner_handoff_images`)等と衝突しない専用キーを新設する

### 影響ファイル(見込み)
- `js/common.js`: ②のマスク合成ロジック変更
- `js/pipeline.js`・`js/accessories.js`: ③(stageAccessoriesへの面ごとの
  サイズ受け渡し、localAlpha/backPointsToModel/sidePointsToModelの
  W,H使い回し修正)
- `ghost_scanner.html`: ①④⑥
- `landmark_tool.html`: ⑤
- `js/idb.js`: ⑥用の保存キー追加の可能性

### 状態
完了(2026-07-08)。実装内容:
- ①: `ghost_scanner.html`の`refreshColormapPromptText()`が常に`VIEW_LABEL_JP.front`
  固定でプレビュー表示していたバグを修正。front/side/backそれぞれの実際の
  送信文面を並べて表示する形にし、summary文言も「front/side/back共通の
  テンプレート」である旨に修正した(実際の生成処理`generateColormaps()`/
  `P3D.scannerBuildColormapPrompt`自体は元々view別に正しく渡していたため無改修)。
- ②: `js/common.js`に`P3D.significantComponentsMask(mask,w,h,minAreaPx)`を追加
  (一定面積以上の全連結成分をOR合成、既定の最小面積16px未満はアンチエイリアス
  ノイズとみなして除外)。`extractMaskFromColormap()`が`largestComponent`ではなく
  こちらを使うように変更(引数`minAreaPxOpt`を追加、`largestComponent`自体は
  `loadRgbaRemoveWhite`向けに無改修のまま残置)。
- ③: `js/pipeline.js`の`stageAccessories`呼び出しを、`stageVisualHull`と同様に
  `faW/faH`(front+back用。carving.jsの前提通りback/frontは同サイズ)と
  `saW/saH`(side専用)を区別して渡す形に変更。`js/accessories.js`の
  `stageAccessories`内(`localAlpha`のfront/back/side呼び出し、
  `backPointsToModel`の反射計算、`carveRegion`呼び出しのfaW/faH/saW/saH)も
  同様に区別するよう修正。サンプル画像(front/side/back全て1024×1024)では
  数値上front=side=back寸法のため`generation-pipeline.test.js`のgolden hashは
  変化なし(PASS確認済み)。
- ④: `ghost_scanner.html`のタブ6-2に`renderMaskPreview()`を追加。
  `landmark_tool.html`の「自動マスク」タブと同じ重ね描きパターン(front/side/back
  画像+抽出済みマスクをalpha 0.55で重畳)で、`#maskPreviewList`に表示する
  (`extractMasks()`実行後・確定解除時にクリア)。
- ⑤: `landmark_tool.html`の`#automaskColormaps img`セレクタが実際は`<canvas>`
  要素で効いていなかったCSSを修正(`canvas`セレクタ追加+`.automaskItem`で
  幅上限140pxを指定)。
- ⑥: `js/idb.js`に`saveScannerSessionImages`/`loadScannerSessionImages`/
  `clearScannerSessionImages`(専用キー`scanner_session_images`)を追加。
  `ghost_scanner.html`に`landmark_tool.html`と同様の自動保存(4秒間隔ポーリング、
  変化があればlocalStorageのJSON+IndexedDBのBlobを保存)と、起動時の
  「前回の続きから」/「破棄して新規作成」カードを追加。保存対象はfront/side/back
  生成画像・色分けマップ画像・ランドマーク・アクセサリー一覧(色分けマップ由来の
  抽出済みマスクは対象外、色分けマップさえ復元できれば無料で再抽出可能なため)。
- 検証: Playwrightで①②③④⑥それぞれ実データに近い形の動作確認を実施
  (コンソールエラーなし)。`npm test`は`generation-pipeline`/`generator-ui`/
  `viewer-ui`がPASS、`controller-ui`は本変更と無関係(`controller.html`無改修)の
  既存の失敗(PIPカメラ表示のアサーション)。
