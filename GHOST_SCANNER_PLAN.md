# ゴーストスキャナー(AI前処理ツール)計画書

## 目的
1枚の任意イラスト(ポーズ・アングル不問)を入力し、Gemini APIを使って
①front(Tポーズ正面)/side/backの3面図を段階的に生成、②各面のランドマーク・
生成パラメータを自動推定、③既存のジェネレータ(`landmark_tool.html`)へ
そのまま引き継げる形で出力する、半自動の前処理ツール。既存の3ツール
(ゴーストジェネレータ/ゴーストビューア/ゴーストコントローラー)と並ぶ
4つ目のツールとして`index.html`に追加する。

## 名称
**ゴーストスキャナー**(Ghost Scanner)。1枚の絵から"本当の姿"(3面図+骨格情報)
を読み取る機能に合わせた、既存ツール群と同じ「ゴースト+平易な英単語」の命名規則。

## APIキーの取り扱い
- `geminiAPIkey`リポジトリ(このリポジトリとは別)にキーを保管(user管理、期限付きで
  ローテーションする前提)。
- ゴーストスキャナー自体はキーをコードに埋め込まない。初回起動時に入力欄へ
  手動で貼り付け、`localStorage`に保存して以降は自動読込する(GitHubからの
  自動取得はしない=「鍵を取ってくるための鍵」問題を避けるため)。

## UI構成: フェーズごとのタブ(左→右に進行するウィザード形式)

```
[1.画像投入] → [2.正面生成] → [3.側面生成] → [4.背面生成] → [5.ランドマーク] → [6.パラメータ(任意)] → [7.出力/ジェネレータへ]
```

- 各タブに、実際にAPIへ送信する**プロンプト全文を表示**するパネルを設置(隠さない)
- 前のタブの結果が確定するまで次のタブには進めない直列進行(後戻りしてやり直すことは可能)

## 3面図生成: 逐次生成方式(同時3枚生成ではない)

1. 元イラストを**先に1024×1024へリサイズ**してからGeminiに渡す(Geminiのリサイズは使わない)
2. **front生成**: リサイズ画像を参照にTポーズ正面を生成。輪郭線を出さない指示、
   人体の正中線を画像中心に揃える指示をプロンプトに含める。確認・必要ならリトライ
3. **side生成**: 確定した**frontを参照画像**として使用(元イラストではなくfront基準にすることで
   キャラクターの一貫性を担保)。確認・必要ならリトライ
4. **back生成**: 同様にfrontを参照。確認・必要ならリトライ

front/side/back生成(タブ2〜4)は**Geminiのセッションを毎回新規にする**(会話履歴を
持ち越さない)。誤った生成結果に後続のリクエストが引きずられるのを防ぐため。

## ランドマーク/パラメータ: チャットによる反復微調整

タブ2〜4とは逆に、タブ5(ランドマーク)・タブ6(パラメータ)は**会話を継続してよい**:
- 初回は検証済みプロンプト(後述)で自動推定
- 推定結果を、front画像上に点/領域をオーバーレイした**簡易プレビュー**で確認
- 誤りがあれば、下部のチャット欄で「肘をもう少し下に」のように自然文で指示すると、
  直前のJSON+画像+会話履歴を踏まえてGeminiが修正後のJSONを返す(タブ内でのみ会話を保持)
- 「この内容で確定」で次のタブへ進み、以降は会話を持ち越さない

## 検証済みのランドマーク推定プロンプト(要点)
実際に試して精度を検証済み。得られた知見:
- **キャリブレーション手順が有効**: 点を打つ前に「シルエットの最上端・最下端・肩幅・
  ウエストのくびれ」を画像から直接読み取らせることで、頭身比率の思い込みによる
  下半身の圧縮(誤差3〜5%程度)を軽減できた
- **左右の定義は「画面に向かって左=_L」で明記する**: 「本人視点の左右(=画面上で反転)」
  という指示は誤って理解されやすく、実際に左右反転という重大な誤りを引き起こした。
  このツールの実データ(`landmarks_ai.json`)で"screen-left = _L"であることを確認済み
- 誤差が大きくなりやすい点(head_top・elbow・knee・ankle・toe)には、失敗しやすい
  傾向を明記した具体的な判断基準を与えると改善する
- セルフチェック手順(シルエット最上端/最下端との整合性確認、左右の再確認)を
  プロンプト末尾に含めると精度が上がる

## 共通化(先行タスク、フェーズ0)
簡易プレビューの描画は、既存の`landmark_tool.html`が持つ点描画(`drawCross`/`drawLabel`)・
領域描画(`drawRegionOutlines`)・色定義(`GCOL`/`LM`)と見た目を揃えたいが、これらは
現状`landmark_tool.html`にベタ書きされていて`js/common.js`(全ツール共通読み込み)には
入っていない。**ゴーストスキャナー着手前に、これらをcanvasコンテキストと座標だけを
受け取る汎用関数として`js/common.js`に切り出すリファクタを先に行う**(landmark_tool.html
側もそちらを呼ぶように変更)。これにより見た目が完全に統一され、将来の変更も1箇所で済む。

## ジェネレータへの引き継ぎ
出力(front/side/back画像+`points_px`+任意で`gen_params`/`accessories`)を
IndexedDB経由で`landmark_tool.html`に渡し、遷移後すぐ編集画面が開くようにする
(ダウンロード→手動アップロードより滑らかな導線)。`landmark_tool.html`側に
読み込み分岐を1つ追加する軽微な改修が必要。基本フォーマットは`landmarks_ai.json`
v2互換のため`applyLoadedJson`は無改修で動作するはず。

## フェーズ一覧(効果/コスト/実現性)

| # | フェーズ | 内容 | 効果 | コスト | 実現性 | 状態 |
|---|---|---|---|---|---|---|
| 0 | プレビュー描画ロジックの共通化(先行リファクタ) | `landmark_tool.html`内の`LM`/`GCOL`/`drawCross`/`drawLabel`/`drawRegionOutlines`を`js/common.js`に切り出し | 中(保守性・見た目統一) | S〜M | 高 | 未着手 |
| 1 | タブUI骨格+APIキー入力/保存+index.htmlへのカード追加 | 7タブのウィザード枠組み、localStorage管理 | - | M | 高 | 未着手 |
| 2 | 画像リサイズ処理(1024×1024) | canvas APIでの前処理(タブ1) | 高 | S | 高 | 未着手 |
| 3 | front生成(タブ2)+プロンプト可視化+新規セッション必須 | 単発リクエスト(履歴を持たない) | 高 | M | 中(要プロンプト調整) | 未着手 |
| 4 | side生成(タブ3)/back生成(タブ4)+新規セッション必須 | 確定frontを参照に都度新規セッション、個別リトライ | 高 | M | 中(frontの質に依存) | 未着手 |
| 5 | ランドマーク自動配置+簡易プレビュー+チャット微調整(タブ5) | 検証済みプロンプトで初回推定→チャットで反復修正→点オーバーレイで確認 | 高 | M | 高 | 未着手 |
| 6 | パラメータ/アクセサリー自動提案+除外マスク導出+簡易プレビュー+チャット微調整(タブ6、任意スキップ可) | body/accessory二分類(front+side+back同時入力)でaccessories JSONを取得、同じ結果からexclude_masksをcanvasに機械的に描画。チャットで反復修正、領域オーバーレイ表示 | 中 | M〜L | 中 | 未着手 |
| 7 | 出力/ジェネレータへの受け渡し(タブ7) | IndexedDB経由で`landmark_tool.html`へ遷移、読み込み分岐を軽微追加 | 高 | M | 高 | 未着手 |
| 8 | index.htmlへのカード追加(導線) | 4枚目のカード | 中 | S | 高 | 未着手 |

## body/accessory の定義(アクセサリー自動検出の判断基準)

「アクセサリー」は、体に密着したものではなく、**通常人体の輪郭からはみ出るもの全般**と定義する
(スカート・マント・広がった袖・大きなリボン・髪の房・帽子・獣耳・尻尾・翼・武器・鞄なども含む)。
判断基準: 「その部分を取り除いたときに、下の体のシルエットが単純な円柱/紡錘形に近い形で
残るか」。体に密着した服(通常のシャツ・ズボン・靴等、体のラインに沿うもの)はbody扱いとし
アクセサリー検出の対象外とする。

## 除外マスク(ジェネレータの「除外」タブ)への対応
`landmark_tool.html`の除外タブは、毛先など「シルエット測定(体の輪郭検出)を狂わせる部分」を
塗って除外する機能(`exclude_masks`、front/side/backごとにcanvas)。**これは新規のAI呼び出しを
追加せず、アクセサリー検出(プロンプトF)の結果を流用して機械的に生成する**:
プロンプトFをfront/side/back**3枚同時入力**に拡張し、各accessoryについて実際に写っている面
ごとの多角形領域を返させ、その多角形をcanvas上に赤で塗りつぶして`exclude_masks[view]`
(PNG dataURL)を合成する(AI呼び出しは1回のまま、accessories出力とexclude_masks出力を
同時に導出する)。

## 最優先の不確実要素
フェーズ3・4(front確定→side/back生成の一貫性)。3面図生成自体は動作実績があるが、
「安定しない」という申告どおりプロンプトの工夫が必須。フェーズ0・1・2(共通化+骨格+
リサイズ)を先に作ってから、このツール上でフェーズ3のプロンプトを繰り返しテストする
順番が効率的。

## 運用ルール
1. 着手するときはこのファイルの状態列を「進行中」に、完了したら「完了」に更新する。
2. フェーズ0は他フェーズの前提(先行タスク)。フェーズ1・2は並行着手可。
3. 実施中に追加の課題が見つかったら、末尾に追記してよい。

## プロンプト集(実装時にそのまま使用する)

### プロンプトA: front(正面Tポーズ)生成

```
入力画像は、ある2Dアニメ調キャラクターの原画です(ポーズ・アングルは不定)。
この画像に描かれたキャラクターのデザイン(顔立ち・髪型・髪色・瞳の色・服装・
配色・アクセサリー)を完全に維持したまま、以下の条件を満たす新しい1枚の画像を
生成してください。

## 生成条件
1. **ポーズ**: 直立・Tポーズ(両腕を真横に一直線に伸ばす、脚は肩幅程度に
   自然に開くか揃える。膝は曲げない)
2. **アングル**: 完全な正面(カメラ方向を向く。体・顔とも横向き成分を含まない)
3. **構図**: キャラクターの**人体の正中線(頭頂から股間を通る中心線)**を、
   画像の水平方向の中心(x=画像幅の50%)に一致させる。イラストの見た目上の
   重心(髪の広がりや小物の位置)ではなく、あくまで骨格の中心線を基準にすること
4. **輪郭線**: 黒い縁取り線(アウトライン)を入れない。面と面の境界は色の差だけで
   表現し、線画的な黒縁は一切使わないこと
5. **背景**: 完全な透過(アルファチャンネル)、または均一な単色背景
6. **余白**: キャラクター全体(頭頂からつま先まで)が画像内に収まるよう、
   上下左右に適度な余白を残すこと(画像端でトリミングされないこと)
7. **画風**: 元画像と同じ画風(線の太さ、塗りの質感、色調)を維持すること

出力は画像のみとしてください。
```

### プロンプトB/C: side(側面)/back(背面)生成

`front`確定後、その画像を入力にして実行(sideの場合の例)。**新規セッションで実行**する。
backは「真横」→「完全に背後(180度)」、「側面図」→「背面図」に置換するだけで流用できる。

```
入力画像は、あるキャラクターの正面Tポーズの立ち絵です。この画像と全く同じ
キャラクターデザイン(髪型・髪色・瞳の色・服装・配色・アクセサリーの形状や
位置)を維持したまま、**真横から見た側面図**を生成してください。

## 生成条件
1. **アングル**: 完全な真横(90度)。前後どちら向きでも構いませんが、体の
   向きが左右で一貫していること(横向きの顔・体で、正面成分を含まない)
2. **ポーズ**: 入力画像と同じTポーズ(直立、腕を体の前後どちらかに真横に
   伸ばした状態、膝は曲げない)
3. **構図**: キャラクターの**人体の正中線**を画像の水平方向の中心に一致させる
4. **輪郭線**: 黒い縁取り線を入れない(入力画像も輪郭線が無い状態のはずなので、
   その画風を維持する)
5. **背景**: 完全な透過、または入力画像と同じ単色背景
6. **余白**: 頭頂からつま先まで画像内に収まるよう適度な余白を残す
7. **一貫性**: 服のシワ・アクセサリーの位置・体型プロポーションが正面図と
   矛盾しないこと(正面で見えていた要素が側面から見て不自然に消えたり、
   別のデザインに変わったりしないこと)

出力は画像のみとしてください。
```

### プロンプトD: ランドマーク推定(検証済み・最終版)

```
あなたは2Dアニメ調キャラクター立ち絵の画像1枚から、3Dモデル生成用の骨格関節点
(合計18点)をピクセル座標で推定する専門家です。添付画像は、キャラクターが
Tポーズ(直立・両腕を真横に一直線に伸ばした姿勢)で描かれた正面(front)の
透過PNGです。

## 手順0: 必ず最初に行うキャリブレーション(内部処理・出力不要)
点を打つ前に、必ず以下を画像から直接読み取ってください(一般的な体型比率の
知識だけで推測せず、この画像自身のシルエット輪郭を見て判断すること):
- キャラクターのシルエット(髪を含む)の最上端・最下端のy座標
- 胴体の左右幅が最も広い場所(肩)と最も狭い場所(ウエスト)のx座標
- 脚の分岐点(股)のy座標
このキャリブレーションを基準にして、以降の各点を「画像全体に対する一般的な
比率」ではなく「このキャラクター自身のシルエット輪郭のどこに実際に線・面の
境目があるか」で判断してください。よくある失敗は、頭身比率の思い込みで
下半身(膝から下)を実際より短く推定してしまうことです。膝・足首・つま先は
必ずシルエットの輪郭線(脚と靴の境目、靴の先端)を直接見て置いてください。

## 左右の定義(重要)
_L/_Rは「画面に向かって左側にあるものが_L、右側にあるものが_R」です
(本人から見た左右ではありません。画像上でそのまま左にある方が_L、
右にある方が_Rと覚えてください)。服・髪で隠れていても、実際の体の
構造上の位置を透視して推定してください。

## 手順1: 各点の詳細な定義

- **head_top**: 髪型を含めた輪郭全体の最上端の1点。つむじや髪飾りの先端では
  なく、シルエット全体で最もyが小さい点。
- **chin**: 顔の輪郭(髪ではなく肌の輪郭)で最もyが大きい点。前髪や後ろ髪の
  影に惑わされず、顔の肌色パーツの下端を見ること。
- **clavicle_L/R**: 首の付け根と肩関節の中間あたりの、体幹の輪郭線上の点。
  肩関節(shoulder)よりも中心寄り・やや上。服の首周りの縫い目があれば目安になる。
- **shoulder_L/R**: 腕を体幹から分離する回転軸の位置。Tポーズで腕が真横に
  伸びている場合、「体幹の輪郭線が終わり、腕の輪郭線が始まる境目のx座標」を
  軸に、その高さでの体幹側面のyと一致させる。腕の一番外側の輪郭(二の腕の
  盛り上がり)と混同しないこと。
- **elbow_L/R**: Tポーズで腕が完全に伸びている場合、肩と手首を結ぶ直線の
  ほぼ中間点(腕の長さの45〜55%あたり)。腕の輪郭線に実際にわずかな
  くびれ・曲がりが描かれていればそこを優先する。腕が真横一直線でなく
  少し下がって描かれている場合は、その角度に沿って肩から中間の位置を取る。
- **wrist_L/R**: 前腕と手のひらの境目(手首の骨のくびれ、または袖口・
  リストバンド等の装飾の位置)。指先まで数えないこと。
- **waist_L/R**: 体幹の左右輪郭のうち、胸から腰にかけて最もx方向にくびれて
  いる(中心線に最も近づいている)高さの左右の点。服のベルト・リボンの
  位置が目安になることが多い。
- **hip**: 両脚のシルエットが分岐を始める、股間に相当するy座標の中心
  (x=シルエット中心線)。スカート等で脚の分岐が見えない場合は、脚の
  付け根の輪郭が左右に開き始める高さを股の位置として推定する。
- **knee_L/R**: 脚のシルエット輪郭で、太もも(上)とすね(下)の境目に
  あたるくびれ・曲がりの位置。ズボン等のシワや服の切り替え線があれば
  目安にする。**過去の失敗傾向として、膝・足首・つま先を実際より上
  (体に近い側)に置きすぎることが多いため、シルエット輪郭の下端付近まで
  しっかり目を配ること。**
- **ankle_L/R**: すねと足(靴)の境目。靴を履いている場合は、靴の甲の
  盛り上がりが始まる位置。
- **toe_L/R**: 足/靴の輪郭で最もyが大きい(=シルエット全体の下端に最も
  近い)点のうち、つま先側の点。**この点はキャラクター全体のシルエット
  最下端とほぼ同じ高さになることが多い**(踵より前に出ている場合)。

## 手順2: セルフチェック
JSON出力前に、以下を自己確認してください:
1. head_topのyは、あなたが手順0で読み取ったシルエット最上端のyと一致しているか
2. toe_L/toe_Rのyは、シルエット最下端のyに近いか(大きく上に外れていないか)
3. _Lと付く点がすべて画像の左半分側、_Rと付く点がすべて画像の右半分側に
   あるか(中心線を挟んで正しい側にあるか)を再確認する
4. shoulder→elbow→wristが概ね直線(Tポーズ)上に並んでいるか

## 出力形式
このAPIレスポンスは自動処理されるため、説明文やMarkdownのコードフェンスは
付けず、有効なJSONオブジェクトのみを出力してください。

{
  "image": {"w": <画像幅>, "h": <画像高さ>},
  "points_px": {
    "head_top": {"x": 0, "y": 0},
    "chin": {"x": 0, "y": 0},
    "clavicle_L": {"x": 0, "y": 0},
    "clavicle_R": {"x": 0, "y": 0},
    "shoulder_L": {"x": 0, "y": 0},
    "shoulder_R": {"x": 0, "y": 0},
    "elbow_L": {"x": 0, "y": 0},
    "elbow_R": {"x": 0, "y": 0},
    "wrist_L": {"x": 0, "y": 0},
    "wrist_R": {"x": 0, "y": 0},
    "waist_L": {"x": 0, "y": 0},
    "waist_R": {"x": 0, "y": 0},
    "hip": {"x": 0, "y": 0},
    "knee_L": {"x": 0, "y": 0},
    "knee_R": {"x": 0, "y": 0},
    "ankle_L": {"x": 0, "y": 0},
    "ankle_R": {"x": 0, "y": 0},
    "toe_L": {"x": 0, "y": 0},
    "toe_R": {"x": 0, "y": 0}
  }
}
```

### プロンプトE: 生成パラメータ提案(参考、精度は中程度)

技術的なメッシュパラメータ(`body_vox`/`psq_*`/`arm_circle`等)は画像から厳密に
読み取れる性質のものではないため、「妥当な既定値からの微調整」を提案させる
程度に留める。

```
添付のキャラクター正面画像を見て、3Dモデル生成時のパラメータについて
簡単な提案をしてください。以下の既定値からの変更が必要と思われる項目のみ、
理由と共に挙げてください(変更不要な項目は既定値のままでよい、無理に
全項目を変えようとしないこと):

- psq_torso/psq_legs/psq_arms(断面の丸み、1.2〜6、2に近いほど丸く自然、
  大きいほど角ばる): キャラクターの体型・衣装が丸みのあるシルエットか、
  ローブや鎧のような角ばった衣装かで判断
- arm_circle(腕を円筒形にする、true/false): 腕が単純な円筒に近い形状か、
  袖や籠手で複雑な形状かで判断
- hand_extrude(手を厚み付き平面として押し出す、true/false): 手が画像内で
  明確に確認できるかどうかで判断

出力はJSON形式のみ({"suggestions": [{"key": "...", "value": ..., "reason": "..."}]})
としてください。判断材料が無い項目は含めないでください。
```

### プロンプトF: body/accessory二分類+アクセサリー領域抽出+除外マスク用データ(改訂版)

front/side/back**3枚同時入力**。exclude_masksもこの結果から機械的に導出する。

```
添付の3枚の画像(front=正面、side=側面、back=背面)は、同一キャラクターの
Tポーズ立ち絵です。これらを横断的に見て、以下の2種類に分類してください。

## 分類基準
- **body(通常人体)**: 頭・首・胴・腕・脚そのもののシルエット、および
  肌や体に密着してその輪郭に沿う衣服(体のラインをそのままなぞる
  シャツ・タイツ・手袋・靴下など)。**このカテゴリの領域は出力不要です**
  (デフォルトで全てbody扱いにするため)。
- **accessory(通常人体の輪郭からはみ出るもの)**: 体そのものの輪郭を
  超えて外側に広がる・突き出るすべてのもの。判断基準は**「その部分を
  取り除いたときに、下の体のシルエットがそのまま単純な円柱/紡錘形に
  近い形で残るか」**です。例:
  - スカート・マント・広がった袖・大きなリボン(体の輪郭より外側に
    張り出す衣服)
  - 髪の房・ツインテール・アホ毛など、頭部の基本シルエットを超えて
    伸びる髪
  - 帽子・ヘッドホン・眼鏡・獣耳・尻尾・翼・武器・鞄などの装着物

体に密着した服(通常のTシャツ・ズボン・靴等、体のラインに沿うもの)は
accessoryに含めないでください。迷った場合は「取り除いても体の基本
シルエットが崩れないか」で判断してください。

## 出力手順
1. 検出したaccessoryそれぞれに、以下のパレットから重複しないよう
   順に色を割り当ててください: #7aa2ff, #ff6ad5, #52e0c4, #ffb454,
   #ff8a8a, #b48cff, #8cffb4, #5be6ff
2. 各accessoryについて、取付ボーンを以下から1つ以上選んでください:
   hips, torso, chest, neck, head, clavicle_L, upperarm_L, forearm_L,
   wrist_L, hand_L, clavicle_R, upperarm_R, forearm_R, wrist_R, hand_R,
   thigh_L, shin_L, foot_L, toe_L, thigh_R, shin_R, foot_R, toe_R
   (1つのボーンに固定なら"rigid"、複数ボーンにまたがり体の動きに追従
   するなら"soft"。例: スカート→hips+thigh_L+thigh_R、soft。
   ヘッドホン→head、rigid。尻尾→hips、rigid)
3. 各accessoryについて、front/side/backそれぞれの画像に実際に写っている
   場合はその面での多角形領域も、画像のピクセル座標で時計回りに6〜12点
   程度で近似して返してください(写っていない、または隠れて見えない
   面は省略してよい)

## 出力形式(JSONのみ、説明文・コードフェンス不要)
{
  "accessories": [
    {
      "name": "例: スカート",
      "mode": "soft",
      "bones": ["hips", "thigh_L", "thigh_R"],
      "color": "#7aa2ff",
      "reason": "腰から広がり、脚のシルエットの外側に張り出しているため",
      "regions": {
        "front": {"points": [[0,0], [0,0], [0,0]]},
        "side": {"points": [[0,0], [0,0], [0,0]]},
        "back": {"points": [[0,0], [0,0], [0,0]]}
      }
    }
  ]
}
```

**Ghost Scanner側の処理**: このJSONを受け取ったら、①`accessories`フィールドとして
そのまま`landmarks_ai.json`に格納、②各accessoryの`regions[view].points`をcanvas上に
赤で塗りつぶし、`exclude_masks[view]`(PNG dataURL)として合成する。
