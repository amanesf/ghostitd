# Veoターンテーブル動画 → 3Dモデル化パイプライン計画(v2)

## 0. 背景・v1からの方針転換

v1計画は「多視点シルエット彫刻+8方位タイルアトラス」だったが、**テクスチャが結局
8方位の離散ビューに落ちており、それなら静止画生成(nanobanana等)で8枚作るのと変わらない**
という指摘を受けて全面改訂した。v2の設計原則:

> **動画の価値は「ビューが密で、時間的に連続している」こと。
> 全フレーム・全ピクセルを幾何とテクスチャの両方に使い切る。**

具体的には:

- **形状**: 全Nフレーム(48〜96枚、2〜7°刻み)のシルエットを使う投票制彫刻(v1から継続)。
  さらに、フレームの**ピクセル内容**(色の多視点整合性)で凹形状を彫る精密化パスを追加する。
- **テクスチャ**: 「どのビューのタイルを貼るか」を選ぶ方式を全廃し、
  **テクセル単位で全フレームを可視性・法線適合度で加重合成するベイク方式**にする。
  - 継ぎ目が**構造的に発生しない**(同じ表面点は常に同じブレンド結果になるため、
    現行の`seam_*`/`color_grad_*`系の妥協パラメータ群が丸ごと不要になる)。
  - 隣接フレームは同じ表面をほぼ同じ角度から映しているので、多数フレームの加重平均が
    動画圧縮ノイズ・フリッカーを打ち消す=**時間的冗長性がそのまま画質(実効SNR・
    実効解像度)になる**。これが静止画8枚に対する動画の決定的な優位点。
- 制約は現行どおり: **ブラウザ完結・サーバなし・ビルドなし**。重い処理はWebGL/WebGPUと
  WebWorkerで賄う。**出力はボーン入りGLB必須**(既存コントローラーで動くこと)。
- 見た目の基準は「元イラスト再現」に固定しない。元イラスト3面は「信頼度の高い3フレーム」
  として同じ加重合成の枠組みに参加させる(§9.4)。

フォトグラメトリ(COLMAP)・NeRF・3D Gaussian Splattingは、学習/最適化基盤か
メッシュ化(+リギング)の後工程がブラウザ完結制約に反するため引き続き不採用。
ただし§7の photo-consistency 彫刻は space carving 系の考え方であり、
「ピクセル内容から幾何を推定する」方向の実測はスコープに含める。

---

## 1. ツール構成: 既存4ツールとの関係(★ユーザー方針確定済み)

**既存の3面モード(ゴーストスキャナー/ゴーストジェネレータ/ゴーストビューア/
ゴーストコントローラー)はそのまま残し、動画方式は「2」系の新ツールとして作る。**

### 1.1 スキャナー2とジェネレータ2は1ページに統合する(推奨案)

新ツールは **「ゴーストスタジオ」(`ghost_studio.html`、仮称)** 1ページとし、
タブ前半がスキャナー2相当(AI連携・素材準備)、後半がジェネレータ2相当(生成・調整)を担う。
命名は既存規則「ゴースト+平易な英単語」に従う(名称は実装時に確定でよい。
以下本書では「スタジオ」と呼ぶ)。

統合する技術的理由(単なるUI好みではない):

1. **キャリブレーションが彫刻を必要とする**: 角度推定の精密化(§5の
   analysis-by-synthesis)は「粗く彫って→再投影して→角度を直す」の反復であり、
   スキャナー相当の工程がジェネレータ相当の彫刻エンジンを内部で呼ぶ。
   2ページに分けるとこのループがページ境界をまたいでしまう。
2. **素材の受け渡しが重い**: 3面方式のJSON(画像3枚+点)と違い、動画方式の素材は
   フレーム数十枚(GPUテクスチャ常駐)。ページ間でIndexedDB経由の受け渡しを挟むと
   メモリ・時間の二重コストになる。1ページならフレームをGPUに載せたまま
   彫刻→ベイク→再調整まで通せる。
3. **反復調整の応答性**: `illust_weight`等のベイク系パラメータ変更は再ベイク数秒で
   反映できる(§9)。フレームが手元にある同一ページ内だからこそ成立する。

ページ肥大(単一巨大IIFEの再発)への手当て: ロジックは全て`js/`側の新モジュール
(§10)に置き、`ghost_studio.html`はタブUI・状態管理のみとする。状態は1つの
状態オブジェクトに集約する(GHOST_SCANNER_PLAN.md「実装方針」と同じ規律)。

### 1.2 スタジオのタブ構成(ウィザード形式、左→右)

既存スキャナーのウィザード設計・Gemini API層(`js/scanner_api.js`)・手動フォールバック
方式をそのまま流用する:

```
[1. 画像投入]        元イラスト1枚(任意ポーズ)。既存スキャナー/ジェネレータのJSONの
                     読み込みもここで受け付ける(3面図・ランドマーク・colormap済み素材の再利用)
[2. 正面図生成]      Tポーズ正面画像の生成(既存スキャナーのプロンプトAを流用。
                     API生成/プロンプトコピペ+手動アップロードの併設も同じ)
[3. 動画生成]        Veoターンテーブル動画(§3のプロンプト雛形を表示。
                     ★Veo APIは課金前提のため、既存スキャナーの画像生成と同じ
                     3手段を用意する: API生成/プロンプトコピペ→外部で生成→
                     mp4アップロード/直接ファイル選択)
[4. フレーム検証]    抽出・シルエット・キャリブレーション(§4〜5)。受け入れチェック
                     リスト(§3)の自動判定結果と角度曲線・残差グラフを表示し、
                     不合格ならタブ3に戻って再生成
[5. ランドマーク]    正面キーフレーム上でランドマーク推定(既存プロンプトD+チャット
                     反復微調整を流用)。骨格pivots用
[6. 領域分け]        front/side/backキーフレームのcolormap生成(既存プロンプトF=
                     body/accessory分類を流用)。パーツ分割・rigid/bones指定用
[7. 生成・調整]      彫刻→ベイク→GLB(§6〜9)。ベイク系ライブパラメータもここ
[8. 出力]            GLBダウンロード/ビューア2へIndexedDB引き継ぎ
```

- タブ2は、素材が最初からTポーズ正面ならスキップ可。タブ1で既存スキャナーJSONを
  読み込んだ場合はタブ2/5/6が「確定済み」状態で埋まる(再利用)。
- Veo API呼び出しは`js/scanner_api.js`にVeo系エンドポイントを追加する形で対応
  (APIキー管理・モデル名のlocalStorage上書きの仕組みは既存のまま共用)。

### 1.3 ビューアは専用の「ビューア2」を新設する

`character_3d2.html`(仮称ゴーストビューア2)を新設し、既存`character_3d.html`は
一切触らない。理由:

- 動画方式のライブ編集はatlas段が根本的に別物(§9: チャートUV+GPUベイク。
  ベイクにはフレーム素材が必要)。既存ビューアの巨大IIFEへこの分岐を差し込むのは
  CLAUDE.mdで明文化した命名衝突・回帰リスクをまともに踏む。
- ビューア2は`finishFromIntermediate`の後段を共有しつつ、atlas段だけ
  `js/gpu_bake.js`を呼ぶ薄いページとして新造した方が安全で小さい。
- **中間データ契約の拡張**: ビューア2でベイク系パラメータを再調整できるよう、
  IndexedDBの中間データに`bakeSources`(キーフレーム+使用フレームのJPEG圧縮版、
  キャリブレーション結果θ/w、チャートUV入力)を追加する(64枚×約200KB≒13MB、
  IndexedDBとして現実的)。ビューア2はこれをGPUに載せ直して再ベイクする。
- ゴーストコントローラーは出力GLBしか見ないため共用のまま(無改造)。

### 1.4 既存ツールへの変更は「index.htmlへの入口追加」のみ

`ghost_scanner.html` / `landmark_tool.html` / `character_3d.html` は無改造で併存。
`index.html`にスタジオ/ビューア2の入口を追加する。

---

## 2. 全体アーキテクチャ(処理フロー)

```
[元イラスト1枚(任意ポーズ)]
        │  ghost_studio.html タブ1〜2(既存プロンプトA流用)
        ▼
[Tポーズ正面図] ──(タブ3: Veoプロンプト/手動アップロード)──▶ [turntable.mp4]
        │                                                          │
        ▼                                                          ▼
[Stage 1] フレーム抽出(WebCodecs)+品質フィルタ(タブ4)
        ▼
[Stage 2] シルエット抽出+フレームごとの符号付き距離場(SDF)
        ▼
[Stage 3] ターンテーブル・キャリブレーション(角度θ_i・信頼度w_i・軸・スケール)
        ▼
[Stage 4] 多視点ソフトビジュアルハル彫刻(全フレーム投票制)
        ▼
[Stage 5] photo-consistency精密化(凹形状。narrow band限定、実験パス)
        ▼
[Stage 6] パーツ分割・骨格・スキニング(タブ5〜6の成果物を使用)
        │  rawUnified{V,F,owner} + partsMeta + pivots ←既存の中間データ契約
        ▼
[Stage 7] チャートUV展開+ジオメトリイメージ生成
        ▼
[Stage 8] 全フレーム・テクセル単位加重合成ベイク(WebGLで1フレーム=1パス)
        ▼
[Stage 9] GLB書き出し(タブ8)・IndexedDB(+bakeSources)でビューア2へ
```

---

## 3. Stage 0: Gemini/Veoプロンプトとフロー(スタジオ タブ1〜3)

### 3.1 フロー全体

**タブ1(イラスト投入)→タブ2(正面図生成)は既存スキャナーと完全に同じ**とする:

- タブ1: 元イラスト1枚(ポーズ・アングル不問)を投入。1024×1024レターボックス
  リサイズも既存処理を流用。
- タブ2: **プロンプトA(逐語流用)**でTポーズ正面図を生成。API生成/プロンプト
  コピペ→手動アップロード/直接ファイル選択の3手段、失敗時リトライ、
  Geminiセッションを毎回新規にする方針もすべて既存スキャナーのまま。
- タブ3: **タブ2で確定した正面図を初期フレーム(first frame)に指定した
  image-to-videoで、その場で1回転させる**(下記プロンプトV)。元イラスト
  ではなく正面図を渡すのは、既存スキャナーのside/back生成が「frontを参照画像に
  してキャラ一貫性を担保する」のと同じ理屈+Veoのimage-to-videoは初期フレームを
  ピクセルで拘束できるため、**動画のフレーム0=タブ2正面図(θ=0)が厳密に成立**し、
  キャリブレーションの強いアンカーになる(§5)。

3面方式のside/back画像生成(プロンプトB/C)はこのフローでは呼ばない(動画が代替)。
ランドマーク(プロンプトD)・領域分け(プロンプトF)は流用する(§3.5)。

### 3.2 プロンプトV(ターンテーブル動画)全文案

既存プロンプトの実地検証で得た流儀を踏襲する: 条件を絞って指示過多を避ける、
否定形(「〜するな」)は無視されやすいので肯定形で書く、番号付き条件、
プロンプト全文をタブ内に表示して隠さない。

```
入力画像は、私のオリジナルキャラクターの正面Tポーズの立ち絵です。
この画像に描かれたデザイン(髪型・髪色・瞳の色・アクセサリーの形と色・
服の柄と色・靴の色や装飾など、見えているすべての要素)を寸分違わず
維持したまま、このキャラクターがフィギュア(彫像)のように完全に静止
した状態で、その場でゆっくりY軸周りに自転する動画を生成してください。

## 生成条件
1. **カメラ・画角**: 三脚に固定した望遠寄りのレンズで撮影し、入力画像と
   全く同じ画角・フレーミング(被写体からの距離・ズーム倍率・構図)を
   動画の最初から最後まで完全に維持すること。ドリー・ズームイン・
   ズームアウト・パン・チルトは一切行わない完全な固定ショットとし、
   回転中のどの瞬間も入力画像と同じ構図(画面内でのキャラクターの位置・
   大きさ)であること。回転軸(キャラクターの正中線)は入力画像と同じく
   画面の水平方向の中心に固定
2. **回転**: 反時計回り(キャラクターの右肩が先にカメラへ近づく向き)に
   等速で、0度(正面)→90度(右側面)→180度(背面)→270度(左側面)→
   360度(正面に戻る)の順に、動画全体を通してずっと同じ方向に回転し
   続けること。**180度(背面)に到達した後も回転方向を反転させず、
   そのまま同じ向きで残り半周を続けて正面に戻ること**(ファッションショーの
   ように背面を見せてから逆回転で正面へ戻る動きにはしないこと)。回転を
   生み出す装置(ターンテーブルの台座・モーター・回転盤など)は画面に
   描写せず、キャラクターの足元は均一な白背景のみとすること
3. **ポーズ・素材の硬直**: 最初のフレームのTポーズを彫像のように完全に
   保つ。髪(房・ツインテール等の形も含む)、マフラー/スカーフ、リボン、
   スカートの裾など、通常は風や動きで揺れる部分も含めて全身が硬い樹脂
   でできているように、回転中ずっと最初のフレームと同一の形状を保つこと
4. **デザインの再現精度**: 髪型・髪色・瞳の色・アクセサリーの形と色・
   服の柄と色・靴の色や装飾など、入力画像に見えているすべてのデザイン
   要素を、簡略化や解釈を加えずピクセル単位の忠実さで維持すること
5. **照明**: 影やハイライトのない均一なフラット照明。回転中も全フレームで
   明るさ・色味を一定に保つ(表面のツヤ・光の反射はマットに描く)
6. **背景**: 常に均一な白(#FFFFFF)。床も影も白に含める

出力は動画のみとしてください。
```

- **英語版を併記する**: Veoは公式ドキュメントがプロンプト英語推奨のため、同内容の
  英語版をタブ内トグルで切り替えられるようにし、実地A/Bで良い方を既定にする
  (プロンプトA〜Fが日本語で実績を出している経緯があるため、決め打ちにしない)。
- 回転方向は「反時計回り」で固定する必要はない(§5のキャリブレーションがθの符号を
  両方試してIoU合計の良い方を採用する=方向の指示が破られても壊れない)。
  条件2に向きを書くのは、テイク間で方向が揺れて比較しづらくなるのを防ぐため。
- 条件5の括弧書き(マット指示)は、テクスチャ全フレーム合成(§9)にとって
  「回転につれて動くハイライト」が最大の敵であるため。それでも動く素材は
  §9.3のロバスト化と受け入れチェックで扱う。

### 検証メモ(2026-07-18、実機テスト・手動フォールバック経路)

実際にVeoへ投げて得られた知見。以降のプロンプト改訂・実装判断の参考にする:

- **「あるアニメ調キャラクター」で生成拒否、「私のオリジナルキャラクター」に
  変更したら通った**: 拒否理由は明示されなかったが、既存の版権キャラクターの
  模倣を疑われた(著作権関連の誤検知)可能性が高いと判断した。これは安全フィルタの
  意図的な回避ではなく、「入力画像は誰のオリジナル創作物か」という申告の誤りを
  訂正したことで正しく判定された、という位置づけ。プロンプト本文を上記の通り
  更新済み。
- **秒数・アスペクト比はプロンプト文で制御できない**: 「8秒」「1:1」を意図しても、
  実際の生成結果は**10秒・9:16**になった(手動フォールバック経路、Flow等の
  ツールUI経由)。既存の画像生成(`imageConfig`)と同じく、秒数・アスペクト比は
  プロンプト文ではなくツールのUI設定(またはAPIの構造化パラメータ)で明示的に
  指定する必要がある。手動生成時は、プロンプトコピペ前にツール側の設定欄で
  秒数・アスペクト比を確認するようUIに注記を入れる。
- **手足が回転中に見切れる**: 9:16のような縦長比率だと、Tポーズで横に伸ばした
  腕の幅が画面幅に収まりきらず発生しやすい。対策として、抽象的な「余白を
  確保しろ」という指示をプロンプトに書くのではなく、**参照画像側で最初から
  9:16に収まるタイトな構図(手足が見切れない余白込み)を用意し、プロンプト
  条件1で「入力画像と全く同じフレーミングを維持する」と指示する**方式に
  変更した(余白の量そのものをVeoの解釈に委ねない)。
- **ズームイン・アウトが発生**: 条件1に「位置・角度・ズームを一定に保つ」と
  書いていても、動画全体で寄り引きが発生した。推定原因は、それまで使っていた
  「大きなキャンバスに被写体を小さく配置」した参照画像(§前掲のアンクロップ
  画像)が、多くの動画生成モデルが学習データで持つ「引きの構図から被写体へ
  ドリーインする」という映画的パターンを誘発した可能性。対策として、
  ①参照画像を9:16でキャラクターがフレームいっぱいに写るタイトな構図に変更、
  ②条件1を「入力画像と全く同じ画角・フレーミングを維持」「ドリー・ズームイン・
  ズームアウト・パン・チルトは一切行わない」という明示的な固定ショット指定に
  強化、③望遠寄りレンズの指定(§13で予定していた弱透視ズレ対策を前倒しで導入)、
  の3点で対処した。
- **ターンテーブル装置そのものが画面に映ってしまう**: 旧版の「電動ターンテーブルの
  上で回転させた動画」という表現が、装置(台座・回転盤)の実写的な描写を誘発したと
  推定。条件2に「装置を描写しない」旨を明記する形に変更した。
- **マフラー(スカーフ)の動きが不自然**: 「彫像のように静止」という指示だけでは、
  なびきやすい個別パーツ(マフラー・房状の髪等)まで強くは効かなかったと推定。
  条件3で該当パーツを名指しして硬直を明示する形に変更した。
- **背面(180度)付近で回転が逆転する**: 「動画全体でちょうど1回転」という
  指示だけでは、背面を見せた後に逆回転して正面へ戻る(ファッションショーの
  ターンのような)動きになってしまった。動画生成モデルの学習データに
  「人物が振り返って背中を見せ、また向き直る」動きの方が「一方向に回り
  続けて一周する」動きより多く含まれているためと推定。条件2に0/90/180/270/
  360度の通過点を明記し、「180度到達後も反転しない」旨と、避けたい具体的な
  動きのパターン(ファッションショーのターン)を名指しする形に変更した。
- 上記変更は未検証(次回生成時に効果を確認する)。

### 3.3 Veo API呼び出し設計(`js/scanner_api.js`拡張)

- Veo系のGemini APIは画像生成と違い**長時間実行オペレーション**
  (`predictLongRunning`→operationをポーリング→完了後に動画URIを取得)。
  `scannerCallVeo(prompt, firstFrameDataUrl, opts)`を新設し、ポーリング
  (数十秒〜数分)・進捗表示・キャンセルをAPI層に閉じ込める。
- モデル名は既存の仕組みどおりlocalStorage上書き+models.list連動のプルダウン
  (既定はコード更新時点の最新Veo。Veoはモデル名の変化が速いためハードコードに
  依存しない)。生成パラメータ: 8秒・アスペクト比1:1(不可なら16:9で左右余白)・
  解像度は選べる最大。
- **手動フォールバックが正規ルート**(課金・到達性のため): プロンプトコピペ
  ボタン+正面図ダウンロードボタン(外部のGemini/Flowへ持ち込む用)+mp4
  アップロードボタンの3点セット。既存の画像生成タブと同じUIパターン。
- **Veo以外の候補(2026-07-17メモ、ユーザー方針: 中国系サービスは避ける)**:
  Veoは長時間実行オペレーション(ポーリング)で統合がやや面倒+単価も高め。
  Kling/MiniMax(Hailuo)/Viduは安価で評判も良いが中国系のため除外。
  非中国系の代替候補はRunway・Luma AI(Dream Machine)・Pika・Stability AI
  (Stable Video Diffusion、オープンウェイトで自前ホストも可)。単価はKling等
  より高めだがVeoよりは安い見込み(要最新価格確認)。API統合はReplicate.com/
  fal.ai等のアグリゲータ経由だとVertexのポーリング作法を個別に実装せずに済み、
  モデル切替もしやすい。M6(API自動生成)着手時に選定・検証する。

### 3.4 受け入れチェックとリトライ指針(タブ4→タブ3の戻りループ)

タブ4の自動チェックが落ちた場合、どの生成条件を強調して再生成するかを
対応表としてUIに出す(チェック結果からリトライ文面を自動提案):

| 自動チェック | 合格基準 | 不合格時の対処 |
|---|---|---|
| 1回転完了 | 先頭/末尾シルエットIoU>0.97 | 条件2を強調して再生成。**±20°程度の過不足はキャリブレーション側で許容**(不足帯は欠測として投票母数から除外、超過帯は重複フレームとして両方使う)ため、軽微なら合格扱いにしてよい |
| ポーズ固定 | シルエット面積の変動係数<3% | 条件3の彫像・樹脂の比喩を強調して再生成。局所的な揺れ(髪だけ等)ならそのフレームの信頼度w_iを下げて続行 |
| 無地背景 | 四隅パッチの色分散<閾値 | 条件5を強調。軽微な床影は背景色推定+EDTのマージンで吸収可 |
| 見切れなし | シルエットbboxが画面端に非接触 | 条件6を強調。数フレームだけなら該当フレーム除外で続行 |
| 照明一定 | フレーム間の前景平均色ドリフト<閾値 | 条件4を強調。緩やかなドリフトなら正面キーフレーム基準のトーン正規化で補正して続行 |
| キャラ同一性 | 目視(フレーム0/90/180とタブ2画像の並列比較UI) | 再生成(プロンプトでの対処は難しい。テイクガチャ) |

複数テイクを保持して並べて選べるようにする(既存スキャナーのリトライUIと同様、
確定するまで前のテイクを捨てない)。

### 3.5 プロンプトD/Fの流用方法(タブ5〜6)

- **ランドマーク(プロンプトD、逐語流用)**: 入力は動画フレームではなく
  **タブ2の正面図そのもの**(1000×1000レターボックスの既存
  `scannerBuildDetectionCopy`流用)。フレーム0=正面図なので、推定結果は
  そのまま動画座標系のθ=0に対応し、既存のチャット反復微調整UIも流用できる。
- **領域分け(プロンプトF、逐語流用)**: 入力は
  `[タブ2正面図, 動画のθ≈90°キーフレーム静止画, θ≈180°キーフレーム静止画]`。
  既存スキャナーがfront/side/back 3枚を渡すのと同じ形式のため、プロンプト文面・
  レスポンス処理(colormap生成)とも無改造で流用できる。キーフレーム静止画の
  実解像度(720p級)はcolormap分類の用途には十分。

### 3.6 補助素材のプロンプト(任意、§14 M6)

- **背面起点の半回転動画**: プロンプトVの初期フレームをθ=180キーフレーム静止画に
  差し替え、条件2を「ちょうど半回転」にした変形。正面起点動画の背面側
  (Veoの想像で描かれた部分)を実測で上書きしたい場合に使う。
- **顔アップのターンテーブル動画**: プロンプトVに「バストアップの構図」条件を
  追加した変形。頭部bbox限定の高解像度再ベイクに使う。

---

## 4. Stage 1: フレーム抽出(`js/video_frames.js` 新規)

- WebCodecs `VideoDecoder`第一候補+`<video>`シークのフォールバック。N=48〜96
  (既定64)を等間隔サンプル、ブレ(Laplacian分散)/重複フレームは自動除外。
- 全フレームがテクスチャ源になるため**原寸保持が必須**。抽出直後に各フレームを
  WebGLテクスチャ(ImageBitmap経由)へ載せ、JS側のcanvas/配列は保持しない。
  シルエット外黒塗り+bboxクロップでGPUメモリを節約(1024²×96枚でも実用範囲)。

---

## 5. Stage 2〜3: シルエット・キャリブレーション

- シルエット: 四隅パッチから背景色推定→色距離→`significantComponentsMask`+
  `fillHoles`(既存流用)。フレームごとにEDT(Felzenszwalb 2パス、`js/edt.js`新規)で
  符号付き距離場化。
- キャリブレーション(`js/turntable_calib.js`新規): 弱透視+Y軸回転モデル。
  bbox高さでスケール/上下ドリフト正規化 → 重心Xで回転軸 → 等速仮定を初期値に
  キーフレーム同定で区分線形補正 → 粗彫刻→再投影IoU最大化の
  analysis-by-synthesisでθ_iを±1°級へ精密化(2〜3反復)。IoU残差の大きい
  フレームは信頼度w_iを下げる(以降の全Stageで共通に使う重み)。
- **キーフレームのアンカー**: フレーム0=タブ2正面図(§3.1)なのでθ=0は探索不要で
  フレーム0に固定できる。探索するのはside(θ=90)/back(θ=180)相当フレームのみ
  (タブ2画像のシルエットを鏡像込みでIoU照合)。回転方向はプロンプトの指示が
  破られる可能性を踏まえ、θの符号を両方試してIoU合計の良い方を採用する(§3.2)。
  1回転の過不足(±20°程度)は、不足帯を欠測(投票母数から除外)、超過帯を
  重複フレーム(両方使う)として扱う(§3.4)。
- タブ4のUI: 角度曲線+フレーム別残差グラフ、不良フレームの手動除外。

---

## 6. Stage 4: 多視点ソフトビジュアルハル彫刻(`js/multiview_hull.js` 新規)

- 共有ボクセルgrid(`P3D.buildGrid`流用、XYZ等方解像度)。各ボクセルを全フレームへ
  弱透視投影し、SDFをバイリニアサンプル。
- **投票制**: `field = 重み付き下位quantile(d_i)`(既定: `mv_vote_frac=0.9`)。
  厳密hull(min)はポーズ揺れ1フレームで形が痩せるため、外れ実測の棄却で対処する
  (形を均す平滑化ではない)。
- coarse-to-fine(1/4解像度→表面narrow bandのみ本解像度)+Worker並列。
  marching cubes・断片除去・法線/巻き補正は既存流用。
- 現行carveRegionの腕円形断面・手押し出し・スーパー楕円断面(`psq_*`)は全廃
  (全角度の実測が代替)。

---

## 7. Stage 5: photo-consistency精密化(凹形状、実験パス)

シルエットに現れない凹み(首まわりの窪み、髪の内側、スカート内側の脚との隙間)を、
**フレームのピクセル内容**から彫る。space carvingの考え方をnarrow band限定で行う:

1. Stage 4のハル表面から内側へ数ボクセル(`mv_pc_band`、既定4)を対象にする。
2. 各表面ボクセルについて、可視なフレーム集合(深度マップ§9.2で判定)から色を
   サンプルし、**重み付き色分散**を計算する。
3. 真の表面なら全フレームでほぼ同色(固定照明を§3で担保)。分散が閾値
   `mv_pc_var_thr`を超えるボクセルは「実は表面ではない」として削り、露出した
   新表面で再評価(数反復)。
4. ガード: シルエットSDF由来のhullを外側上限として常に維持(削る方向にしか
   働かせない)。1反復の削り量も1ボクセルに制限。

アニメ調の平坦色領域では色分散が判別力を持たないため、フレームの局所勾配強度を
信頼度に織り込む。効果は素材依存が大きく、**既定OFFの実験機能**(`mv_photo_consistency`)。
効かなくてもStage 4までの品質は損なわれない(独立パス)。

---

## 8. Stage 6: パーツ分割・骨格・スキニング

- pivots: タブ5のランドマーク(プロンプトD+チャット微調整。入力は§3.5のとおり
  タブ2正面図)→`buildDerivedLandmarks`→`computePivots`を流用。フレーム0=
  タブ2正面図なので、座標変換はレターボックスの相似変換1つで済む(3面方式の
  ような別素材間のズレ吸収は不要)。
- owner: タブ6のcolormap(プロンプトF流用。入力はタブ2正面図+動画のθ≈90/180
  キーフレーム静止画、§3.5)を最終メッシュへ法線最適ビューで投影して最近傍色分類→
  メッシュ隣接多数決で飛び地除去。ownerはスキニングとチャート分割のヒントにしか
  使われないため、この精度で足りる。
- `nearestBoneSegmentSkin`/`rigidSkin`/`finishFromIntermediate`以降は無改造。
  bone_bridge/part_gap_closeは不要(最初から1つの実測fieldなので空隙が出ない)。

---

## 9. Stage 7〜8: 全フレーム・テクセル単位ベイク(v2の中核)

### 9.1 チャートUV展開(`js/uv_charts.js` 新規)

ビュー由来のタイルではなく、**メッシュ固有のUVアトラス**を作る(内容とレイアウトの分離)。
ビルドなし制約下でxatlasは使えないため、簡易チャート方式を自前実装する:

1. 面を「owner(パーツ)× 法線の主方向(6方位クラスタリング)」でシード分割し、
   隣接面の法線が閾値内なら同チャートへ貪欲併合(チャート内の法線円錐角<60°を保つ)。
2. 各チャートを平均法線に垂直な平面へ投影して2D化(歪みは法線円錐角制限で抑制)。
3. 2D bboxを面積降順にシェルフ詰めでアトラスへパック(テクセル密度をチャート間で
   統一、境界に4pxパディング)。
- 出力アトラス解像度は既定2048²(§9.3の多フレーム合成が720p素材からでも
  この密度を意味のあるものにする)。

### 9.2 ジオメトリイメージと深度マップ

- **ジオメトリイメージ**: アトラスの各テクセルに対応する表面位置(x,y,z)と法線を
  floatテクスチャへラスタライズ(チャートをUV空間で描画するだけ。
  `EXT_color_buffer_float`使用)。以降のベイクはこの2枚を入力とする画像処理になる。
- **可視性(深度マップ)**: 各フレームiのカメラ(弱透視=OrthographicCamera+
  Stage 3のθ_i/スケール/ドリフト)からメッシュを深度レンダリング(N回、GPUなので
  数秒)。テクセルの表面点をフレームiへ投影し、深度比較で自己遮蔽を判定。

### 9.3 加重合成ベイク(1フレーム=1レンダーパス)

全テクセルについて、**可視な全フレームの色を重み付き合成**する。WebGLの加算ブレンドで
「重み付き色の累積」と「重みの累積」を作り最後に除算(フラグメントシェーダ2本+
Nパス。JSループ不要でN=96でも数秒):

```
パスi(フレームiについて):
  入力: ジオメトリイメージ(位置・法線)、フレームiのテクスチャ、深度マップi、θ_i、w_i
  各テクセル:
    p, n ← ジオメトリイメージ
    uv_i ← pをフレームiへ投影(弱透視)
    可視でなければ discard(深度比較+シルエットSDF>マージン)
    w = w_i                        … フレーム信頼度(Stage 3)
        × max(0, n·view_i)^k       … 法線適合度(既定k=4。斜め視は解像度が低いので急峻に減衰)
        × sharpness_i(uv_i)        … 局所ブレ(局所Laplacian分散、前計算)
        × edgeFade(SDF_i(uv_i))    … シルエット際は背景混入するため減衰
    accumColor += w * frame_i(uv_i);  accumWeight += w
最終パス: atlas = accumColor / accumWeight
```

- **継ぎ目レス**: チャート境界の両側は同じ表面点→同じ重み関数→同じ色に収束するため、
  ビュー切替方式で必要だった`seamAngles`/`seamNoSide`/`seamSmoothIters`/
  `colorGradWidth`/`colorGradStrength`は**全部不要になる**。
- **実効超解像**: 1テクセルは数十フレームから独立サンプルされる。バイリニア+
  サブピクセル投影ずれが自然なマルチサンプルになり、720p素材でも2048アトラスの
  密度が埋まる(平均によるボケは重みk乗の急峻化とロバスト化で抑える)。
- **ロバスト化(ハイライト・ゴースト対策)**: 重み付き平均から外れたサンプルの重みを
  下げて再平均(1回の再重み付け、Tukey型)を`mv_bake_robust`として用意。
  GPU実装は累積バッファをもう1往復するだけ。
- **穴埋め**: どのフレームからも不可視のテクセル(深い凹み・股下等)はベイク後に
  pull-push補間。現行bleedEdgesの役割はこれが引き継ぐ。

### 9.4 元イラストの参加(見た目の最適化)

タブ2の正面図(と既存JSONのside/back、あれば)を「θ=0/90/180の追加フレーム」として
同じ枠組みに入れ、重み係数`illust_weight`(既定2.0程度)で動画フレームより優遇する。

- `illust_weight`を大きくすると正面・側面・背面が元イラスト寄り、0にするとVeoの
  質感寄りになる。**「イラスト再現⇔Veo再現」がスライダー1本の連続量になる**。
- 実装上は特別扱い不要(フレームリストに3枚足すだけ)。色調ベースを動画側に
  合わせる逆方向ヒストグラムマッチングをオプションで持つ(`illust_tone_match`)。

### 9.5 GLB接続とビューア2

- `buildGLB`は無改造(UV+単一テクスチャという契約は同じ)。アトラス圧縮は
  `compressAtlas`流用。
- 段階キャッシュの依存関係: decimate → mesh_finish → **uv_charts → bake** →
  model_glb(UVは間引き・平滑化・スキニング確定後の最終メッシュから作る)。
- ビューア2(`character_3d2.html`)は中間データ+`bakeSources`(§1.3)を
  IndexedDBから読み、`finishFromIntermediate`相当の後段(atlas段は`js/gpu_bake.js`)
  でライブ編集する。既存ビューアの3面方式コードとは交差しない。

---

## 10. 新規・変更ファイル一覧

| ファイル | 新規/変更 | 内容 |
|---|---|---|
| `ghost_studio.html` | 新規 | スキャナー2+ジェネレータ2統合ツール(§1.2のタブUI・状態管理のみ。ロジックはjs/へ) |
| `character_3d2.html` | 新規 | ビューア2(bakeSources再ベイク対応のライブ編集。既存ビューア無改造) |
| `js/video_frames.js` | 新規 | WebCodecs抽出+フォールバック、ブレ/重複除外、フレーム→GPUテクスチャ管理 |
| `js/edt.js` | 新規 | 2値マスク→符号付き距離場 |
| `js/turntable_calib.js` | 新規 | 軸/スケール/角度推定、キーフレーム同定、analysis-by-synthesis、信頼度w_i |
| `js/multiview_hull.js` | 新規 | 投票制彫刻(coarse-to-fine+Worker)、photo-consistency精密化 |
| `js/uv_charts.js` | 新規 | チャート分割・平面投影・パッキング・ジオメトリイメージ生成 |
| `js/gpu_bake.js` | 新規 | 深度マップN枚レンダリング、加重合成ベイク(WebGL、インラインGLSL)、pull-push穴埋め |
| `js/scanner_api.js` | 変更 | Veo系エンドポイント追加(APIキー管理・モデル名上書きの仕組みは既存共用)。既存関数は不変 |
| `js/pipeline.js` | 変更 | `runToIntermediateFromVideo`追加(既存`runToIntermediate`不変) |
| `js/idb.js` | 変更 | 中間データへの`bakeSources`追加(後方互換: 無ければ3面方式として扱う) |
| `js/common.js` | 変更 | 新gen_params既定値、ヒストグラムマッチング |
| `index.html` | 変更 | スタジオ/ビューア2の入口追加 |
| `tests/video-pipeline.test.js` | 新規 | §12 |

**触らない**: `ghost_scanner.html`、`landmark_tool.html`、`character_3d.html`、
`controller.html`、`js/carving.js`、`js/atlas.js`、`js/skeleton.js`、
`js/marching_cubes.js`、`js/model_export.js`(3面方式はそのまま併存)。

---

## 11. 新規gen_params(案)

| キー | 既定値 | 意味 |
|---|---|---|
| `mv_frames` | 64 | 使用フレーム数 |
| `mv_vote_frac` | 0.9 | 投票制彫刻の採択率(1.0=厳密hull) |
| `mv_vox` | 0.0034 | ボクセルサイズ(XYZ等方) |
| `mv_photo_consistency` | false | §7の凹形状精密化(実験) |
| `mv_pc_band` / `mv_pc_var_thr` | 4 / 素材依存 | photo-consistencyの帯幅/分散閾値 |
| `mv_atlas_res` | 2048 | ベイクアトラス解像度 |
| `mv_bake_k` | 4 | 法線適合度の指数(大=正対ビュー重視でシャープ、小=平均寄りでノイズ減) |
| `mv_bake_robust` | true | 外れサンプル再重み付け |
| `illust_weight` | 2.0 | 元イラスト(タブ2画像等)の重み(0=Veo質感のみ) |
| `illust_tone_match` | true | イラスト→動画のトーン合わせ |

現行の`psq_*`/`arm_*`/`hand_*`/`track_*`/`depth_gap_close_px`/`bone_bridge`/
`part_gap_close`/`seam_*`/`color_grad_*`は本パイプラインではすべて不要。

---

## 12. テスト計画(`tests/video-pipeline.test.js`)

1. **合成動画ゴールデン**: three.jsで既知GLBをターンテーブル・レンダリングした合成
   フレーム列をfixture化。(a)角度推定誤差<2°、(b)再構成メッシュのハウスドルフ距離
   閾値、(c)**ベイクテクスチャの再投影誤差**(生成GLBを同角度でレンダリング→合成
   フレームとのPSNR閾値)、(d)GLBのSHA-256ゴールデン。
2. **外れ値耐性**: フレームの10%にポーズ揺れ/ハイライト移動を注入し、投票彫刻と
   ロバストベイクで品質が閾値内に留まること。
3. **UI回帰**: スタジオのタブ進行(動画アップロード→検証→生成)+ビューア2への
   IndexedDB引き継ぎ(Playwright)。Gemini/Veo API呼び出し部はモックし、
   手動アップロード経路をテストの主経路にする(既存スキャナーのテスト方針と同じ)。

---

## 13. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| Veoがポーズ/キャラを回転中に変える | 形状痩せ・テクスチャゴースト | 投票制彫刻+フレーム信頼度+ロバストベイク。タブ4の不良フレーム手動除外 |
| 照明・ハイライトが回転中に動く | ベイクの色ムラ・ボケ | プロンプトで固定照明を強制+ロバスト再重み付け+チェックリストで不合格素材を弾く |
| 多フレーム平均によるボケ | シャープさ低下 | 法線適合度k乗の急峻化+sharpness重み+ロバスト化。`illust_weight`で正対は原画優位 |
| photo-consistencyがアニメ調平坦色で無力 | 凹形状が出ない | 既定OFFの実験パスとして隔離。効かなくても他Stageに影響なし |
| チャートUVの歪み・パッキング効率 | テクセル密度ムラ | 法線円錐角制限+密度統一パッキング。不足ならチャート分割閾値を締める |
| float render target等のWebGL拡張非対応 | ベイク不可 | ハーフフロート→8bit多パスの縮退実装。WebGPU対応は将来課題 |
| GPUメモリ(原寸フレームN枚) | モバイル等でクラッシュ | bboxクロップ+シルエット外黒塗り+`mv_frames`削減の自動フォールバック |
| 弱透視仮定とのズレ | 上下端の投影ズレ→二重写り | 望遠プロンプト指示+ロバストベイクで吸収。残差が大きければ焦点距離1パラメータ推定を追加 |
| Veo API課金・到達性 | タブ3が使えない | 既存スキャナーと同じ3手段(API/プロンプトコピペ+手動生成/ファイル選択)。手動経路を正規ルートとして設計 |
| スタジオ1ページの肥大 | 保守性悪化(巨大IIFE再発) | HTML側はタブUI+状態オブジェクトのみ、ロジックは全て`js/`新モジュールへ(§1.1) |

---

## 14. マイルストーン

| M | 内容 | 受け入れ基準 |
|---|---|---|
| M1 | スタジオのタブ1〜4骨格: 動画アップロード+フレーム抽出+シルエット/SDF+キャリブレーション可視化(Veo API連携は後回し、手動アップロード経路のみ) | 実Veo素材で角度曲線・残差表示、キーフレーム同定が目視で正しい。受け入れチェック自動判定が動く |
| M2 | 投票制彫刻(頂点色の仮テクスチャでプレビュー) | 合成テストで形状誤差閾値内。斜め45°形状が現行3面方式より明確に良い |
| M3 | チャートUV+GPUベイク(v2の本丸。骨格より先に検証) | 合成テストの再投影PSNR閾値。実素材で継ぎ目・色ムラが目視ゼロ。`illust_weight`スライダーが機能 |
| M4 | タブ5〜6(ランドマーク/領域分け、既存プロンプトD/F流用)+スキニング統合 | コントローラーで既存モーション再生 |
| M5 | ビューア2+`bakeSources`引き継ぎ+Playwright回帰一式 | `npm test`全PASS |
| M6(任意) | タブ2/3のGemini/Veo API自動生成、photo-consistency精密化、顔アップ動画の頭部高解像度上書き | API経路が手動経路と同じ状態に合流する/凹みが出る/顔の密度向上(実素材でA/B) |
| M7(任意、提案段階) | VRM出力: ヒューマノイドボーンマッピング+スプリングボーン(揺れもの)+表情(顔テクスチャ差し替え/UVアニメ)。詳細は§17 | VSeeFace/Warudo/cluster等の既存VTuberアプリで読み込み・トラッキング動作する。まばたき/口パク/揺れものが目視で自然 |

v1との最大の違いはM3の位置づけ(テクセルベイクこそが動画方式の存在意義なので、
骨格統合より先に単体検証する)。AI連携(API自動生成)は手動アップロード経路が
全区間通ってからのM6で足す(既存スキャナーの手動フォールバック実績があるため、
API無しでもパイプライン全体を検証できる)。

---

## 15. 既存3面方式との関係

- 3面方式の4ツールは全て無改造で併存(Veo素材が無い・作れない場合の正規ルート)。
- スタジオは既存スキャナー/ジェネレータのJSONをタブ1で読み込める(3面図・
  ランドマーク・colormapの再利用。逆方向の互換は持たない)。
- コントローラーはGLBしか見ないため共用。
- v2のテクセルベイク(Stage 7〜8)は入力が「キャリブレーション済みビュー集合」で
  あればよいので、**将来的に3面方式のアトラスをこの方式で置き換える**(3ビューだけの
  加重合成ベイク)ことも可能。seam系パラメータ群の廃止はその時点で3面方式にも波及できる。

---

## 16. M1+M2 実装状況(2026-07-18〜、`ghost_studio/`)

### 実装スコープと配置

計画時のファイル配置(§10)から**フォルダ構成を変更**した。`ghost_studio.html`を
リポジトリ直下に置く案から、`ghost_studio/`フォルダを新設しその直下に
`index.html`を置く形にした(成果物・テスト用サンプルを`ghost_studio/samples/`
にまとめて置きたいという要望のため)。新規jsモジュールは既存の慣習どおり
共有の`js/`直下に置き(`ghost_studio/index.html`から`../js/...`で参照)、
既存の`js/carving.js`(`buildGrid`/`marchingCubes`/`computeNormalsFixWinding`/
`dropSmallFragments`/`laplacianSmoothPreserveExtent`/`gridClusterDecimate`)を
そのまま流用している。

- `ghost_studio/index.html` — タブUI(1.動画投入 2.フレーム抽出 3.シルエット・
  較正 4.彫刻プレビュー)。M1のタブ5〜8(ランドマーク・領域分け・出力)は未着手
- `ghost_studio/samples/turntable_sample_360.mp4` — 手動生成+反転結合で作った
  検証用サンプル動画
- `js/video_frames.js` — Stage1。**WebCodecs経路は見送り**、`<video>`要素の
  seekベースのフォールバックのみ実装(mp4コンテナのデマルチプレクスに外部
  ライブラリが要り、ビルドなし・CDN非依存の方針に反するため)
- `js/edt.js` — Stage2。シルエット抽出(四隅背景色推定+色距離+既存
  `significantComponentsMask`/`fillHoles`流用)、符号付き距離場(Felzenszwalb
  2パス)
- `js/turntable_calib.js` — Stage3。**簡略版**: 計画書本文が言う「粗彫刻→
  再投影IoU最大化のanalysis-by-synthesis」は彫刻結果への反復依存があり
  実装コストが高いため見送り、bbox幅最小(=真横向き)のキーフレーム検出+
  区分線形補正のみ実装(M1受け入れ基準「角度曲線・残差表示、キーフレーム
  同定が目視で正しい」は満たす設計)
- `js/multiview_hull.js` — Stage4(M2)。投票制彫刻+coarse-to-fine(下記参照)。
  §7のphoto-consistency精密化は実験パス扱いのため未着手
- `js/model_export.js` — `buildStaticVertexColorGLB`を追加(既存の`buildGLB`は
  UV+テクスチャ+スキニング前提のため、M2の無UV・仮色メッシュ用に頂点カラー
  (`COLOR_0`)のみの簡易GLB書き出しを別関数として追加。既存`buildGLB`は無改造)

### 実地検証で見つかった不具合と対処(重要)

実際に生成した動画で彫刻してみたところ、当初は頂点が板状に潰れる・足元に
浮遊した塊が残る等、実用に耐えない品質だった。原因を1つずつ特定して修正した:

1. **足元の浮遊物**: 動画の白背景に残っていたごく薄い(ほぼ白に近い)影/
   台座のノイズが、フレームごとのシルエット抽出で本体と分離した別の連結成分
   として検出され、彫刻結果に浮遊した塊として混入していた。対処:
   `js/edt.js`の`silhouetteFromImageData`で最大連結成分のみを残すよう変更
   (ターンテーブル動画の全身シルエットは基本的に単一連結領域という前提。
   色分けマップの複数accessory検出とは前提が異なる)
2. **腕が板状に潰れる**: フレームごとのbbox高さ/bbox中心/bbox下端を毎回
   再計算して投影に使っていたため、フレーム個別のセグメンテーションノイズ
   (数px単位のブレ)がそのまま多数フレームの重ね合わせに乗り、細い部位が
   にじんで平べったくなっていた。対処: `calibrateTurntable`が全フレームから
   頑健に求めた基準値(`refHeight`/`refCx`/`refYbot`)を**全フレーム共通**で
   使うよう変更(フレーム個別の実測値は使わない)
3. **メッシュが空になる回帰**: 上記2の修正時、除外フレーム処理用に
   `calib`を複製する箇所(`ghost_studio/index.html`)で`refCx`のコピーを
   忘れ、`undefined`が投影計算に伝播してNaN化→marching cubesが等値面を
   一切検出できなくなっていた。ケアレスミスだが、基準値を増やすたびに
   複製箇所の伝播漏れが起きうるので注意
4. **0/90/180/270度の重み付け**: ユーザー指摘により、この4方向(3面方式の
   front/side/backに相当する基準ビュー)は較正で直接特定できている
   (0度はフレーム0そのもの、90/270度はシルエット幅最小のキーフレーム検出)
   ため、他の中間角フレームより信頼度を大幅に上げて投票を支配させるべき、
   という設計に変更(`CARDINAL_WEIGHT_MULT=15`)

### 解像度・頂点数まわりの方針転換(重要、性能問題あり)

当初M2は「投票制彫刻(頂点色の仮テクスチャでプレビュー)」という位置づけで
軽く見ていたが、実地検証で「3面方式は最終(間引き後)約5万頂点」という
基準が示され、**彫刻は画像の実ピクセル1個=1ボクセル相当の解像度で行い、
既存の`gridClusterDecimate`で目標頂点数(既定5万)まで間引く**方式に転換した。

- 精細ボクセルサイズは`calib.refHeight`(px)から自動計算(既定、UIで上書き可)
- **性能問題が発覚→解決**: 既定の外接範囲(体全体+余白)をそのまま実ピクセル
  解像度で彫刻しようとすると2億ボクセル超になり、10分以上ハングしたように
  見えるレベルで非現実的に遅かった。対処として
  coarse-to-fine(粗解像度で1回彫刻→等値面近傍`bandMargin`以内だけを精細
  解像度で再計算、深い内部/外部はcoarse値を流用)に加え、①coarse結果から
  実形状のtight bboxを求めて精細グリッドの走査範囲を絞る
  (`tightBoundsFromCoarseField`)、②精細パスの重み付き分位点計算のボトルネック
  が「フレーム数ぶんのソート」ではなく**ホットパス内での配列/クロージャの
  毎回割り当て(GC圧力)**だったと判明したため、`voteAtPoint`をスクラッチ
  バッファ(使い回しのTypedArray)+モジュールスコープの比較関数へ書き換え、
  ③精細パスで使うフレーム数に上限(既定48、0/90/180/270度は必ず保持)を
  設ける(`subsampleFramesKeepingCardinals`)、の3点で対処した。
- **実測結果(148フレーム抽出中64フレーム使用、既定パラメータ)**: 精細grid
  627×647×249(約1.01億ボクセル、narrow band 14.8%=約1495万ボクセル)、
  彫刻(粗+精細)全体で**約40〜50秒**まで短縮(対策前は10分超でも未完走)。
  彫刻直後621,330頂点→`gridClusterDecimate`で目標5万頂点に間引き後
  **27,445頂点/57,304面**(目標よりやや少なめ。`gridClusterDecimate`の
  目標頂点数はセルサイズからの概算のためこの程度のズレは想定内)。
  セーターのシワ・指の形・スカートの広がり・髪の房などのディテールが
  目視で確認できる品質になった。

### mv_vote_frac(採択率)の再検討(重要、ユーザー指摘・未解決)

M2完了報告後、ユーザーから「悪くないけど四角くなってる、40点。髪もうまく出ていない」
「0/90/180/270度を強くしたいのではなく、そこからブレるとおかしいという意味。
全フレームが同格の直接証拠のはず」という指摘を受けた。整理すると:

- **CARDINAL_WEIGHT_MULTは撤回**した(15→1、事実上無効化)。特定角度だけ
  重みを強くするのはユーザーの意図(全フレーム同格)と逆だったため。
- 「全フレームが同格」を素直に実装すると**厳密な交差(AND、`mv_vote_frac=1.0`)**
  になる。既定値を0.9→1.0に変更し、bandMargin(coarse-to-fineのnarrow band幅)
  もcoarseVox*1.5→*4に拡大(ブロック状に見える原因が、narrow bandからこぼれた
  細い部分に粗い解像度の値がそのまま出ていることだと推測したため)。
- **実測(64フレーム、mv_vote_frac=1.0、bandMargin=coarseVox*4)**: 彫刻直後
  436,546頂点(mv_vote_frac=0.9時点の621,330より減少)→間引き後22,142頂点/
  45,516面。**懸念通り、頭部・髪の毛がボロボロに欠ける結果になった**
  (ギザギザの穴だらけ)。厳密AND(1点でも「外側」と言うフレームがあれば
  除外)は、較正に残るサブピクセル単位のノイズにそのまま弱く、細い部位
  (髪)から壊れていくことが実地で確認できた。体・脚・スカートは大きく
  崩れていない。
- **未解決・次回に持ち越し(2026-07-18時点、以下は撤回)**: ~~1.0(完全AND)と
  0.9(緩い多数決)の中間(`mv_vote_frac=0.95〜0.99`)で、髪の欠損が起きない
  最も厳しい値を探る作業に着手したが、セッション終了時点で結果が出ていない。~~
  この探索方針は下記の通り撤回した。単一の分位点(quantile)は「1点」の
  統計量であるため、境界をどこに引いてもノイズに対して脆弱な閾値効果が残る
  (0.99でも1.0でも「その1点の外側にいる1フレーム」に形状が丸ごと左右される
  構造は変わらない)。0.95〜0.99刻みの探索は「どのノイズ耐性のなさが
  一番マシか」を探す作業になってしまっており、根本原因(統計量の選び方)への
  対処ではなかった。

### 統計量の見直し: quantile方式からbottom-k平均へ(方針転換、2026-07-18)

ユーザーからの再指摘「0/90/180/270度を強くしたいのではなく、そこからブレると
おかしいという意味。全フレームが同格の直接証拠のはず」を受けて再検討した。
「全フレーム同格」という要求を**厳密AND(quantile=1.0)として実装したのが
誤り**だったと判断する:

- ANDは「同格」の実装ではなく「どの1フレームにも拒否権を与える」制度になる。
  較正に残るサブピクセル単位のノイズが1フレームでも「外側」と言えば、他63
  フレームの証拠を無視して削れてしまう。実測(§前掲)で髪から壊れたのはこの
  構造的欠陥の表れであり、0.95〜0.99の探索で「マシな閾値」を探しても、
  同じ脆弱性を程度の差で先送りするだけだった。
- 一方、単一分位点(0.9等)ではノイズを**平均して打ち消す**働きが無いため、
  場が滑らかにならずブロック状の見た目(ユーザー指摘「四角くなってる」)が
  残る。
- 「全フレームが同格の直接証拠」を最も素直に実装する統計量は**平均**である。
  ただし全フレームの単純平均は交差性(股・脇等の隙間を彫る力)を壊すため、
  各ボクセルで**符号付き距離が小さい(=表面に近い、その点にとって最も強い
  証拠を持つ)側から並べた下位k件(既定 k≈10%)の重み付き平均**
  (bottom-k mean)を採用する:
  - 表面点では、接線方向から見ている少数フレーム(効いているフレーム)だけが
    平均に入り、較正ノイズが `1/√k` 相の縮小で打ち消される→1フレームの
    ノイズで髪が消えない。
  - 隙間領域では、下位k件も全員「外側」の値を返すため交差性は保たれる
    (visual hullとしての妥当性は維持)。
  - 場が滑らかになるため、marching cubes段のブロック感も直接軽減する。
  - 実装上はホットパスが軽くなる副次効果もある: 現行の「重み付き分位点」
    (フレーム数ぶんのソート+スクラッチバッファ)より、「下位k件の部分選択
    +加算」の方が計算・GC負荷とも小さく、§16で問題になった連続彫刻時の
    タイムアウトの圧力低減も期待できる。
  - この変更により`mv_vote_frac`という「妥協の1点を探すパラメータ」自体が
    不要になる(廃止し、`mv_bottomk_frac`のような比率パラメータに置き換える)。
    テクスチャ側で`seam_*`系パラメータ群がベイク方式転換により丸ごと
    不要になった(§9.3)のと同じ構造の整理。

**あわせて較正精度の底上げも優先する**: bottom-k meanは較正ノイズを統計的に
打ち消す設計だが、根本原因である較正のサブピクセル誤差そのものを減らせば
効果はさらに安定する。§5で簡略実装に留めていたanalysis-by-synthesis
(Stage3)を、フル反復ではなく縮小版で追加する: 粗彫刻→各フレームへ
シルエット再投影→**フレームごとにθ_i+2Dシフト+スケールの4パラメータ**を
IoU最大化で座標降下2〜3反復。Veoの被写体は完全剛体ではない(わずかな
フレーム間ドリフトがある)ため、フレーム個別の2Dドリフト吸収が特に効くと
見込む。

### bottom-k平均の再撤回→単一連結クラスタ平均への再置換(2026-07-19、実装・実測済み)

上記のbottom-k平均(固定比率)を実装し、Stage3較正精密化(analysis-by-synthesis
簡易版、`js/turntable_calib.js`の`refineCalibrationAnalysisBySynthesis`)も
合わせて実装した。しかし実サンプル動画(帽子キャラ、ツインテール)での実機確認で
ユーザーから「精度は上がったがツインテール表現できてない。ちゃんと穴開けれて
ない。90度で見ると一目瞭然」という指摘を受け、**bottom-k平均も別の欠陥を持つ
ことが判明した**:

- ツインテール間・帽子のつばの裏側のような「ごく一部の角度域(2〜4フレーム
  程度)からしか外側と判定できない狭い可視窓」を持つ隙間で、固定比率
  (既定10%=64枚中約6〜7枚分)の重みを機械的に埋めようとするため、実際に
  外側と言っている少数フレームだけでは予算が余り、残りはオクルージョンで
  たまたま内側寄りに見えている無関係なフレームで埋められ、平均が内側(正)に
  寄って隙間が彫れなくなっていた。「固定個数/固定比率を強制する」という
  設計自体が原因(§9.4のようなquantile一点選択の脆さとは別種の欠陥)。
- 対処として、**最小値から始める単一連結(single-linkage)クラスタの重み付き
  平均**(`clusterMeanFromMin`/`voteAtPoint`、`js/multiview_hull.js`)へ
  置き換えた: ソート順に隣接値との差が`clusterEpsPx`(元動画のpx単位)以内で
  ある限り連鎖的に取り込み、差が閾値を超えた時点で打ち切る。固定個数/固定
  比率を強制しないため、クラスタは点ごとに自然なサイズになる(狭い可視窓の
  隙間なら少数、広い接線角度域なら多数)。
- **実装時に見つかった副作用と対処**:
  1. **上限なしの連結は性能問題を再発させた**: narrow band(表面近傍)の点は
     多くのフレームでdが緩やかに連続変化するため、上限無しだと48枚中40枚
     以上を1クラスタに飲み込むことが常態化し、旧bottom-k(固定10%)より
     大幅に遅くなった(実測: 彫刻386秒、タイムアウト閾値400秒に迫った)。
     `MAX_CLUSTER_WEIGHT_FRAC=0.25`でクラスタ増殖に上限を設けて対処。
  2. **既定`clusterEpsPx=3.0`(較正ノイズのみを想定した小さい値)は表面全体を
     スパイク状にノイズらせた**: 較正精密化の有無に関わらず再現したため
     (較正精密化オフでも同じ症状が出た)、原因は較正ノイズではなく、隣接
     フレーム間(64枚/360°で約5.6°刻み)の**真の**形状変化量(曲面なら角度が
     変わるだけで実際にd値が数px動く)が較正ノイズと同程度の大きさだった
     ため、3px程度の許容差ではほぼ全点でクラスタが1〜2枚しか繋がらず、
     単一フレームに近い脆弱な結果に逆戻りしていたと判明。既定値を
     **`clusterEpsPx=10.0`に変更**したところ表面が滑らかになり、
     0/90/180/270度の再投影IoUもわずかに改善した(0.86前後→0.89前後)。
- **実測比較(bottom-k mean → 単一連結クラスタ平均、eps=10、64フレーム、
  較正精密化オン)**: 彫刻直後頂点数408,210→**534,304**(隙間が実際に
  彫れるようになった分、頂点数は増える方向に変化)。間引き後25,658頂点/
  52,260面。基準4方向再投影IoU: 0°=0.893 90°=0.892 180°=0.868 270°=0.897
  (bottom-k平均時点では未計測。この診断自体が今回新設)。45度側面の
  スクリーンショットで、帽子のつばの下から左右に分かれて垂れる房状の形状
  (ツインテールに相当)が視認できるようになったことを確認した(bottom-k平均
  時点では帽子下が単一の塊だった)。
- `mv_vote_frac`は完全に廃止し、`mv_bottomk_frac`という置き換え案も
  不採用とした(bottom-k自体が構造的欠陥を持っていたため)。最終的な
  gen_paramsは**`mv_cluster_eps_px`(既定10.0)**。

### 0/90/180/270度の再投影フィデリティ診断(2026-07-19、新設)

ユーザー指摘「0度90度180度270度で合うような方式であるかもちゃんと考えて
実装すべき」を受けて、彫刻結果を基準4方向の実測カメラへ再投影し実測
シルエットとのIoUを返す`P3D.evaluateReprojectionIoU`
(`js/turntable_calib.js`)を新設した。彫刻(タブ4)完了後、
`ghost_studio/index.html`に自動表示される。目視でモデルを回転させなくても
基準4方向での忠実度を機械的に確認できる(§12のPSNR診断のプロトタイプ的
位置づけ)。

### 「0/90/180/270度でピクセル単位一致」の実測結果(2026-07-19、重要・未解決)

ユーザーから「IoU 0.87〜0.90を『できた』と言うな。ピクセル単位で完全一致
しているか確認してから先に進め」という強い指摘を受けた。これは正しい指摘
だった: `evaluateReprojectionIoU`は128px角へダウンサンプルしたIoUで、
輪郭が全周にわたって数〜十数px想像以上にズレていても高い値(0.87〜0.90)が
出てしまう、実態を隠す指標だった。

**`P3D.renderReprojectionOverlayAt`(新設)で元動画解像度(720px角)相当・
境界距離px単位の実測を行った結果**:

| 角度 | IoU(720px相当) | 境界距離 平均 | 境界距離 95%点 | 境界距離 最大 |
|---|---|---|---|---|
| 0° | 0.905 | 4.4px | 17.0px | 30.4px |
| 90° | 0.894 | 4.3px | 13.2px | 22.0px |
| 180° | 0.880 | 5.4px | 23.0px | 41.6px |
| 270° | 0.897 | 4.4px | 15.8px | 25.5px |

**結論: ピクセル単位での完全一致には全く到達していない。** 720px級の動画で
平均4〜5px、外れ値では最大42pxのズレがあり、これは輪郭全体に均一に散った
アンチエイリアス誤差の範囲を明確に超える。オーバーレイ画像(白=一致・
緑=実測のみ・赤=予測のみ)で見ると、ズレはランダムではなく**構造的に
特定箇所へ集中**していた:

1. **ツインテール(房)が4方向すべてで緑(実測にあるのに彫刻に無い)**。
   特に180度(背面)側で顕著。今回のクラスタ平均+較正精密化は「房を
   分離する」ことには効いたが、「太さ・長さを実物と一致させる」ことには
   全く効いていない。
2. **90度/270度で顎下〜首元に赤い塊(彫刻側にだけある余剰形状)**。
   Tポーズの腕がカメラ軸方向を向く角度で紛れ込んだ余剰形状と推定される
   (未調査)。
3. **靴底に緑(彫刻が実物よりわずかに短い)**。

**原因は複合的で、`clusterEpsPx`等のパラメータ微調整では埋まらない**:
- 間引き(彫刻直後53万頂点→間引き後2.6万頂点)による形状の丸め
- ラプラシアン平滑化(3反復)
- coarse-to-fineの粗解像度フォールバック(narrow band外はcoarse値を採用)
- クラスタ平均そのもの(ノイズ耐性と引き換えに輪郭の鋭さを犠牲にする設計)
- 視覚的殻(visual hull)方式そのものの限界(自己遮蔽・細い形状は原理的に
  苦手、§16 todo旧7で既に指摘済みの限界が実測で裏付けられた形)

このため、**この先の作業は上記の要因を1つずつ切り分けて、どこまで
ピクセル一致に近づけられるか(あるいは近づけられない性質のものか)を
明らかにすることを最優先とする**。M3(テクスチャベイク)は幾何が
ピクセル一致していない状態で着手すると、ベイクされる色がそもそも
ズレた場所に貼られることになり手戻りが大きい。

### npm testの3件の失敗はこのセッションの変更と無関係(2026-07-19、切り分け済み)

本セッションの変更を`git stash`で退避しコミット済みベースラインで`npm test`を
実行しても、**同じ3件**(`controller-ui`のWebGLコンテキストロスト復帰、
`generation-pipeline`と`viewer-ui`の`character_3d.html`への遷移タイムアウト)
が失敗することを確認した。3ツール(`controller.html`/`landmark_tool.html`/
`character_3d.html`)側のコードは本セッションで一切変更していないため、この
実行環境(サンドボックス)固有の既存の不安定さであり、本セッションの変更が
原因ではない。`tests/README.md`の「既知の注意点」にWebGLコンテキストロスト
テストの不安定さが既に記録されており、今回の`character_3d.html`遷移
タイムアウトも同種の環境要因である可能性が高い。次回セッションでも
再現する場合はこの実行環境側の問題として扱うこと(本セッションのpipeline
変更を疑って`git stash`で切り分ける必要はない、済んでいる)。

### 方針転換: 色・特徴の追跡で立体を取る「回転ステレオ」方式(2026-07-19、机上確認済み・実装は次回)

ピクセル単位一致の実測(§前掲)を受けて、ユーザーから「角度もそうだけど
色も含めて立体取らないと、できないんじゃない。例えば正面の特徴を捉えて
どう移動していくかとか」という方針指摘があった。これは本質的な指摘で、
**現行パイプラインは動画のピクセルの大半(色・模様・内部エッジ)を幾何に
一切使っておらず、シルエット(輪郭)だけで立体を取ろうとしていた**のが
根本の限界だった。以下の新方式を採用する(理論的な机上確認まで完了、
実装は次回。ユーザー指示により追加の実機検証はせず机上確認で確定)。

#### 原理(なぜターンテーブルで特別に強いか)

固定カメラ+Y軸回転+弱透視の条件下で、表面点(x,y,z)のフレームiでの投影は:

```
u_i = cx + s·(x·cosθ_i + z·sinθ_i)     … 既知角θ_iに対して (x,z) の「線形」式
v_i = ybot − s·(y − yMin)              … フレームによらずほぼ一定(同じ行)
```

つまり1つの特徴点(瞳、髪の房の境界、服の柄の角、セーターのシワ等)を
複数フレーム追跡して u_i を集めれば、**未知数(x,z)の線形最小二乗**で
その点の3D座標が閉形式で出る(非線形フィット不要)。v_i は高さyを与え、
トラックの縦ドリフトはそのままフレーム別較正誤差の実測になる。

#### 机上確認の結果(理論的破綻の有無)

| 検討項目 | 結論 |
|---|---|
| 可識別性(解の一意性) | u_iは(x,z)の線形式なので、θの観測スパンが2角度以上あれば一意(鏡像等の曖昧性なし)。実際のトラックは可視窓≈半回転−自己遮蔽で数十フレーム分の観測があり、条件数も良い。**破綻なし** |
| 遮蔽・追跡断絶 | 可視窓の端でマッチングコストが上がって自然に途切れる。誤追跡は2未知数の線形フィットに対する残差ゲート/RANSAC(2点で候補が決まるので極めて軽い)で棄却可能。**破綻なし** |
| アニメ平坦色(特徴が無い領域) | アンカーが取れないのは事実だが、特徴が無い場所はテクスチャも平坦なので幾何誤差が見た目に出ない(ズレが痛い場所=エッジのある場所=追跡できる場所、という相補性)。平坦領域はhull+補間で埋める。**破綻ではなく限界の許容** |
| アパーチャ問題(直線エッジ上の点) | 回転による動きは水平なので、縦エッジはu(=幾何)に効き、横エッジはv(=ドリフト較正)に効く。特徴の種類で使い分ける(標準的な扱い)。**破綻なし** |
| v=一定の仮定 | 弱透視+純Y回転なら厳密に成立。Veoのドリフト・弱い透視効果はvの系統変化として現れ、それ自体が較正情報になる。**破綻なし(むしろ測定チャネル)** |
| Veoの時間的非剛性(フレーム間でディテールが微妙に描き変わる) | 最大のリスク。ただしトラックの動きは低周波で、長い弧への正弦波(線形)フィットが平均化する+フィット残差で信頼度を数値化して悪いアンカーを落とせる。**精度フロア≈1px前後を規定する要因だが破綻ではない**(このフロアはテクスチャベイクを含む全工程に共通の、素材そのものの限界) |
| 過去のCOLMAP棄却との整合 | 棄却理由「平坦色で特徴マッチング壊滅」は視点が大きく離れた画像間の総当たりマッチングの話。今回は5.6°刻みの隣接フレーム追跡+動きは1Dの既知モデルという遥かに簡単な問題で、当たらない。**矛盾なし** |
| 計算量(ブラウザ完結制約) | 64フレーム×数百特徴×NCC探索(予測付きなら探索窓は小さい)≈数G乗算=素のJSで数秒〜数十秒。ビルドなし・GPU不要で実装可能。**実現可能** |
| 精度の見込み | NCCサブピクセル補間で1観測≈0.2〜0.3px、1トラック≈30観測の線形フィットで統計誤差は0.1px未満まで縮む。実効精度はVeo非剛性フロア(≈1px)が支配。**「ピクセル単位一致」の目標と整合** |

#### 新パイプライン構成

```
Stage A: シルエットhull彫刻(現行)= 役割を「外側の上限・初期値」に限定
Stage B(新設・本丸): 特徴検出→隣接フレーム追跡(NCC、行拘束付き)
         →(x,z)線形フィット→3Dアンカー点群+色パッチ
         +全トラック残差の交互最小化によるθ_i/ドリフト/スケール同時精密化
         (現行のCanvas2D再投影IoU座標降下版analysis-by-synthesisを置き換える。
          こちらの方が速く、サブピクセル精度で、全フレームを同時に使う)
Stage C: 融合 — hullのSDFをアンカーで補正
         ・アンカーが等値面の外→過彫刻の証拠→局所的に field を戻す
           (ツインテールの細り対策。hullの外には出さない=シルエット整合は維持)
         ・アンカーが等値面より明確に内側→凹みの証拠→そのアンカーを観測した
           視線に沿って手前の空間を彫る(対応点既知のspace carving。§7の
           色分散方式を置き換え、実験パス扱いを卒業させる)
Stage D: ベイク(M3、§9)— 無改造。アンカーはテクスチャ位置合わせ精度も上げる
```

新規モジュールは`js/feature_tracks.js`(検出・追跡・フィット・同時較正)と
Stage C融合の`js/multiview_hull.js`への追加。実測で確定した3つのズレとの
対応: ツインテールの細り→房のエッジはアンカーが実際の太さを固定/90・270度の
顎下の余剰→対応するアンカー不在+色不整合で削る根拠が得られる/靴底の
短さ→トラック縦ドリフトからybot較正を直接補正。

#### あわせて確定した方針(2026-07-19、ユーザー決定)

- **間引きは維持**(コントローラー用に必要)。
- **ラプラシアン平滑化は廃止**(実装済み: `meshFromField`の既定smoothIters=0。
  平滑化は表面を縮める方向に働き境界誤差を直接悪化させる。細い部位=
  ツインテールほど縮む。fineVoxが実ピクセル相当の現行設定ではボクセル
  ガタつきも元々小さい)。
- **精細パスは全フレーム使用**(実装済み: maxFineFrames既定48→Infinity。
  「全フレーム一致」が目標である以上、精細パスから外したフレームは
  系統的に不一致になるため)。
- **受け入れ基準は「全フレームの実測シルエットとの再投影ピクセル一致」**。
  測定は`renderReprojectionOverlayAt`を全フレームに回した境界距離
  (mean/p95/max)。目標はmean 1px級(Veo非剛性フロア相当)。
- **64フレームで幾何は足りる**(机上計算): 5.625°刻みの接線サンプリングが
  輪郭に残す食い込み誤差はR·Δθ²/8で、最大半径(Tポーズ指先R≈345px)でも
  ≈0.4px=サブピクセル。実測された4〜5pxのズレの原因はフレーム数ではない。
  特徴追跡でもトラックあたり≈30観測が取れて十分(指先等の速い部位は
  フレーム間最大≈34px動くが、正弦波予測付き探索で追跡可能。必要なら
  同じ動画から追跡専用に密なフレームを追加抽出すればよく、再生成は不要)。
- 平滑化廃止・全フレーム化の2変更は実装済みだがユーザー指示により実機
  検証は行っていない(次回、Stage B実装時の全フレーム境界誤差測定に含める)。

### 既知の未対応・todo(次回セッションへの引き継ぎ、2026-07-19更新)

1. **最優先(新規)**: Stage B(`js/feature_tracks.js`)の実装。特徴検出→
   NCC追跡(行拘束+正弦波予測)→(x,z)線形フィット→アンカー点群+
   θ/ドリフト同時精密化。完了時にIoU座標降下版
   `refineCalibrationAnalysisBySynthesis`を置き換える
2. Stage C(アンカーとhull SDFの融合)の実装。過彫刻の押し戻し(hull上限)+
   対応点既知space carvingによる凹み彫り
3. 受け入れ測定: `renderReprojectionOverlayAt`を全フレームに回し、境界距離
   mean/p95/maxのフレーム分布を出す(平滑化廃止・全フレーム化の効果測定も
   ここに含める。前回実測値: 基準4方向でmean 4.3〜5.4px/p95 13〜23px/
   max 22〜42px。これを1px級まで縮めるのが目標)
4. M3(UVチャート+GPUベイク)は幾何のピクセル一致(上記3)が確認できてから。
   品質基準は§12テスト(c)の再投影PSNR
5. `gridClusterDecimate`が目標頂点数よりやや少なめに間引く傾向がある。また
   間引きが境界誤差に与える寄与は未測定(生メッシュとの比較測定は次回)
6. 彫刻時間(較正精密化込み約370〜390秒、旧48フレーム時)。全フレーム化で
   さらに約3割増える見込み。Stage Bでの較正置き換え(Canvas2Dラスタライズ
   撤廃)が最大の短縮源になる見込み
7. `clusterEpsPx=10.0`は1キャラクター・1動画でのみ検証した値。複数素材での
   再確認が要る(Stage C導入後は役割が「初期値の品質」に下がるため優先度低)
8. 髪(房)の太さ不足はStage B/Cのアンカーで解消される見込みだが、それでも
   残る場合はアクセサリー(髪)別パーツ方式への切り替えを検討する

### Stage B実装・実測結果(2026-07-19、今回セッション。todo 1実施・todo 3の一部実施)

`js/feature_tracks.js`を実装し(Shi-Tomasiコーナー検出→隣接フレームNCC追跡
[行拘束+走行中の線形フィットによる予測]→(x,z)線形最小二乗+シグマクリップ
残差ゲート→全トラック残差の交互最小化によるフレーム別θ/cx/ybot/scale同時
較正)、`ghost_studio/index.html`タブ3に組み込んだ(旧IoU座標降下版
`refineCalibrationAnalysisBySynthesis`は彫刻前の較正段階で不要になったため
既定オフに変更。比較用に残置)。合成データ(既知のフレーム別ドリフトを
注入した検証、`sigmaClip`が単一トラック内の外れ値観測だけを正しく除去し
track全体を誤って棄却しないこと、完全なランダムトラックは正しく棄却される
ことをNode上で確認済み)では較正パラメータをサブピクセル精度で復元できた。

実素材(`ghost_studio/samples/turntable_sample_360.mp4`、64フレーム)での
実測:

| 指標 | 値 |
|---|---|
| 検出トラック数 → 残差ゲート後採用数 | 567 → 379 |
| Stage B較正後の全トラック平均u残差 | **1.115px**(目標のVeo非剛性フロア≈1pxにほぼ到達) |
| 較正+追跡の所要時間(64フレーム) | 約14秒 |
| 彫刻結果`renderReprojectionOverlayAt`境界距離(全64フレーム) mean/p95/max | **5.47px / 20.4px / 37.3px** |
| 彫刻結果IoU(全64フレーム) mean | 0.878 |

前回実測(旧IoU座標降下較正、基準4方向のみ): mean 4.3〜5.4px/p95 13〜23px/
max 22〜42px。**今回は横ばい〜わずかに悪化**(全64フレーム評価なので単純
比較はできないが、範囲が重なっており明確な改善は見られない)。

**診断**: 較正精度そのものは理論限界(トラック残差1.1px)まで達したにも
関わらず彫刻結果の境界誤差が変わらないのは、**ボトルネックが較正から
彫刻(投票)アルゴリズム側に移った**ためと考えられる。具体的には
`clusterEpsPx=10.0`(単一連結クラスタの許容差、multiview_hull.js)は
「較正がまだ数px単位でブレていた時代にそのノイズを均すため」に選ばれた
値であり、較正誤差が1px級まで縮んだ今は不要な平均化(=輪郭のぼかし)に
なっている可能性が高い。加えて彫刻直後535,310頂点→間引き後24,630頂点
(目標50,000の半分以下、todo 5参照)という強い間引きも輪郭を丸める要因
になり得る。次回はまず`clusterEpsPx`を下げて(例: 10→3〜4px)再彫刻し、
境界距離が縮むかどうかで「較正 vs 彫刻アルゴリズム」のどちらが支配的かを
切り分ける(計測は彫刻のみやり直せばよく、較正・追跡の再実行は不要)。
根本的にはStage C(アンカー↔hull SDF融合、todo 2)が彫刻結果自体を
アンカーに合わせ込む設計なので、この切り分け実験の結果に関わらずいずれ
着手が要る。

Stage Bの実装によりtodo 1は完了、todo 3(受け入れ測定)は実施済み(数値は
上表)。todo 7(`clusterEpsPx=10.0`の再検証)は上記の切り分け実験と統合して
次回優先的に扱う。

---

## 17. VTuber化ロードマップ(VRM出口・テクスチャ表情方式、M7相当、2026-07-19提案)

★2026-07-19、ユーザー提案(Stage B実装待ちの雑談中に着想、計画への追記として
採用。まだ着手はしていない提案段階のロードマップで、実装順序はM3(テクセル
ベイク)完了後)。「3Dで、VTuberも3Dで、可愛く」という目的を置くと、出口
フォーマットをGLBからVRMへ伸ばすだけで既存VTuberエコシステム全体に接続でき、
今のパイプラインの遠回りに見える地味な作業(Stage Bのピクセル一致、平滑化廃止)
がそのままVTuber化の土台になる、という整理。

### 戦略の核: 出口をGLBからVRMへ伸ばす

VRMはGLBの拡張仕様で、日本の3D VTuberエコシステムの事実上の標準規格。VRM準拠に
すればVSeeFace・Warudo・cluster等の既存VTuberアプリにそのまま読み込め、
トラッキング・配信ソフトを自作する必要が無い。「ブラウザ完結で可愛いVRMを
生成する」ことだけに集中し、外部エコシステムとの接続は標準フォーマットに
委譲する、という分業がこのプロジェクトの制約(サーバサイド無し・ビルド無し)
とも噛み合う。

GLB→VRMに足りないものは3つ(難易度順):

1. **ヒューマノイドボーンマッピング(易)**: 既存の`js/skeleton.js`のpivots/骨格を
   VRMの標準ボーン名(`hips`/`spine`/`head`/`leftUpperArm`等)に対応付ける変換
   テーブル1枚で済む。
2. **スプリングボーン(揺れもの、中)**: ツインテール・スカート・マフラーが歩き・
   首振りで揺れる。「可愛さ」の体感に対する費用対効果が最も高い項目。今まさに
   ピクセル一致で戦っているツインテールは、静的な形状精度だけでなく「揺れる
   パーツとして独立している」ことにも価値が出る(§16 todo 8「髪の房の太さ不足が
   残る場合はアクセサリー別パーツ方式への切り替えを検討する」という判断に
   影響: 別パーツ化は揺らす前提ならむしろ好都合になる)。
3. **表情・ブレンドシェイプ(難だが抜け道あり)**: まばたき・口パク・笑顔。
   メッシュ変形(モーフターゲット)が正攻法だが、アニメ調ゲームの定石は
   **目と口をテクスチャ差し替え/UVアニメで表現する**方式。このパイプラインとは
   相性が良く、「表情差分の顔テクスチャをAIで生成→ベイク済みアトラスの顔領域
   だけ差し替え」というAIネイティブな表情システムが組める。彫刻結果から幾何
   モーフを逆算する必要が無い。

### 「可愛い」の分解と優先順位

可愛さの実体は影響が大きい順に ①絵(テクスチャ)の可愛さ ②顔 ③揺れ・仕草
④シルエットの綺麗さ、と分解できる。現行計画への対応:

| 要素 | 現行計画での担当 | 備考 |
|---|---|---|
| ①テクスチャの可愛さ | M3(チャートUV+加重合成ベイク) | 可愛さの最大の搬送路。ズレた幾何にベイクすると顔の線がにじむため、順序は変わらず「Stage B(幾何のピクセル一致)→M3」が正解。今の地味な作業はM3の土台工事でもある |
| ②顔 | §3.6(顔アップターンテーブル動画→頭部高解像度上書き、M6) | アニメ顔は「ほぼ平面+絵が命」。VTuber化するなら優先度を上げるべき項目 |
| ③揺れ・仕草 | 本節(M7)のスプリングボーン+まばたき+視線(look-at)+アイドル揺れ | この4点だけで「生きて見える」の体感の7割が出る見込み |
| ④シルエットの綺麗さ | Stage B・平滑化廃止(§16) | 元動画(Veoの出力)自体が可愛いので、そこにピクセル一致させること自体が可愛さの保存になる |

### 結論・実装順序

方向性は一本道: **Stage B(幾何のピクセル一致)→ M3(ベイク=絵の可愛さ)→
VRM出口(ボーンマッピング→スプリングボーン→テクスチャ表情)**。M7は既存の
M1〜M6のいずれも置き換えず、その先に積む追加ゴールという位置づけ。着手は
M3完了後(§14の「M3の位置づけ」方針と同じ理由: 土台が先)。

---

## 18. Stage B/C実装・実測、および方針転換の検討(2026-07-19、本セッション)

### Stage C実装・実測結果

`js/multiview_hull.js`に`fuseAnchorsIntoHull`(過彫刻の押し戻し+凹みの
space carving)を実装し、カメラ方向の符号規約(§3.2のプロンプトVの回転
方向記述から導出、合成テストで検証済み)も確定させた。実素材での実測:

- `clusterEpsPx`を10→4→2まで絞っても境界距離は変化なし(mean 2.340→
  2.340→2.322px、raster256換算)。todo 7は「検証済み・無関係と判明」で
  クローズ。
- Stage C適用後も境界距離は無改善(mean/p95/max とも小数点以下でしか
  変化せず)。内訳: 全379アンカー中、過彫刻の押し戻し**0件**、凹みの
  space carving 154件、補正不可108件、補正不要117件。

### 根本原因(ゼロベースで再検討して判明)

`clusterEpsPx`が無関係だった理由も、Stage Cが無効だった理由も、同じ1つの
構造的事実で説明できる: **視覚的ハル(視錐交差)は定義上、全フレームの
中で一番厳しい制約(交差=min操作)を超えられない。** ツインテールのような
回転軸から外れた方向へ張り出す細長い/偏平なパーツは、一周のどこかで必ず
エッジオン(ほぼ真正面)に見える角度が存在し、その角度でのシルエット幅が
「一番厳しい制約」として全体を支配する。これはノイズではなくbimodalな
証拠の断絶なので、epsilonをどう振っても解消しない。

さらに、Stage Cの「過彫刻の押し戻しはhull=シルエット整合を上限とする」
という設計(`hardHullAtPoint`、全フレーム中の厳密な最小値)は、**この
細り現象を引き起こしている量そのもの**を上限に使っていたため、原理的に
このケースを直せない自己矛盾になっていた(実測でoverCarved=0件だったのは
当然の帰結)。この再検討はユーザー指摘により中断・保留中(コード自体は
コミット済みだが、設計の作り直しは次回以降の課題)。

### 方針転換の検討: 動画→3D化を続けるべきか

上記の発見を受け、ユーザーから「そもそもこの動画から3D化を進めるべきか」
という問いが出て、代替アーキテクチャ(事前生成2D差分アセット+LLMルー
ティングによるVTuber駆動エンジン)の検討に移った。詳細・議論の全内容は
**`VTUBER_ASSET_ENGINE_PLAN.md`**(新設)に記録した。動画→3D化を完全に
止めるとは決めていない(併用の可能性も残る)が、少なくとも「ピクセル単位
一致」を目標にした本チャプター(§16)のこれ以上の深追いは、次の判断が
出るまで保留する。
