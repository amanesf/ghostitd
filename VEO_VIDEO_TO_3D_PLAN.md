# Veoターンテーブル動画 → 3Dモデル化パイプライン計画

## 0. 背景・方針転換

これまでのジェネレータ(`landmark_tool.html`)は「正面/側面/背面の3枚絵+ランドマーク」から
visual hullを彫る方式だった。この方式の品質上限は構造的に決まっている:

- **断面が数式合成**: 各行のX/Z断面はスーパー楕円(`psq_*`)で「それらしく」合成しているだけで、
  斜め45度から見た形状は実測データが一切ない(`js/carving.js`の`carveSdfField`参照)。
- **凹形状が原理的に出ない**: 3方向のシルエットに現れない凹み(首まわり、髪の内側、
  スカートと脚の間)は彫れない。
- **テクスチャは3投影のハード切替**: 斜め方向の面はfront/side/backのどれかを引き伸ばして
  貼るしかなく、境目処理(`seam_*`/`color_grad_*`)はすべてこの穴埋めの妥協。

Gemini Veo(image-to-video)は、キャラクターイラスト1枚から「フィギュアが
ターンテーブルで360度回る」動画をかなりの品質で生成できる。この動画は実質的に
**任意角度のシルエット実測値+任意角度のテクスチャ実測値**の供給源であり、
上記3つの構造的限界をすべて実測データで置き換えられる。本計画はこの
「Veo動画 → 多視点再構成」パイプラインの具体設計である。**実装はまだ行わない。**

### 方針の要点

1. **形状は多視点シルエット彫刻(ソフトビジュアルハル)**。フォトグラメトリ(COLMAP等)や
   NeRF/Gaussian Splattingはサーバ/GPU学習基盤が必要でブラウザ完結の制約に反する。
   多視点シルエット彫刻は既存のボクセル+marching cubes資産の自然な拡張で、
   ブラウザのみで現実的に動く。
2. **テクスチャの主役は今後も元イラスト3面**(「イラストイメージのままキレイに出す」を維持)。
   動画フレームは (a) 形状の実測、(b) 斜め方向のテクスチャの実測、の2役に限定する。
   Veoフレームは720p〜1080p+動画圧縮なので、1024pxの元イラストより画質が低い前提で扱う。
3. **骨格・スキニング・GLB書き出し・ビューア(`character_3d.html`)は既存資産をそのまま使う**。
   中間データ契約(`rawUnified`/`partsMeta`/`pivots`/`calib`)を守る形で新パイプラインを実装し、
   `finishFromIntermediate()`以降は無改造で通す。
4. **既存3面方式は併存させる**(Veo到達性がないセッション・素材でも従来通り生成できること)。

---

## 1. 全体アーキテクチャ

```
[元イラスト front/side/back + ランドマークJSON(既存)]
        │
        ▼
[Stage 0] Veoでターンテーブル動画を生成(ツール外・手作業。プロンプトレシピ§2)
        │  turntable.mp4 (8s, 24fps, 720p/1080p)
        ▼
[Stage 1] video_tool.html(新規): フレーム抽出 (WebCodecs) + 品質フィルタ
        │  frames[N] (N=48〜96枚のcanvas)
        ▼
[Stage 2] シルエット抽出(フレームごとの2値マスク+符号付き距離場)
        ▼
[Stage 3] ターンテーブル・キャリブレーション
        │  各フレームの回転角θ_i・回転軸・スケール・上下ドリフト補正
        ▼
[Stage 4] 多視点ソフトビジュアルハル彫刻(js/multiview_hull.js 新規)
        │  共有ボクセルgrid + 投票制彫刻 + marching cubes(既存流用)
        ▼
[Stage 5] パーツ分割・骨格・スキニング(既存colormap/pivots資産を投影で再利用)
        │  rawUnified{V,F,owner} + partsMeta + pivots  ←既存の中間データ契約
        ▼
[Stage 6] テクスチャベイク: 元イラスト3面を主、Veoフレーム斜め4方位を従とする8方位アトラス
        ▼
[Stage 7] GLB書き出し・IndexedDB経由でビューアへ(既存流用)
```

---

## 2. Stage 0: Veo素材の生成レシピ(ツール外だが品質の起点)

パイプラインの入力品質はここで決まる。ツールのドキュメント(またはvideo_tool.htmlの
ヘルプパネル)として以下を整備する。

### プロンプト雛形(image-to-video、元イラストfrontを初期フレームに指定)

> The camera is completely static. The character stands perfectly still and rigid,
> like a 3D figurine on a motorized turntable, and rotates exactly 360 degrees
> clockwise at a constant speed, completing exactly one full revolution.
> Pose is frozen (no breathing, no hair or cloth movement). Plain solid white
> background, flat even studio lighting, no shadows on the background,
> full body always fully in frame.

### 受け入れチェックリスト(不合格なら再生成。複数テイク生成して最良を選ぶ)

| 項目 | 判定方法(Stage 1〜3で自動計測できるものは自動化) |
|------|------|
| ちょうど1回転している | 最終フレームと先頭フレームのシルエットIoU > 0.97(Stage 3で自動判定) |
| ポーズが固定 | 全フレームのシルエット面積の変動係数 < 3%(自動) |
| 背景が無地 | フレーム四隅パッチの色分散が閾値未満(自動) |
| 体の一部が見切れない | シルエットbboxが画面端に接するフレームが無い(自動) |
| キャラ同一性(顔・服が化けない) | 目視。正面付近フレームと元イラストの比較UIを用意 |

### 補助素材(任意、品質を上げたい場合)

- **2本目**: 背面イラストを初期フレームにした半回転動画(180°→360°)。正面起点動画の
  後半(背面側)はVeoの想像で描かれるため、背面実測との食い違いをここで補正できる。
- **顔アップのターンテーブル動画**: 頭部だけの高解像度素材。Stage 4を頭部bbox限定で
  もう1周実行し、頭部ボクセルだけ高解像度で上書きする(マイルストーンM6、後述)。

---

## 3. Stage 1: フレーム抽出(`video_tool.html` + `js/video_frames.js` 新規)

- **入力UI**: mp4ファイルのD&D + 既存ランドマークJSON(3面イラスト入り)の読み込み。
  既存`landmark_tool.html`のJSONをそのまま受け取る(ランドマーク・colormap・
  アクセサリー定義を再利用するため)。
- **デコード**: WebCodecs `VideoDecoder`を第一候補(フレーム正確・高速)。
  非対応ブラウザは`<video>.currentTime`シーク+`drawImage`のフォールバック
  (シークは±1フレーム誤差があるが、Stage 3の角度推定が吸収する)。
- **抽出数N**: 既定64(8s×24fps=192フレームから等間隔サンプル)。
  UIで32〜128を選択可。N=64で5.6°刻みの実測になる。
- **品質フィルタ(自動)**:
  - ブレ検出: グレースケールLaplacian分散が全フレーム中央値の50%未満のフレームを破棄。
  - 重複検出: 隣接フレームとの差分がほぼゼロ(動画の先頭/末尾の静止)を破棄。
- **メモリ**: 1024²RGBA×64枚≒256MB。フレームは抽出直後にStage 2のマスク+
  縮小カラー(512²)へ変換して原寸canvasは即破棄する設計にする(原寸で保持しない)。

---

## 4. Stage 2: シルエット抽出

- 無地背景前提なので、四隅パッチから背景色を推定→色距離しきい値→
  `P3D.significantComponentsMask`+`P3D.fillHoles`(既存流用)で2値マスク化。
- しきい値1本で不安な素材向けに、既存ゴーストスキャナー(`ghost_scanner.html`)で
  正面/側面/背面キーフレームだけcolormap化する運用も許す(§7参照)。
- **各フレームのマスクから符号付き距離場(SDF)を前計算する**(`js/edt.js`新規、
  Felzenszwalb-Huttenlocherの2パス法、O(W·H))。Stage 4の彫刻で「ボクセル投影点が
  シルエットの内か外か」を二値でなくサブピクセル距離で評価でき、現行の
  `subpixel_edges`と同等以上の輪郭精度が全角度で得られる。
- マスクは長辺512pxに縮小して持つ(SDF精度は縮小率で1px≒2px原寸相当。
  必要なら最終高解像度パスのみ原寸)。

---

## 5. Stage 3: ターンテーブル・キャリブレーション(`js/turntable_calib.js` 新規)

**前提モデル**: カメラ固定・被写体がY軸(鉛直)まわりに回転・弱透視(奥行きによる
スケール差を無視)。Veoの実出力は完全には従わないため、以下はすべて「推定+外れ値耐性」で設計する。

1. **スケール・上下ドリフト正規化**: 各フレームのシルエットbbox高さ(ytop/ybot)を
   基準フレームに揃える相似変換を求める(現行の`YTOP/YBOT`キャリブレーションと同じ考え方)。
   回転しても身長は不変、という拘束が使える。
2. **回転軸のX位置**: 全フレームのシルエット重心Xの平均。ターンテーブルなら重心Xは
   軸まわりに振動するのでその中心を取る。
3. **角度θ_iの初期値**: 等速仮定 θ_i = 360°·i/N。
4. **キーフレーム同定**: 元イラストfront/side/backのシルエット(ランドマークJSONから取得、
   Stage 2と同じ縮小・正規化を適用)と各フレームのシルエットIoUを総当たりし、
   IoU最大のフレームを front(θ=0)/side(θ=90)/back(θ=180) として角度曲線を区分線形補正。
   これで「等速で回っていない」テイクも大枠が合う。
5. **analysis-by-synthesis精密化**(2〜3反復):
   - 現在のθ_iで Stage 4 の粗い彫刻(低解像度grid、後述のcoarse pass)を実行。
   - 各フレームについて、ハルをθ±10°の範囲で1°刻みに投影し、実シルエットとの
     IoUが最大になるθへ更新。IoUが全フレーム中央値より大きく劣るフレームは
     **外れ値として重みを下げる**(Stage 4の投票で使う信頼度w_i)。
   - θ更新後に再彫刻。収束は速い(1回で大半が±1°に入る想定)。
6. **成果物**: `{theta[N], weight[N], axisX, scale[N], dy[N], keyframes:{front,side,back}}`。
   UIに「角度曲線グラフ+フレームごとのIoU残差」を表示し、ユーザーが不良フレームを
   手動除外できるようにする(自動化しつつ最終判断は目視、という現行ツールの流儀を踏襲)。

---

## 6. Stage 4: 多視点ソフトビジュアルハル彫刻(`js/multiview_hull.js` 新規)

### 6.1 基本アルゴリズム

- ボクセルgridは既存`P3D.buildGrid`を流用。**今回はX/Z断面も実測になるため、
  現行の「XZは粗くてよい」前提(`body_vox_xz`)は撤廃し、XYZ等方解像度に戻す。**
- 各ボクセル中心 `(x,y,z)` をフレームiへ投影:
  `u = (x−axisX)·cosθ_i + z·sinθ_i`, `v = y`(弱透視+スケール/ドリフト補正)。
  フレームiのSDFをバイリニアサンプル→ `d_i`(内側が正)。
- **ソフト化(投票制)**: 厳密なvisual hull(全フレームのmin)は、1フレームでも
  ポーズが揺れる/シルエットが欠けると正しい形状が削れて痩せる。Veo素材では
  これが必ず起きる前提で、
  `field(voxel) = weighted k-th percentile of {d_i}`(既定: 下位10%点、
  信頼度w_iで重み付け)とする。パラメータ `mv_vote_frac`(既定0.9 =
  「90%のフレームが内側と言えば内側」)。これが本計画の
  **平滑化に頼らないノイズ対処**の中核(形を均すのではなく、外れ実測を棄却する)。
- marching cubes・`dropSmallFragments`・`computeNormalsFixWinding`は既存流用。

### 6.2 計算量と実装方針

- 目安: grid 200×400×200 = 16Mボクセル × 64フレーム ≒ 10億サンプル。
  素朴なJSでは数十秒〜数分になるため:
  1. **coarse-to-fine**: まず1/4解像度(50×100×50)で全ボクセル彫刻→表面から
     ±2セルのnarrow bandだけを本解像度で再評価。評価ボクセル数が1〜2桁減る。
  2. **WebWorker化**: フレーム方向で分割して並列評価(SDFはTransferableな
     Float32Arrayなのでworkerへゼロコピーで渡せる)。メインスレッドの
     `await tick()`方式(現行)より応答性も上がる。
  3. cos/sinはフレームごとに前計算、投影はY行単位でインクリメンタル計算。
- 既存carveRegionとの関係: **体本体の彫刻はcarveRegion(行スキャン+スーパー楕円)を
  使わない**。carveRegionが担っていた腕の円形断面・手の押し出し(`arm_circle`/
  `hand_extrude`)も、多視点実測が同じ情報を含むため不要になる(横に伸ばした腕は
  回転中に上下から見えないが、64方位の側方シルエットで断面が十分拘束される)。

### 6.3 凹形状の扱い(限界の明示)

多視点シルエットでも「どの角度のシルエットにも現れない凹み」(へそ、鎖骨の窪み等)は
彫れない。これはVeo動画の陰影から推定する将来課題(shape-from-shading)とし、
本計画のスコープ外。既存の`face_sculpt`(目窩の凹み)はStage 4の後処理として
そのまま適用可能なので維持する。

---

## 7. Stage 5: パーツ分割・骨格・スキニング(既存資産の再利用)

- **骨格pivots**: ランドマークJSONの点(front画像px座標)を使う既存の
  `buildDerivedLandmarks`→`computePivots`をそのまま流用。front画像座標系と
  動画フレーム座標系のズレは、Stage 3のキーフレーム同定で得た相似変換で吸収する。
- **パーツ(アクセサリー)ownership**: 動画の全フレームをcolormap化するのは非現実的
  なので、**既存の3面colormapを最終メッシュへ投影してownerを付与する**:
  各頂点を法線方向が最も正対する正準ビュー(front/side/back)へ投影し、
  colormap最近傍色分類(`P3D.classifySilhouetteRaw`と同じ色候補)でownerIdを決定。
  その後、メッシュ隣接での多数決平滑化を数回かけて飛び地を除去する。
  境界の厳密さは現行方式(ボクセル所有権)より落ちるが、現行方式が担保していた
  「パーツ間の隙間ゼロ」は統合彫刻(1枚のメッシュ)側で満たされているため、
  ownerはスキニング・アトラスの分類にしか使われず要求精度が低い。
- **rigid/bones指定・スキニング**: `partsMeta`の形式は現行と同一。
  `nearestBoneSegmentSkin`/`rigidSkin`は無改造で流用。
- これにより`rawUnified{V,F,owner} + partsMeta + pivots + calib`という既存の
  中間データ契約が満たされ、`finishFromIntermediate()`(間引き・スキニング・GLB)へ
  そのまま接続できる。**bone_bridge/part_gap_closeはこのパイプラインでは不要**
  (全パーツを最初から1つの実測fieldとして彫るため、パーツ間の空隙が発生しない)。

---

## 8. Stage 6: テクスチャベイク(8方位アトラス、`js/atlas.js`拡張)

### 8.1 設計

- 現行アトラスはfront/back/sideの3タイル+法線ベースの領域ハード判定。これを
  **8方位**(front, back, side右, side左, 斜め前右45°, 斜め前左315°, 斜め後右135°,
  斜め後左225°)へ一般化する:
  - **正面/背面/側面タイル: 元イラスト**(bleed済み、現行と同じ)。最高画質を維持し
    「イラストのまま」の見た目を守る。
  - **斜め4タイル: Veoフレーム**(Stage 3で同定したθ≒45/135/225/315°のフレーム。
    ブレの少ない近傍フレームを選ぶ)。
- 領域判定は現行の`sideS`/`frontS`2スカラー場方式を「法線と8方位ベクトルの内積の
  argmax+境界のメッシュ平滑化」へ一般化する(`seamSmoothIters`の考え方は維持)。
  タイルごとの`seamNoSide`相当(「このパーツは斜めタイルを使わない」)も
  パーツ単位フラグとして残す。
- アトラスcanvasは3W×H→4W×2H(8タイル)。`buildAtlasCanvas`と
  `stageAtlasBake`のUV式をタイル表引きに一般化。UVクランプ(回り込み虹色縞対策)は
  タイル内クランプとして踏襲。

### 8.2 色合わせ(Veoフレームと元イラストの色ズレ対策)

- Veo出力は照明・トーンが元イラストと微妙に違う。斜めタイルに使うフレームは、
  **正面キーフレーム vs 元イラストfront のペアから求めたチャンネル別トーンカーブ
  (ヒストグラムマッチング)を適用してから**アトラスに貼る。
  補正はシルエット内ピクセルのみで推定する。
- タイル間の残差は既存`colorGradWidth/Strength`(境目の実カラーブレンド)を
  8方位の隣接タイルペアへ一般化して吸収する。

---

## 9. 新規・変更ファイル一覧

| ファイル | 新規/変更 | 内容 |
|---|---|---|
| `video_tool.html` | 新規 | 動画+既存JSONの読み込みUI、フレーム/角度/残差の検証UI、生成実行。IIFE単一スコープ・素`<script>`読み込みの現行流儀に従う |
| `js/video_frames.js` | 新規 | WebCodecs抽出+`<video>`フォールバック、ブレ/重複フィルタ |
| `js/edt.js` | 新規 | 2値マスク→符号付き距離場(Felzenszwalb 2パス) |
| `js/turntable_calib.js` | 新規 | スケール/軸/角度推定、キーフレーム同定、analysis-by-synthesis反復、信頼度w_i |
| `js/multiview_hull.js` | 新規 | 投票制多視点彫刻(coarse-to-fine、Worker並列)、`face_sculpt`後処理の呼び出し |
| `js/multiview_atlas.js` | 新規 | 8方位領域判定+UV生成(atlas.jsの`stageAtlasBake`を一般化。既存3面パスは無改造で残す) |
| `js/pipeline.js` | 変更 | `runToIntermediateFromVideo(state)`を追加(中間データ契約は既存と同一形)。既存`runToIntermediate`は不変 |
| `js/common.js` | 変更 | 新gen_params既定値、ヒストグラムマッチング等の小物ユーティリティ |
| `character_3d.html` | 変更 | 中間データに`atlasLayout:'3view'|'8view'`フラグを追加し、`finishFromIntermediate`のatlas段だけ分岐 |
| `tests/video-pipeline.test.js` | 新規 | §11参照 |

**触らないもの**: `js/carving.js`(既存3面方式用として現状維持)、`js/skeleton.js`、
`js/marching_cubes.js`(ownerField引数は流用)、`js/model_export.js`、`controller.html`。

---

## 10. 新規gen_params(案)

| キー | 既定値 | 意味 |
|---|---|---|
| `mv_frames` | 64 | 使用フレーム数 |
| `mv_vote_frac` | 0.9 | 投票制彫刻の採択率(1.0=厳密hull) |
| `mv_vox` | 0.0034 | ボクセルサイズ(XYZ等方) |
| `mv_mask_res` | 512 | シルエット/SDFの作業解像度 |
| `mv_angle_refine_iters` | 2 | analysis-by-synthesis反復回数 |
| `mv_outlier_iou` | 0.85 | フレーム外れ値判定のIoU下限 |
| `mv_diag_tiles` | true | 斜め4タイルをVeoフレームから貼るか(falseで現行3面アトラス互換) |
| `mv_tone_match` | true | Veoフレームのトーンカーブ補正 |

現行の`psq_*`、`arm_*`、`hand_*`、`track_*`、`depth_gap_close_px`、`bone_bridge`、
`part_gap_close`は本パイプラインでは**不要**(実測とソフト投票が代替)。
パラメータ数が28個→実質8個程度に減ることも副次的な利点。

---

## 11. テスト計画(`tests/video-pipeline.test.js`)

1. **合成動画によるゴールデンテスト**: three.jsで既知メッシュ(現行サンプルGLB)を
   実際にターンテーブル・レンダリングした合成フレーム列をfixtureとして同梱し、
   (a)角度推定の誤差 < 2°、(b)再構成メッシュと元メッシュのハウスドルフ距離が閾値未満、
   (c)生成GLBのSHA-256ゴールデン比較、を検証する。Veo実素材はネットワーク・
   再現性の問題があるためリポジトリには固定fixtureのみ置く。
2. **外れ値耐性テスト**: 合成フレームの10%に意図的なポーズ揺れ/欠けを入れ、
   `mv_vote_frac=0.9`で形状誤差が悪化しないこと。
3. **UIテスト**: 既存`generator-ui.test.js`と同様に、動画読み込み→フレーム表示→
   生成→IndexedDB引き継ぎのPlaywright回帰。

---

## 12. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| Veoがポーズ・キャラを回転中に変えてしまう | 形状が痩せる/化ける | 投票制彫刻(§6.1)+フレーム信頼度(§5-5)+受け入れチェックリスト(§2)。最悪、不良区間をUIで手動除外 |
| 回転が等速でない/1回転しない | 角度推定の破綻 | キーフレームIoU同定+区分線形補正(§5-4)。1回転未満は「欠測角度帯」として投票母数から除外(その帯は現行同様の補間形状になる) |
| 透視歪み(弱透視仮定とのズレ) | 上下端で幅がズレる | Veoプロンプトで望遠気味を指示。残差はスケール正規化がY方向は吸収。将来課題として焦点距離1パラメータ推定を挙げておく |
| WebCodecs非対応環境 | 抽出不可 | `<video>`シークのフォールバック(精度低下は角度推定が吸収) |
| 計算時間(10億サンプル) | UX悪化 | coarse-to-fine+Worker並列(§6.2)。目標: 64フレーム・16Mボクセルで30秒以内 |
| Veoフレームの画質(720p+圧縮ノイズ) | 斜めタイルだけ甘くなる | 正準3面は元イラスト維持(§8.1)。`mv_diag_tiles=false`で完全に現行アトラスへ退避可能 |
| メモリ(フレーム保持) | タブクラッシュ | 原寸フレームは即時縮小・破棄(§3)。斜めタイル用の4枚だけ原寸保持 |

---

## 13. マイルストーン(各段階で動くものを確認してから次へ)

| M | 内容 | 受け入れ基準 |
|---|---|---|
| M1 | `video_tool.html`+フレーム抽出+シルエット/SDF+**キャリブレーション可視化**(彫刻なし) | 実Veo素材で角度曲線とIoU残差が表示され、キーフレーム同定が目視で正しい |
| M2 | 合成動画fixture+多視点彫刻(テクスチャなしGLB) | 合成テストで形状誤差が閾値内。実Veo素材で斜め45°の形状が現行3面方式より明確に良い(スクショ比較) |
| M3 | パーツownership投影+スキニング統合(中間データ契約接続) | ビューアで既存モーションが再生できる |
| M4 | 8方位アトラス+トーン補正 | 斜め45°ビューで現行の「引き伸ばしテクスチャ」が消える。正面ビューは現行と同一(元イラスト画質維持) |
| M5 | パラメータUI・IndexedDB引き継ぎ・Playwright回帰一式 | `npm test`全PASS |
| M6(任意) | 顔アップ動画による頭部高解像度上書き(§2補助素材) | 顔の造形・テクスチャ密度が全身素材比で向上 |

---

## 14. 既存3面方式との関係(併存方針)

- `landmark_tool.html`(3面方式)は削除も凍結もしない。Veo素材が用意できない場合の
  正規ルートとして残す。
- ランドマークJSONは両方式の共通入力(video_tool.htmlは動画を追加で受け取るだけ)。
- ビューア・コントローラーは中間データ契約とGLBしか見ないため、どちらの方式で
  生成したかを意識しない(atlasLayoutフラグのみ)。
- 3面方式の今後のブラッシュアップ投資は、本パイプラインのM2(形状品質の実証)の結果を
  見てから判断する(M2で期待通りなら、形状系の3面方式改善は打ち切ってよい)。
