# Veoターンテーブル動画 → 3Dモデル化パイプライン計画(v2)

## 0. 背景・v1からの方針転換

v1計画は「多視点シルエット彫刻+8方位タイルアトラス」だったが、**テクスチャが結局
8方位の離散ビューに落ちており、それなら静止画生成(nanobanana等)で8枚作るのと変わらない**
という指摘を受けて全面改訂した。v2の設計原則:

> **動画の価値は「ビューが密で、時間的に連続している」こと。
> 全フレーム・全ピクセルを幾何とテクスチャの両方に使い切る。**

具体的には:

- **形状**: 全Nフレーム(48〜96枚、2〜7°刻み)のシルエットを使う投票制彫刻(v1から継続)。
  さらに、フレームの**ピクセル内容**(色の多視点整合性)で凹形状を彫る精密化パスを追加する。
- **テクスチャ**: 「どのビューのタイルを貼るか」を選ぶ方式を全廃し、
  **テクセル単位で全フレームを可視性・法線適合度で加重合成するベイク方式**にする。
  - 継ぎ目が**構造的に発生しない**(同じ表面点は常に同じブレンド結果になるため、
    現行の`seam_*`/`color_grad_*`系の妥協パラメータ群が丸ごと不要になる)。
  - 隣接フレームは同じ表面をほぼ同じ角度から映しているので、多数フレームの加重平均が
    動画圧縮ノイズ・フリッカーを打ち消す=**時間的冗長性がそのまま画質(実効SNR・
    実効解像度)になる**。これが静止画8枚に対する動画の決定的な優位点。
- 制約は現行どおり: **ブラウザ完結・サーバなし・ビルドなし**。重い処理はWebGL/WebGPUと
  WebWorkerで賄う。**出力はボーン入りGLB必須**(既存ビューア/コントローラーで動くこと)。
- 見た目の基準は「元イラスト再現」に固定しない。元イラスト3面は「信頼度の高い3フレーム」
  として同じ加重合成の枠組みに参加させる(§8.4)。

フォトグラメトリ(COLMAP)・NeRF・3D Gaussian Splattingは、学習/最適化基盤か
メッシュ化(+リギング)の後工程がブラウザ完結制約に反するため引き続き不採用。
ただし§7の photo-consistency 彫刻は space carving 系の考え方であり、
「ピクセル内容から幾何を推定する」方向の実測はスコープに含める。

---

## 1. 全体アーキテクチャ

```
[元イラスト front/side/back + ランドマークJSON(既存)]
        │
        ▼
[Stage 0] Veoでターンテーブル動画を生成(プロンプトレシピ§2)
        │  turntable.mp4 (8s, 24fps, 720p/1080p)
        ▼
[Stage 1] video_tool.html(新規): フレーム抽出(WebCodecs)+品質フィルタ
        ▼
[Stage 2] シルエット抽出+フレームごとの符号付き距離場(SDF)
        ▼
[Stage 3] ターンテーブル・キャリブレーション(角度θ_i・信頼度w_i・軸・スケール)
        ▼
[Stage 4] 多視点ソフトビジュアルハル彫刻(全フレーム投票制)
        ▼
[Stage 5] photo-consistency精密化(凹形状。narrow band限定、実験パス)
        ▼
[Stage 6] パーツ分割・骨格・スキニング(既存資産を投影で再利用)
        │  rawUnified{V,F,owner} + partsMeta + pivots ←既存の中間データ契約
        ▼
[Stage 7] チャートUV展開+ジオメトリイメージ生成
        ▼
[Stage 8] 全フレーム・テクセル単位加重合成ベイク(WebGLで1フレーム=1パス)
        ▼
[Stage 9] GLB書き出し・IndexedDB経由でビューアへ(既存流用)
```

Stage 0〜4はv1と同じ(§2〜6に再掲・要点のみ)。v2の中核はStage 5/7/8。

---

## 2. Stage 0: Veo素材の生成レシピ

v1から変更なし。要点:

- image-to-video、元イラストfrontを初期フレームに。「カメラ固定・フィギュアが
  ターンテーブルで等速にちょうど360°・ポーズ完全固定・無地背景・フラット照明・全身常時フレームイン」。
- 受け入れチェック(自動化可能なもの): 先頭/末尾シルエットIoU>0.97(1回転完了)、
  シルエット面積変動係数<3%(ポーズ固定)、四隅パッチ色分散(無地背景)、bbox画面端接触なし。
- キャラ同一性(顔・服が化けない)は目視。複数テイクから最良を選ぶ運用。
- 補助素材(任意): 背面起点の半回転動画、顔アップのターンテーブル動画(§10 M6)。

**v2で追記**: テクスチャを全フレーム合成する設計になったため、「照明が完全に一定」の
重要度が上がった。プロンプトに "no rim light, no moving highlights, ambient
occlusion-free flat lighting" を加え、回転につれてハイライトが動く素材は
チェックリストで弾く(動くハイライトは加重平均でボケ・色ムラになる。
ロバスト合成での緩和は§8.3)。

---

## 3. Stage 1: フレーム抽出(`video_tool.html` + `js/video_frames.js` 新規)

v1から変更なし(WebCodecs第一候補+`<video>`シークフォールバック、N=48〜96、
ブレ/重複の自動除外)。

**v2で変更**: v1は「原寸フレームは即破棄、縮小のみ保持」だったが、v2は全フレームが
テクスチャ源になるため**全フレーム原寸保持が必須**。メモリ対策として、抽出直後に
各フレームをWebGLテクスチャ(ImageBitmap経由)へ載せ、JS側のcanvas/配列は保持しない。
1024²RGBA×96枚≒384MBはGPUメモリとしては現実的な範囲。加えて、シルエット外を
黒塗りした上で各フレームをbboxクロップしてから載せる(キャラが画面の一部しか
占めない素材でGPUメモリを大幅節約)。

---

## 4. Stage 2〜3: シルエット・キャリブレーション

v1から変更なし。要点のみ:

- シルエット: 四隅パッチから背景色推定→色距離→`significantComponentsMask`+`fillHoles`
  (既存流用)。フレームごとにEDT(Felzenszwalb 2パス、`js/edt.js`新規)で符号付き距離場化。
- キャリブレーション(`js/turntable_calib.js`新規): 弱透視+Y軸回転モデル。
  bbox高さでスケール/上下ドリフト正規化 → 重心Xで回転軸 → 等速仮定を初期値に、
  元イラスト3面とのシルエットIoUでキーフレーム(θ=0/90/180)を同定して区分線形補正 →
  粗彫刻→再投影IoU最大化のanalysis-by-synthesisでθ_iを±1°级へ精密化(2〜3反復)。
  IoU残差の大きいフレームは信頼度w_iを下げる(以降の全Stageで共通に使う重み)。
- UI: 角度曲線+フレーム別残差グラフ、不良フレームの手動除外。

---

## 5. Stage 4: 多視点ソフトビジュアルハル彫刻(`js/multiview_hull.js` 新規)

v1から変更なし。要点のみ:

- 共有ボクセルgrid(`P3D.buildGrid`流用、XYZ等方解像度)。各ボクセルを全フレームへ
  弱透視投影し、SDFをバイリニアサンプル。
- **投票制**: `field = 重み付き下位quantile(d_i)`(既定: `mv_vote_frac=0.9`)。
  厳密hull(min)はポーズ揺れ1フレームで形が痩せるため、外れ実測の棄却で対処する
  (形を均す平滑化ではない)。
- coarse-to-fine(1/4解像度→表面narrow bandのみ本解像度)+Worker並列。
  marching cubes・断片除去・法線/巻き補正は既存流用。
- 現行carveRegionの腕円形断面・手押し出し・スーパー楕円断面(`psq_*`)は全廃
  (全角度の実測が代替)。

---

## 6. Stage 5: photo-consistency精密化(凹形状、実験パス)

シルエットに現れない凹み(首まわりの窪み、髪の内側、スカート内側の脚との隙間)を、
**フレームのピクセル内容**から彫る。space carvingの古典的アイデアをnarrow band限定で行う:

1. Stage 4のハル表面から内側へ数ボクセル(`mv_pc_band`、既定4)のnarrow bandを対象にする。
2. 各表面ボクセルについて、そのボクセルが可視なフレーム集合(深度マップ§8.2で判定)から
   色をサンプルし、**重み付き色分散**を計算する。
3. 真の表面なら全フレームでほぼ同色(ランバート面+固定照明の仮定が§2で担保される)。
   分散が閾値`mv_pc_var_thr`を超えるボクセルは「実は表面ではない(=手前の別の面と
   奥の面を混同している)」として削り、露出した新表面で再評価する(数反復)。
4. 削りすぎ防止ガード: シルエットSDF由来のhullは**外側の上限**として常に維持
   (photo-consistencyは削る方向にしか働かせない)。1反復あたりの削り量も1ボクセルに制限。

- アニメ調キャラは平坦色領域が広く、平坦色領域では色分散が判別力を持たない
  (どこでも整合してしまう)。**エッジ・テクスチャのある場所だけが凹みの証拠になる**ため、
  フレームの局所勾配強度を信頼度に織り込む。効果は素材依存が大きいので、
  既定OFFの実験機能としてUIトグル(`mv_photo_consistency`)にする。
- ここが期待通り動けば「visual hullの原理的限界」を超える。動かなくても
  Stage 4までの品質は損なわれない(独立パス)。

---

## 7. Stage 6: パーツ分割・骨格・スキニング

v1から変更なし。要点のみ:

- pivots: ランドマークJSON→`buildDerivedLandmarks`→`computePivots`を流用。
  イラスト座標系と動画座標系のズレはキーフレーム相似変換で吸収。
- owner: 既存3面colormapを最終メッシュへ法線最適ビューで投影して最近傍色分類→
  メッシュ隣接多数決で飛び地除去。ownerはスキニングと後述チャート分割の
  ヒントにしか使われないため、この精度で足りる。
- `nearestBoneSegmentSkin`/`rigidSkin`/`finishFromIntermediate`以降は無改造。
  bone_bridge/part_gap_closeは不要(最初から1つの実測fieldなので空隙が出ない)。

---

## 8. Stage 7〜8: 全フレーム・テクセル単位ベイク(v2の中核)

### 8.1 チャートUV展開(`js/uv_charts.js` 新規)

ビュー由来のタイルではなく、**メッシュ固有のUVアトラス**を作る(内容とレイアウトの分離)。
ビルドなし制約下でxatlasは使えないため、簡易チャート方式を自前実装する:

1. 面を「owner(パーツ)× 法線の主方向(6方位クラスタリング)」でシード分割し、
   隣接面の法線が閾値内なら同チャートへ貪欲併合(チャート内の法線円錐角<60°を保つ)。
2. 各チャートを平均法線に垂直な平面へ投影して2D化(歪みは法線円錐角制限で抑制)。
3. 2D bboxを面積降順にシェルフ詰めでアトラスへパック(テクセル密度をチャート間で統一、
   境界に4pxパディング)。
- 出力アトラス解像度は既定2048²(§8.3の多フレーム合成が720p素材からでも
  この密度を意味のあるものにする)。

### 8.2 ジオメトリイメージと深度マップ

- **ジオメトリイメージ**: アトラスの各テクセルに対応する表面位置(x,y,z)と法線(nx,ny,nz)を
  floatテクスチャへラスタライズ(チャートをUV空間で描画するだけ。WebGLの
  `EXT_color_buffer_float`使用)。以降のベイクはこの2枚を入力とする画像処理になる。
- **可視性(深度マップ)**: 各フレームiのカメラ(弱透視=three.jsのOrthographicCamera+
  Stage 3のθ_i/スケール/ドリフト)からメッシュを深度レンダリング(N回、GPUなので数秒)。
  テクセルの表面点をフレームiへ投影し、深度マップと比較して自己遮蔽を判定する。

### 8.3 加重合成ベイク(1フレーム=1レンダーパス)

全テクセルについて、**可視な全フレームの色を重み付き合成**する。WebGLの加算ブレンドで
「重み付き色の累積バッファ」と「重みの累積バッファ」を作り、最後に除算する設計
(フラグメントシェーダ2本+Nパス。JSループ不要でN=96でも数秒):

```
パスi(フレームiについて):
  入力: ジオメトリイメージ(位置・法線)、フレームiのテクスチャ、深度マップi、θ_i、w_i
  各テクセル:
    p, n ← ジオメトリイメージ
    uv_i ← pをフレームiへ投影(弱透視)
    可視でなければ discard(深度比較+シルエットSDF>マージン)
    w = w_i                        … フレーム信頼度(Stage 3)
        × max(0, n·view_i)^k       … 法線適合度(既定k=4。斜めから見た面は解像度が
                                      落ちているので急峻に減衰させる)
        × sharpness_i(uv_i)        … 局所ブレ(局所Laplacian分散、前計算)
        × edgeFade(SDF_i(uv_i))    … シルエット際は背景が混入するため減衰
    accumColor += w * frame_i(uv_i);  accumWeight += w
最終パス: atlas = accumColor / accumWeight
```

- **継ぎ目レス**: チャート境界の両側は同じ表面点→同じ重み関数→同じ色に収束するため、
  ビュー切替方式で必要だった`seamAngles`/`seamNoSide`/`seamSmoothIters`/
  `colorGradWidth`/`colorGradStrength`は**全部不要になる**。
- **実効超解像**: 表面上の1テクセルは数十フレームから独立サンプルされる。バイリニア+
  サブピクセル投影のずれが自然なマルチサンプルになり、720p素材でも2048アトラスの
  密度が埋まる(単純平均でボケる分は、重みのk乗急峻化と後述ロバスト化で抑える)。
- **ロバスト化(ハイライト・ゴースト対策)**: 平均の代わりに「重み付き平均から
  外れたサンプルの重みを下げて再平均」(1回の再重み付け、Tukey型)をオプションで
  用意する(`mv_bake_robust`)。GPU実装は累積バッファをもう1往復するだけ。
  Veoが回転中にハイライトを動かしてしまった素材の救済用。
- **穴埋め**: どのフレームからも可視でないテクセル(深い凹み・股下等)は、
  ベイク後にアトラス上でpull-push補間(近傍有効テクセルの拡散)。現行bleedEdgesの
  役割はこれが引き継ぐ。

### 8.4 元イラストの参加(見た目の最適化)

元イラスト3面を「θ=0/90/180の追加フレーム」として同じ枠組みに入れる。
重み係数`illust_weight`(既定2.0程度)で動画フレームより優遇する。

- `illust_weight`を大きくすると正面・側面・背面の見た目が元イラストに寄り、
  0にするとVeoの質感に寄る。**「イラスト再現⇔Veo再現」が連続的なスライダー1本になる**
  (ユーザー回答「こだわらない」に対する設計: どちらにも振れる構造にして既定値で最良を狙う)。
- 実装上は特別扱い不要(フレームリストに3枚足すだけ)。ただし色調のベースは
  動画側に合わせるため、逆方向のヒストグラムマッチング(イラスト→動画正面キーフレーム)を
  事前適用するオプションを持つ(`illust_tone_match`)。

### 8.5 GLB接続

- `buildGLB`は無改造(UV+単一テクスチャという契約は同じ)。アトラスPNG/JPEG圧縮は
  既存`compressAtlas`流用。
- `finishFromIntermediate`のatlas_bake段のみ、`atlasLayout:'charts'`のとき
  §8のパスへ分岐(既存3面パスは温存)。ライブパラメータ編集(Tier3)は
  `illust_weight`/`k`/`mv_bake_robust`の再ベイクに対応(GPUベイクは数秒なので
  現行のTier3再計算と同等の応答性が出る)。
- **間引き(decimate)との順序**: ジオメトリイメージは間引き・平滑化・スキニング確定後の
  最終メッシュから作る(UVを先に振ると間引きで壊れるため)。段階キャッシュの依存関係は
  decimate → mesh_finish → **uv_charts → bake** → model_glb となる。

---

## 9. 新規・変更ファイル一覧

| ファイル | 新規/変更 | 内容 |
|---|---|---|
| `video_tool.html` | 新規 | 動画+既存JSON読み込み、キャリブレーション検証UI、生成実行(現行流儀: 単一IIFE・素`<script>`) |
| `js/video_frames.js` | 新規 | WebCodecs抽出+フォールバック、ブレ/重複除外、フレーム→GPUテクスチャ管理 |
| `js/edt.js` | 新規 | 2値マスク→符号付き距離場 |
| `js/turntable_calib.js` | 新規 | 軸/スケール/角度推定、キーフレーム同定、analysis-by-synthesis、信頼度w_i |
| `js/multiview_hull.js` | 新規 | 投票制彫刻(coarse-to-fine+Worker)、photo-consistency精密化(§6) |
| `js/uv_charts.js` | 新規 | チャート分割・平面投影・パッキング・ジオメトリイメージ生成 |
| `js/gpu_bake.js` | 新規 | 深度マップN枚レンダリング、加重合成ベイク(WebGL、インラインGLSL)、pull-push穴埋め |
| `js/pipeline.js` | 変更 | `runToIntermediateFromVideo`追加+`finishFromIntermediate`のatlas段分岐(既存パス不変) |
| `js/common.js` | 変更 | 新gen_params既定値、ヒストグラムマッチング |
| `character_3d.html` | 変更 | `atlasLayout`分岐とベイク系Tier3パラメータUI |
| `tests/video-pipeline.test.js` | 新規 | §11 |

**触らない**: `js/carving.js`、`js/atlas.js`(3面方式用に現状維持)、`js/skeleton.js`、
`js/marching_cubes.js`、`js/model_export.js`、`controller.html`。

---

## 10. 新規gen_params(案)

| キー | 既定値 | 意味 |
|---|---|---|
| `mv_frames` | 64 | 使用フレーム数 |
| `mv_vote_frac` | 0.9 | 投票制彫刻の採択率(1.0=厳密hull) |
| `mv_vox` | 0.0034 | ボクセルサイズ(XYZ等方) |
| `mv_photo_consistency` | false | §6の凹形状精密化(実験) |
| `mv_pc_band` / `mv_pc_var_thr` | 4 / 素材依存 | photo-consistencyの帯幅/分散閾値 |
| `mv_atlas_res` | 2048 | ベイクアトラス解像度 |
| `mv_bake_k` | 4 | 法線適合度の指数(大=正対ビュー重視でシャープ、小=多フレーム平均でノイズ減) |
| `mv_bake_robust` | true | 外れサンプル再重み付け |
| `illust_weight` | 2.0 | 元イラスト3面の重み(0=Veo質感のみ) |
| `illust_tone_match` | true | イラスト→動画のトーン合わせ |

現行の`psq_*`/`arm_*`/`hand_*`/`track_*`/`depth_gap_close_px`/`bone_bridge`/
`part_gap_close`/`seam_*`/`color_grad_*`は本パイプラインではすべて不要。

---

## 11. テスト計画(`tests/video-pipeline.test.js`)

1. **合成動画ゴールデン**: three.jsで既知GLBをターンテーブル・レンダリングした合成
   フレーム列をfixture化。(a)角度推定誤差<2°、(b)再構成メッシュのハウスドルフ距離閾値、
   (c)**ベイクテクスチャの再投影誤差**(生成GLBを同じ角度でレンダリング→合成フレームとの
   PSNR閾値。テクセル合成の劣化検知)、(d)GLBのSHA-256ゴールデン。
2. **外れ値耐性**: フレームの10%にポーズ揺れ/ハイライト移動を注入し、
   投票彫刻とロバストベイクで品質が閾値内に留まること。
3. **UI回帰**: 動画読み込み→検証UI→生成→IndexedDB引き継ぎ(Playwright)。

---

## 12. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| Veoがポーズ/キャラを回転中に変える | 形状痩せ・テクスチャゴースト | 投票制彫刻+フレーム信頼度+ロバストベイク(§8.3)。不良区間の手動除外UI |
| 照明・ハイライトが回転中に動く | ベイクの色ムラ・ボケ | プロンプトで固定照明を強制(§2)+ロバスト再重み付け。チェックリストで不合格素材を弾く |
| 多フレーム平均によるボケ | シャープさ低下 | 法線適合度k乗の急峻化+sharpness重み+ロバスト化。`illust_weight`で正対3面は原画優位 |
| photo-consistencyがアニメ調平坦色で無力 | 凹形状が出ない | 既定OFFの実験パスとして隔離(§6)。効かなくても他Stageに影響なし |
| チャートUVの歪み・パッキング効率 | テクセル密度ムラ | 法線円錐角制限+密度統一パッキング。品質不足ならチャート分割閾値を締める |
| float render target等のWebGL拡張非対応 | ベイク不可 | `EXT_color_buffer_float`非対応時はハーフフロート→8bit多パスの縮退実装。WebGPU対応は将来課題 |
| GPUメモリ(原寸フレームN枚) | モバイル等でクラッシュ | bboxクロップ+シルエット外黒塗り(§3)。`mv_frames`削減の自動フォールバック |
| 弱透視仮定とのズレ | 上下端の投影ズレ→ベイクの二重写り | 望遠プロンプト指示+ロバストベイクが軽微なズレを吸収。残差が大きければ焦点距離1パラメータ推定を追加 |

---

## 13. マイルストーン

| M | 内容 | 受け入れ基準 |
|---|---|---|
| M1 | フレーム抽出+シルエット/SDF+キャリブレーション可視化 | 実Veo素材で角度曲線・残差表示、キーフレーム同定が目視で正しい |
| M2 | 投票制彫刻(頂点色の仮テクスチャでプレビュー) | 合成テストで形状誤差閾値内。斜め45°形状が現行3面方式より明確に良い |
| M3 | チャートUV+GPUベイク(ここがv2の本丸。骨格より先に検証する) | 合成テストの再投影PSNR閾値。実素材で継ぎ目・色ムラが目視ゼロ。`illust_weight`スライダーが機能 |
| M4 | パーツownership+スキニング統合(中間データ契約接続) | ビューアで既存モーション再生 |
| M5 | パラメータUI・IndexedDB・Playwright回帰一式 | `npm test`全PASS |
| M6(任意) | photo-consistency精密化(§6)/顔アップ動画の頭部高解像度上書き | 首まわり等の凹みが出る/顔の密度向上(効果を実素材でA/B) |

v1との最大の違いはM3の位置づけ。v1は「アトラス拡張」が終盤だったが、v2は
テクセルベイクこそが動画方式の存在意義なので、骨格統合より先に単体検証する。

---

## 14. 既存3面方式との関係

- `landmark_tool.html`(3面方式)は併存(Veo素材が無い場合の正規ルート)。
- ランドマークJSONは共通入力。ビューアは`atlasLayout`フラグ以外どちらの方式かを意識しない。
- v2のテクセルベイク(Stage 7〜8)は入力が「キャリブレーション済みビュー集合」で
  あればよいので、**将来的に3面方式のアトラスをこの方式で置き換える**(3ビューだけの
  加重合成ベイク)ことも可能。seam系パラメータ群の廃止はその時点で3面方式にも波及できる。
