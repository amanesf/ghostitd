# ゴーストコントローラー 神社マップ 大幅リクオリティ設計書

## Context

`controller.html`の神社は当初(第0弾)、本殿・狛犬がプリミティブ/ミスマッチなフリーモデル(西洋神殿風・城の尖塔風)で構成され、鳥居の向き異常・物量不足・procedural fallbackの残存といった問題を抱えていた。本ドキュメントはそれらを解消しつつ神社全体を再設計するための計画・調査記録。

## 現状ステータス(2026-07-07時点)

- **マップ分離アーキテクチャ: 実装済み**。`maps/manifest.js`(マップ一覧)+`maps/shrine.js`(`GhostMaps.shrine.build(env)`)に神社固有コードを分離。`controller.html`は汎用ホスト(レンダラー/シーン/キャラクター/カメラ/config)のみを持ち、`colliders`/`FREE_MODEL_CREDITS`/`loadStaticGLB`等をenv経由でマップに渡す。
- **本殿/手水舎: つっちーモデルを採用・軽量化済み**。`models/honden_free.glb`(1.1MB)/`models/chouzuya_free.glb`(600KB、いずれも`gltf-transform`のweld/simplify/prune/quantizeで軽量化済み、欠損なし確認済み)。本殿の前後反転バグ(`rotation.y=Math.PI`で修正)、手水舎の支柱欠落(OBJ版では実は最初から存在、誤診断だった)は解消済み。
- **第11弾(アイデア先行・密度重視の再構築): 実装済み**。駐車場ゾーン(アスファルト+白線、放置車・自販機・社号標・常夜灯)、坂で一気に上る参道(プロシージャル明神鳥居2基のみ)、境内広場化、本殿ゾーン、奥の院(磐座・竹林)まで`maps/shrine.js`に反映済み。鳥居はsugamo/Jacques Fourie等のフリーモデルを全廃し、専用のプロシージャル生成関数(`buildToriiProcedural`)に置き換え済み。

## 決定事項サマリ(調査の結論、参照用)

- **本殿建築の教訓**: 神社本殿には塔状建築(五重塔等、寺院建築)は使えない。平屋・切妻/流造・横長シルエットが必須(Pagoda案は撤回済み)。
- **賽銭箱**: フリーモデルの"Chest"系は全て海賊の宝箱型で使えないため、置かない方針が確定。
- **しめ縄・御神木**: 紙垂(白いジグザグ紙垂れ)が無いと境界として機能しない→薄い平面を等間隔にぶら下げる方式。御神木の縄は根元ではなく幹の胸の高さに巻く。
- **手水舎**: 水盤だけでなく屋根付きが正式。鳥居ゲート直後(本殿より手前)に配置するのが作法。
- **BOOTH/ニコニ立体**: 無料モデルでもアカウント登録必須でこのセッションではダウンロード不可。itch.io「Mini Japan」(Ely Dev)はライセンス不明のため未採用。最終的にuser提供の「つっちー」モデル(商用/改変可・再配布不可、ライセンス確認済み)を採用。

## フリーモデルを効率的に探す方法(今後のセッション用メモ)

1. 主戦場はpoly.pizza(ログイン不要、CC0/CC-BY明記、`.glb`直リンクをHTMLから`grep`で抽出可能)。ネットワークポリシーでブロックされる場合はWebSearchで当たりを付ける。
2. ファイルサイズは`curl -sSI`のcontent-lengthで事前確認。
3. 見た目は必ずthree.jsプレビューで目視確認する(タイトル・説明文だけで判断しない)。
4. 有力な作者: Quaternius・Kay Lousberg・Poly by Google(軽量・CC0/CC-BYが多い)。和風は日本語ローマ字名の投稿者を狙う。
5. 検索語は日本語ローマ字/英語・類語・上位下位概念で広げるが、**カテゴリが変わる候補(神社→寺院建築)は採用しない**(Pagoda事故の教訓)。
6. `github.com`/`raw.githubusercontent.com`はブロック時の代替到達経路になりうる。
7. itch.ioは`curl`のみでログイン無しダウンロード可能(`csrf_token`→`download_url`→`file/<id>`の2段階POST)だが、ライセンス表記が無いことが多く実装前に許諾確認が必須。
8. BOOTH/ニコニ立体は¥0モデルでもアカウント登録必須(自動化ツールだけでは完結不可)。
9. sketchfab.com/cgtrader.com等はSPA/ボット対策で`curl`不可なことが多い(個別ページ直URLなら取得できる場合あり)。

## 参照する既存コード
`maps/shrine.js`: 神社固有コード一式(鳥居・本殿・手水舎・石灯籠・しめ縄境界・地面ゾーニング・`groundHeightAt`/`corridorBoundsAt`)。
`controller.html`: 汎用ホスト(`loadStaticGLB`/`normalizeToHeight`/`colliders`実体/`resolveCollision`/`_camRaycaster`/`charPos`/`characterRoot`)。

## 検証方法(共通)
1. `python3 -m http.server`でリポジトリ配信、Playwrightで`controller.html`サンプルモードを開く。
2. `window.__ghostController.setCharPos(x,z)`で各ゾーンにテレポートしスクリーンショット確認。
3. 鳥居の向き・石段のY追従・浮遊オブジェクト・カメラオクルージョンを実歩行で確認。
4. configパネルのクレジット欄に新規使用モデルが表示されること。
5. 読み込み失敗時にprocedural fallbackが出ないこと(単にオブジェクトが出ないだけであること)。
6. コンソールエラー0件(既知の削除済みモデル404を除く)。

## 実装ログ(圧縮要約)

- **第1〜9弾**: 素材調査・本殿問題の是正(Pagoda撤回→itch.io/BOOTH候補比較→ライセンス問題で保留)・プロレビューによる細部是正(神狐像呼称、紙垂、手水舎屋根等)を経て、最終的にuser提供の「つっちー」本殿/手水舎モデルを採用。FBX→glb変換で頂点欠落が発生し、OBJ+MTL経由の再変換で解消(教訓: バウンディングボックスの数値だけでジオメトリ欠落を判断せず、必ず目視レンダリングで確認する)。
- **第10弾**: 計画書(高低差レイアウト・マップ分離アーキテクチャ)を初めてコードに反映。本殿前後反転バグ・設置物Y座標上書きバグ・`bush_free.glb`原点ズレバグを修正。御神木を大型化した際にカメラオクルージョンが樹冠を拾って画面が黒潰れする不具合を発見し、高さ/配置位置を調整して解消。
- **第11弾**: 駐車場ゾーン新設・参道短縮(坂で一気に上る構成に変更)・境内広場化・鳥居のプロシージャル自作化・浮遊オブジェクトバグ修正を`maps/shrine.js`に実装済み(コード確認済み)。
