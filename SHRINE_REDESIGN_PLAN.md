# ゴーストコントローラー 神社マップ 大幅リクオリティ設計書

## Context

現状のゴーストコントローラー(`controller.html`)の神社は、user評価で「30点」。具体的な指摘:
1. 本殿がプリミティブ形状の代替モデル(`buildMainHallProcedural`)またはフリーモデル「Shrine by Aidan K McLaughlin」で構成されているが、後者は見た目が神社ではなく西洋の神殿/宮殿に近い。
2. 狛犬代替の石像(`buildStatueProcedural`または「Statue by Zsky」)が土台(台座)に乗っておらず、見た目もおかしい。
3. 鳥居の向きがおかしい。
4. オブジェクトの物量が少なく寂しい。
5. 読み込み失敗時のプリミティブ形状フォールバック(procedural fallback)は不要(＝全オブジェクトを実際のフリーモデルで用意し、読み込めなければ何も出さないだけでよい)。
6. マップの面積を現状の約10倍にしたい。

このドキュメントは、上記すべてに応える形で神社全体を再設計するための計画。**フリーモデル探しに特に注力**し、実際にダウンロード・軽量性確認・three.jsでのプレビュー描画による目視確認まで行った結果に基づく(下記「調査済み素材」は推測ではなく実物確認済み)。

### 第2弾ブラッシュアップ(今回)

上記の計画に沿って対象フリーモデル(`models/pagoda_free.glb`等)のダウンロードまでは完了済みだが、`controller.html`本体のコード反映は未着手のまま(直前のコミット時点のメモに"Controller code updates to use these still pending."と明記)。その状態でuserから追加フィードバック:「マップは広くなったが、新規の建物・オブジェクトが少ないのでは。クオリティアップのため高低差も含めて色々考えてよい」。

これを受け、実装に着手する前の設計段階でさらに練り込む。方針は2つ:
- **物量だけでなく"建物の種類"を増やす**: 単に灯籠や祠の個数を増やす(同じアセットの複製)のでは「広いのに同じものの繰り返し」感が残る。新しいカテゴリ(摂社/末社、御神木、賽銭箱、鳥居ゲートの多層化、太鼓橋+池、展望台、絵馬掛け等)を追加し、区画ごとに「そこにしかないもの」を作る。
- **高低差(垂直方向の変化)を追加**: 現状は完全に平坦な一本道(z軸方向のみ)。石段で2〜3段の高台を作り、本殿を最も高い場所に置く(実在の神社の一般的な構成に合致)ほか、池のある窪地・展望台への支道など、上下移動そのものを探索の見どころにする。

以降の章は上記を反映して更新済み。**旧版からの主な変更点**: §8(密度アップ)に新規建物カテゴリを追加、§9(レイアウト)を高低差ありの多段構成に全面書き換え、新設§9.5(高低差の実装方式)でコード上の対応方針を明記、§2.5(調査済みフリーモデル・追加分)で今回リサーチした新規候補を追記。

### 第3弾ブラッシュアップ(今回)

コード反映はまだ未着手のまま、userから3件の追加フィードバック:

1. 「神社が3重塔に置き換えられてるらしいから再度調べて」——**§2.5の「本殿=Pagoda」案は建築的に誤り**という指摘。ネットワークは再設定したので再調査してほしいとのこと。
2. 「地面とかもしっかりして。石畳とか砂利とか普通の地面とか場所に応じて」——地面が現状ただの単色平面のままで、参道・境内・奥の院で素材が変わらないのは寂しいという指摘。
3. 「プロの神社好きの目線で全体レビューして設計書クオリティアップ」——建築・作法まわりの細部を専門家目線で洗い直してほしいという指摘。

これを受けての対応(詳細は各節参照):
- **本殿問題の是正**: 五重塔/三重塔(Pagoda)は仏教寺院の建築物であり神社の本殿には使えないという指摘は完全に正しい。再調査結果と是正方針は§3(旧「本殿」節)および新設§2.6にまとめた。**先に結論を書くと、今回のセッションでもpoly.pizza等の実サイト調査は再びネットワーク方針でブロックされており(§2.6参照)、代わりとなる確実な自由モデルはまだ見つかっていない**。userには実環境のネットワーク設定確認をお願いしたい旨と、代替案(プロシージャル妥協案・別セッションでの再探索・GitHub経由の代替チャネル)を提示する。
- **地面素材**: 新設§9.6で、参道=石畳(中央)+玉砂利(左右)、境内=玉砂利、石段=切石、奥の院=土/苔混じりの未舗装路、という実在の神社の作法に基づくゾーニングと実装方式を計画。
- **プロの神社好きレビュー**: 新設§2.7に、本殿問題以外にも見つかった建築・作法上の是正点(キツネ像の呼称、手水舎の屋根、しめ縄の紙垂、賽銭箱の見た目、御神木への注連縄の巻き方、門松の季節限定性など)をチェックリスト形式でまとめた。
- **マップの分離アーキテクチャ**: 「マップをcontroller.htmlに埋め込まず別ファイル化し、将来複数マップに対応したい」という新規要望。これは今回初めて出た独立した要件のため、新設の大章「## マップ分離アーキテクチャ」にまとめた(実装はまだ行わず、計画のみ)。

### 第4弾ブラッシュアップ(今回)

第3弾の時点では「ネットワークが繋がらず本殿代替を確認できていない」という留保付きだったが、userが環境のネットワークポリシーを実際に修正し、poly.pizza等への直接アクセスが復旧した。これを受けて再度ブラッシュアップ:

- **本殿代替の本格再調査**: §2.6を更新し、ネットワーク復旧後に「shrine hall」「haiden」「japanese temple」等の多数の検索語・複数作者のプロフィールページを実際にダウンロード・three.jsプレビューまで行った結果を追記。**結論は変わらず「見つからない」だが、これは前回までのような環境都合の留保ではなく、実際に幅広く探した上での確定結果**に格上げされた。
- **§2.5(B)の残り項目を本格検証**: 新設§2.8に、石段・太鼓橋・玉垣・案内看板・本殿の引き戸(既存の黒い平面をアップグレード)・鳥居/灯籠のバックアップなど、**実際に採用が確定したもの**と、賽銭箱・篝火・鐘/鈴・展望台・崩れた祠など**探したが見た目が合わず不採用/未発見に終わったもの**を分けて記録。特に賽銭箱は「Chest」系フリーモデルが全て海賊の宝箱型であることが判明したため、**置かない方針を正式決定**した。
- 「他にもフリーのサイトないんかなぁ」という要望を受け、poly.pizza以外に`quaternius.com`(パック一覧)・`opengameart.org`も確認したが、和風の神社建築そのものは見つからなかった(詳細は§2.6・§2.8参照)。

### 第5弾ブラッシュアップ(今回)

「他のサイトも探して。根気よく。」という要望を受け、poly.pizza/quaternius.com/opengameart.org以外のサイトを追加で探索した。結果、**itch.ioで本殿問題を解決しうる決定的な発見があった**(詳細は新設§2.9)。

- `itch.io`の「free game assets tagged japan」を確認し、実在の投稿を1つずつ精査。その中の「Mini Japan」(Ely Dev)という941KBの小さな無料アセットパックに、**求めていた「平屋・切妻(入母屋)屋根・朱塗り柱+白壁」の神社建築そのもの**(House-a/House-b)が2種類、同じ配色で鳥居・太鼓橋・石灯籠(2種)・手水鉢・笹威し(bamboo鹿威し)まで一式揃っていた。§2.6で「見つからない」としていた結論を覆す発見のため、**本殿の実装方針(§3)をこちらの実物モデル採用に更新**した。
- ただし**このパックのitch.ioページには明示的なライセンス表記が無い**(poly.pizzaの全アイテムがCC0/CC-BYを明記しているのとは対照的)。無断使用のリスクを避けるため、**実装前に作者(Ely Dev)へのライセンス確認/許諾依頼を必須の前提条件**とした(§2.9参照)。まだ`models/`への実ファイルコミットは行っていない(ライセンス確認が取れてから正式取り込みする)。
- `sketchfab.com`/`www.cgtrader.com`はPlaywright実ブラウザでのアクセスも試みたが、クライアントサイドレンダリング(SPA)かボット対策により検索結果を取得できなかった。`free3d.com`/`renderhub.com`/`turbosquid.com`/`cults3d.com`は403で直接アクセス不可。これらは今回断念し、次回セッションでの課題として残す。

### 第6弾ブラッシュアップ(今回)

「そのモデルイマイチです。日本のサイトとかないの」——§2.9のEly Dev版は許容範囲内ではあるものの見た目の完成度がuserの期待に届かなかったという指摘。日本語圏のサイトを追加で探索した結果、**§2.9を上回る、日本人モデラーによる非常に高品質かつライセンスの明確な神社モデルが複数見つかった**(詳細は新設§2.10)。

- `BOOTH`(pixiv運営の同人グッズ/自作品マーケット)を検索した結果、「神社ステージ」(プリメロ工房)・「神社（本殿）」(ktgazer.org)・「神社（拝殿）」(プリメロ工房)という3つの無料(¥0)モデルを発見。いずれもBlender出身の日本人モデラーによる本格的な神社建築で、実際に伝統建築(一間社流造等)を参考にしている旨が明記されており、**プレビュー画像を確認した限りではEly Dev版よりも明らかに完成度が高い**。ktgazer.org版は「改変可・商用可(無料版はクレジット表記のみ)」という明確なCC-BY相当の利用規約が本文に明記されており、§2.9のライセンス不明という弱点も解消している。
- `ニコニ立体`(ニコニコが運営するMMD/VRM向け3Dモデル投稿サイト)でも「博麗神社」(東方Projectの神社)・「烏丸神社」等、複数の神社モデルの存在を確認した。
- **ただしBOOTH・ニコニ立体とも、無料(¥0)モデルであってもダウンロードにはサイトのアカウント登録(pixiv/BOOTHアカウント、niconicoアカウント)が必要**で、このセッションでは(メール認証等が必要なアカウント作成はuser本人の情報が必要なため)実際のダウンロードはできていない。プレビュー画像・利用規約の文面までは確認済みだが、**実ファイルの目視確認(three.jsプレビュー)はまだ行えていない**。次のアクションとしてuser側でのアカウント作成・ダウンロードをお願いしたい。

### 第7弾ブラッシュアップ(今回、本殿モデル確定)

userが実際にBOOTHアカウントで§2.10の候補をダウンロードして検証した結果を踏まえ、本殿モデルを最終確定した。

- 「神社ステージ」(プリメロ工房)の実ファイルはglb単体で**17MB**あり、userの「重いの嫌です」という判断で不採用。プリメロ工房の利用規約には「改変可」の明記が無いため、勝手な軽量化(ポリゴン削減等)もしないことを確認済み(§2.10参照、改変可否が不明な素材は改変しない、という方針を徹底)。
- 「神社（本殿）」(ktgazer.org)の実ファイル(FBX)は**4MB**。ライセンスは「改変可・商用可」と明記済みで軽量化してよい対象だが、userからは「本殿にしては小さい」というコメントがあった——これは実は正しく、本殿は本来ご神体を安置する最奥の小さな建物で、拝殿(参拝者が対峙する大きく開放的な建物)より小さいのが正式な様式(§2.6参照)。ただしゲームの見せ場としてのインパクトを優先し、今回は不採用。
- 代わりに、§2.9のitch.io「Mini Japan」(Ely Dev、ライセンス不明で保留中)と、userが自ら見つけて共有してくれた「nojinja01」(kisaraginonono、BOOTH、**利用規約「規約はなし、よしなに」**——実質無制限に近い最も緩いライセンス、376KBと軽量)を比較した結果、**「nojinja01」を採用することでuserと合意した**。見た目は高床式の切妻屋根の建物で装飾は控えめだが(§2.9のEly Dev版と比べても地味な部類)、ライセンスの明確さと軽さを優先する判断。
- `assimp`(このセッションで`apt-get install`して用意)でFBX→glb変換を行い、`models/mainhall_free.glb`として**コミット済み**(492KB、three.jsプレビューで正常に読み込めることを確認済み)。これにより、§3(本殿)の実装候補の宙ぶらりん状態はついに解消された。

## 現状の実装(把握済み、変更対象)

`controller.html` 内、神社構築部分(既存関数、すべて置き換え/削除対象):
- `buildToriiProcedural`/`placeTorii(z)` — 鳥居。フリーモデル`models/torii_free.glb`(Hattie Stroud)+プロシージャルfallback。
- `buildLantern(x,z,mode)` — 石灯籠。完全プロシージャル(円柱+箱+四角錐)。
- `buildBasin(x,z)` — 手水舎。完全プロシージャル。
- `buildMainHallProcedural`/`placeMainHall(z)` — 本殿。フリーモデル`models/shrine_hall_free.glb`(Aidan K McLaughlin)+フォールバック。当たり判定(box collider)・黒い引き戸(`MeshBasicMaterial`平面)・人感センサーライト(`sensorLight`/`hallFrontZ`)を内包。
- `buildStatueProcedural`/`placeStatues(z)` — 石像(狛犬代替)。フリーモデル`models/statue_free.glb`(Zsky)+フォールバック。
- `buildForbiddenBoundary(z,halfW)` — しめ縄+木杭境界。完全プロシージャル。
- `placeTrees()`/`scatterProps(...)` — 杉の木・岩・茂み。フリーモデル使用済み(変更少)。
- `colliders`配列(`{type:'circle',...}`/`{type:'box',...}`) + `resolveCollision(pos)` + `corridorBoundsAt(z)`(z12〜19だけ東側境界を張り出す小広場) — 当たり判定システム、レイアウト拡張はこの仕組みを流用。
- `normalizeToHeight(obj,targetHeight)`/`loadStaticGLB(url)` — 既存のロードヘルパー、そのまま再利用。
- `FREE_MODEL_CREDITS`/`refreshCreditsUI()` — configパネルのクレジット表示、そのまま再利用。
- `updateShrineEffects(dt)` — 入口点滅灯・手水舎の吊り電球の揺れ・人感センサーライトの毎フレーム更新。基本ロジックは流用しつつ吊り電球の対象を新モデルに合わせる。
- `charPos`(`THREE.Vector3`)は現状**Y座標が常に0固定**(`charPos.set(x,0,z)`/`characterRoot.position.set(charPos.x,0,charPos.z)`)。地面(`groundGeo`)も単一の平坦`PlaneGeometry`。高低差を導入するにはここに手を入れる必要がある(→§9.5)。

## 調査済みフリーモデル(poly.pizza、実際にダウンロード・three.jsでプレビュー描画して目視確認済み)

すべてCC0またはCC-BY(要クレジット)。サイズは既存のキャラクターモデル(3MB系)や現状の6素材合計(584KB)と比較して十分軽量。

| 用途 | モデル名 | 作者 | ライセンス | サイズ | URL | 確認結果 |
|---|---|---|---|---|---|---|
| ~~本殿(センターピース)~~ | ~~Pagoda~~ | ~~Poly by Google~~ | ~~CC-BY~~ | ~~484KB~~ | ~~poly.pizza/m/d1M5ncMBUDi~~ | **【第3弾で撤回】三層の反り屋根の塔という見た目は美麗だが、これは五重塔/三重塔=仏教寺院(寺)の建築であり神社(本殿)には使えない、というuser指摘が建築的に正しい。詳細と代替案は§2.6参照** |
| 入口の鳥居(1基のみ、目玉) | Japanese Torii | Jacques Fourie | CC-BY | 846KB | poly.pizza/m/cXyQGUwmlA5 | 厳島神社スタイルの反り屋根+副柱+提灯装飾。非常に情報量が多く豪華 |
| 参道に連続配置する鳥居(千本鳥居風) | torii | sugamo | CC-BY | 20KB | poly.pizza/m/8ZgGY1XCfuR | シンプルで明快な朱色鳥居。角度0で正面から見て開口部がまっすぐ見えることを確認済み(回転補正おそらく不要、実装時に実機で再確認) |
| 石灯籠 | Toro | Matt Newell | CC-BY | 76KB | poly.pizza/m/0SguM8o_PMc | 段状の台座+火袋+笠、正しいシルエット。色が朱色寄りなので必要ならHSL調整で石色に寄せる(本殿と同じ手法) |
| 石像(狛犬代替→キツネに変更提案) | Kurama | Imran Bepari (theCH33F) | CC-BY | 64KB | poly.pizza/m/5KQZFKrA-EM | 九尾狐のような凛々しい低ポリキツネ。**下記「狛犬→キツネ案」参照** |
| 石像の台座 | Pebble Square | Quaternius | CC0 | 数十KB | poly.pizza/m/2YtLzwgsWp | 苔むした石の台座形状。狛犬(キツネ)・灯籠等を載せる土台として使える |
| 小さな祠/道端の目印(バリエーション用) | Shrine (2種) | Kay Lousberg | CC0 | 24KB/36KB | poly.pizza/m/Qq8M5LSXQ2, poly.pizza/m/tFxdxO5clk | 石の小祠+ろうそく、石柱型の祠。境内各所に散らして密度を上げる。**今回、これを単に「散らす小道具」から「摂社/末社セット」の主役に格上げ(§8参照)** |
| 門松(装飾・任意) | kadomatsu | sugamo | CC-BY | 未計測(小) | poly.pizza/m/dGAmgoz6L4T | 正月飾りだが和風演出として入口や社務所前に置ける |
| 杉の木(既存流用) | Pine Tree | Quaternius | CC0 | 94KB | poly.pizza/m/gX8WmgkeEm | 既存のまま。**今回、1本だけ大幅スケールアップして御神木(§8)に転用提案** |
| 岩(既存流用) | Rock | Quaternius | CC0 | 9KB | poly.pizza/m/4MUaQTcDdc | 既存のまま |
| 茂み(既存流用、要再検討) | Flower Bushes | Quaternius | CC0 | 342KB | poly.pizza/m/1X06RgvSr6 | 既存のまま(花付きが違和感あれば実装時に無地の茂みへ差し替え検討) |

**狛犬→キツネ案の理由**: 軽量(数百KB以内)かつ商用/改変可・帰属表示のみで使えるkomainu(獅子狛犬)専用モデルは見つからなかった(唯一の具体的候補はSketchfab上のCC-BYモデルだが17.5kポリゴン+4K石/苔テクスチャ違い2種で数十MB級、ダウンロードにログインも必要で「軽量」に反する)。稲荷系の神社では狛犬の代わりに**キツネ像(眷属)**が対で置かれるのは実在する様式であり、`Kurama`はまさにキツネ型の低ポリモデル。「似たようなものでもいい」との方針に沿い、**キツネの神使像**として採用することを提案する(狛犬の代わりにキツネという設定に振ることで、無理に狛犬を名乗るより自然)。

**しめ縄境界・杭は継続調査**: 専用の「shimenawa」フリーモデルは見つからず。実装時に「rope」「vine」「wooden post/stake」等でもう一段調査し、見つからなければ杭(木の柱系フリーモデルの転用)+縄(vine系フリーモデルの転用)の組み合わせで代替する。**コード内でジオメトリを新規に組み立てる(プリミティブ)ことはしない**——既存の見つかった/類似フリーモデルの組み合わせ・使い回しで表現する。

## 2.5. 今回追加調査した新規カテゴリ(第2弾)

**(第4弾で追記)** このセクションは元々「ネットワークがブロックされておりWebSearchの手がかりのみ、未検証」という前提で書かれていたが、第4弾セッションでuserが環境のネットワーク設定を修正し、poly.pizzaへの直接アクセスが復旧した。以下(A)(B)の内容は当時の記録として残しつつ、**(B)の各項目は第4弾で実際にダウンロード・サイズ確認・three.jsプレビューまで完了しており、確定結果は§2.8にまとめた**。実装時は§2.8の確定結果を優先して参照すること(このセクションの(B)表は「どう探したか」の記録として参考程度に残す)。

### (A) 新規ダウンロード不要・既存アセットの流用で実現できるもの(検証リスクなし、優先度高)
これらは「新しい建物が増えた」という体感を最も安く生む。新規ダウンロードが要らないため、ネットワークが使えない状況でも設計・実装を進められる。

| カテゴリ | 実現方法 | ねらい |
|---|---|---|
| 摂社(せっしゃ)/末社(まっしゃ)セット | `shrine_small1_free`/`shrine_small2_free`(小さな祠モデル)を**単体の道端アイテムではなく**、`torii_row_free`(小鳥居)+`lantern_free`を2基add した「ミニ鳥居+ミニ灯籠+祠」の3点セットとして境内の脇道・木立の陰に2〜3箇所配置。実在の神社に多い「本殿の他に小さな境内社が点在する」構成を再現。1セットが独立した「小さな建物」として認識される | 建物の"種類"ではなく"数"は変えずに、**配置の意味づけ**で新しい建物が増えたように見せる。低コスト高効果 |
| 御神木(ごしんぼく) | `pine_tree_free`を1本だけ通常の3〜5倍スケールでロードし、根元に`stake_free`を環状に数本立てて結界を表現(しめ縄の代替表現は§7と同じ考え方を流用)。境内(Tier1)の広場中央に単独ランドマークとして配置 | 巨木という「その場所にしかない」ランドマークを作り、地図上の目印・記念撮影ポイントにする |
| 鳥居ゲートの多層化 | 現行計画では鳥居は「参道」区間にしかない。`torii_row_free`(小鳥居)を**各段(Tier)の石段の上り口ごとに1基**追加設置し、「上に登るたびに鳥居をくぐる」という実在の神社(伏見稲荷など)の様式を再現 | 高低差の演出と鳥居という既存アセットを組み合わせるだけで新規ダウンロード無しに「多層感」を出せる |
~~賽銭箱(仮)~~ | ~~新規モデル未確保だが、`pedestal_free`(石の台座、CC0)を本殿前に小さめスケールで単体設置し木箱代わりに転用~~ | **【第4弾で撤回】§2.8(B)の通り"Chest"系は全て海賊の宝箱型で賽銭箱に使えないと確定。賽銭箱は置かない方針に決定(§3参照)** |

### (B) 新規ダウンロードが必要・未検証(次回セッションでの再調査対象)
WebSearchでタイトル・作者名の手がかりは得られたが、**サイズ/ライセンス/見た目は完全未確認**。以下は「有力な検索の出発点」であり、実装時に前回と同じ手順(ダウンロード→`curl -sSI`でサイズ確認→three.jsプレビューで目視)を必ず経ること。

| カテゴリ | 用途 | 検索で見つかった手がかり(未検証) | 追加の検索キーワード案 |
|---|---|---|---|
| 石段(階段) | 高低差の各Tier間を繋ぐ実際の階段モデル(見た目の装飾用、当たり判定上の"歩ける面"は§9.5の通り別途スロープで実装) | "Stairs" by Kenney (poly.pizza/m/lZFtRSTeKR)、"Stairs Corner" by Kenney (poly.pizza/m/UF9OUbgbOr)、"spiral stairs" by sugamo (poly.pizza/m/7o_wIY-00y-) | stone stairs, temple steps, staircase |
| 太鼓橋+池 | Tier1境内の脇道に小さな池を作り、そこに架ける反り橋。寄り道すると水面越しに境内を見返せる構図を狙う | 具体的な有力候補は未発見(一般的な"Bridge"検索のみ) | arch bridge, japanese garden bridge, stone lantern water, koi pond |
| 賽銭箱(正式版) | (A)のpedestal代替より神社らしい木箱があれば差し替え | "Chest" by Quaternius (poly.pizza/m/eEcIqgJzJ1、要:神社の賽銭箱らしい素朴な木箱に見えるか目視確認) | offering box, wooden chest, donation box |
| 玉垣(たまがき) | 本殿(Tier2)の周囲を囲う低い柵。「一番格の高い場所」であることを視覚的に強調 | 未発見 | low fence, wooden fence, shrine fence, precinct fence |
| 絵馬掛け/おみくじ結び所 | 木の額(絵馬)を吊るす棚、またはおみくじを結ぶ紐棚。境内の一角に小さな見どころを追加 | 未発見(既存モデルの転用が濃厚:棚/ラック系モデルの流用を想定) | wooden rack, notice board frame, shelf stand |
| 展望台/東屋(あずまや) | Tier2から少し外れた高台の支道の先に設置し、参道全体を見下ろせるビューポイントにする(探索の"ごほうび") | 未発見 | gazebo, azumaya, park pavilion, wooden lookout |
| 鈴(すず)+鈴緒(すずお) | 本殿正面、賽銭箱の真上に吊るす鈴と紐。「お参りの一連の動線」を完成させる小物 | 未発見 | temple bell, hand bell, rope |
| 崩れた祠/廃屋 | 奥の院手前の窪地(Tier1.5、§9参照)に朽ちた雰囲気の小屋を1つだけ置き、「ゴーストコントローラー」らしい不穏さを演出 | 未発見 | ruins, broken shrine, abandoned hut, derelict cabin |
| 案内看板 | 分岐点(摂社への脇道、展望台への支道の入口等)に木の立て看板を置き、経路のヒントと密度を同時に稼ぐ | 未発見 | wooden signpost, trail sign |
| 篝火(かがりび)/常夜灯 | 境内の焚き火台。灯籠と役割が被らないよう、Tier1の広場1箇所だけ設置し光源として活用 | 未発見 | bonfire, brazier, torch stand |

## 2.6. 本殿問題の是正(第3弾、最優先)

### 何が間違っていたか
§2.5で採用していた「Pagoda」(Poly by Google)は、三層の反り屋根を持つ塔——つまり**五重塔・三重塔の系譜の建築物**。これは仏教の仏舎利塔(ストゥーパ)に由来する**寺院建築**であり、神社の本殿・拝殿とは屋根の重なり方から用途からまったくの別物。実在の神社にはまず存在しない(神仏習合期の一部の複合施設に例外はあるが、現代の一般的な「神社」としては誤り)。userの「神社が3重塔に置き換えられてるらしい」という指摘はその通りで、これは見た目の好みの話ではなく**建築カテゴリの取り違え**という設計上のミス。「神殿っぽい問題を解決した」と旧版で書いていたが、西洋神殿から仏教の塔に変わっただけで、どちらも「神社の本殿」としては不正解だったことになる。

参考(神社本殿の実際の構成、プロ目線での基礎知識):
- 神社の中核は普通2棟に分かれる。**本殿**(ご神体を安置する最奥の建物、多くは非公開・比較的小さく装飾も抑えめ)と**拝殿**(参拝者が手を合わせる手前の建物、大きく開放的)。ゲームの表現としては、プレイヤーが実際に対峙する建物は事実上「拝殿」の役割(黒い引き戸、賽銭箱、鈴/鈴緒を正面に配置する計画と整合する)。
- 屋根形式は流造(ながれづくり、片流れが前に伸びる最も一般的な形)・神明造(伊勢神宮系、切妻+棟に鰹木/千木)・春日造・八幡造・大社造など複数あるが、**共通するのは「平屋、切妻または入母屋の直線的な屋根」であり、塔状に何層にも重なることはない**。この「平屋で横に伸びる」シルエットが、五重塔の「縦に重なる」シルエットとの最大の違い。
- 色味は、伊勢神宮系のように無塗装の檜(白木)のままの社と、稲荷系・八幡系のように朱塗り+白壁の社の大きく2系統がある。今回の稲荷風(キツネの神使)の設定なら、朱塗り+白壁+黒い高欄、というのが最も自然。

### 今回の再調査結果(第3弾時点、履歴として保持)
userの依頼で「ネットワークを繋がるよう設定した」とのことで再調査したが、当時のセッションでは実際の素材サイト(poly.pizza / sketchfab.com / kenney.nl / quaternius.com / ambientcg.com / polyhaven.com / en.wikipedia.org 等)への直接アクセスが`curl`・`WebFetch`のいずれでも403で拒否されており、WebSearchの検索結果要約のみで判断するしかなかった。

### 第4弾での再調査結果(結論: ネットワーク到達可能になった状態で再調査済み、確保できない結論が確定)
第4弾セッションで、userが環境のネットワークポリシーを実際に修正し、poly.pizzaをはじめ一般サイトへの直接アクセスが復旧した(`curl`/`WebFetch`とも200で疎通確認済み)。この状態で、以下の手順で本殿代替を本格的に再調査した:
- poly.pizzaで「shrine hall」「haiden」「japanese temple」「shinto」「torii」等、複数の検索語で候補を洗い出し(検索結果ページのHTMLから`/m/<id>`を抽出→各ページの`og:title`とライセンス表記を取得→有力そうなものを実際にダウンロードしてthree.jsで目視プレビュー、という前回セッションまでと同じ手順を今回は最後まで実行できた)。
- 「torii」「Japanese Torii」「kadomatsu」の作者(sugamo、Jacques Fourie、Hattie Stroud)や、和風モチーフを他にも投稿している作者(Flopsi、Aimi Sekiguchi)の**投稿者プロフィールページ(`poly.pizza/u/<作者名>`)を丸ごと確認**し、その作者の他の投稿に神社建築が無いか確認。
- poly.pizza以外に、`quaternius.com`(Quaternius本人サイトのパック一覧)、`opengameart.org`(「japanese shrine」で検索)も確認。

**結果、神社の拝殿/本殿として使える完成度のフリーモデルは、上記のどの経路でも見つからなかった**。具体的には:
- poly.pizzaの「temple」「shrine hall」「japanese temple」系の検索結果は、実際にダウンロードして中身を確認したところ**全て西洋の教会・城の尖塔・ロシア正教風の玉ねぎ屋根といった非日本建築**だった(例: "Wodden Temple"/"Temple First Age Leve"by Quaternius→西洋風木造教会と尖塔、"Temple"by Quaternius(2種)→中世ファンタジー城の尖塔、"Temple"by Poly by Google→ロシア正教風の金色玉ねぎ屋根建築)。名前に釣られて採用すると今回と同じ「カテゴリ違い」のミスを繰り返すところだった。
- torii/kadomatsuの作者(sugamo等)のプロフィールを全件確認しても、神社建築そのものの投稿は無かった。
- `quaternius.com`のパック一覧(全80種以上)にも和風/神社テーマの専用パックは無い(近いものとして`sushirestaurantkit`(寿司屋、提灯等が入っている可能性)、`ultimatemodularruins`(廃墟モジュール、§2.7の崩れた祠に転用できる可能性)があるが、いずれもZIP配布のパックでpoly.pizzaのような個別プレビューができず、今回は中身未確認)。
- `opengameart.org`の検索では「Fujin Statue」(風神像)がヒットしたのみで、建物としての収穫はなかった。

**この結論により、「ネットワークが繋がらなかったから見つからなかった」という第3弾までの留保は外れ、「フリーモデルサイトを実際に幅広く探しても、神社の本殿/拝殿としてそのまま使える完成度の素材は現状無い」という、より確度の高い結論に格上げされた**。§「代替案」の1(プロシージャル暫定実装、平屋・切妻/流造シルエットを守る)を、繋ぎではなく**当面の本命として採用する**方針に変更する。将来的にZIPパック(`ultimatemodularruins`等)を実際に展開して中身を確認する、またはSketchfab(ログインが必要な場合が多い点は要注意)まで手を広げる、という次の一手は残しておく。

### 代替案(いずれか、または組み合わせ)
1. **(推奨・暫定)プロシージャルの妥協案に戻すが、プロポーションだけ正しくする**: 第0弾の指摘⑤「フォールバック全廃」の方針とは矛盾するように見えるが、これは「本殿という最重要オブジェクトについてだけ、良いフリーモデルが見つかるまでの繋ぎとして」明確に一時措置と位置付ける。`buildMainHallProcedural`を作り直す際は、五重塔にならないよう**平屋・切妻(または入母屋)・横長のシルエット**を守り、朱塗りの柱+白壁+黒い高欄+階段+賽銭箱前面、という配色・比率だけプロ目線で作り込む(既存の「プリミティブ品質が低い」という苦情は、形状の作り込み不足が原因であり、平屋であること自体は正しくできる)。
2. **別セッション/ネットワーク到達可能な環境での再探索**: §「フリーモデルを効率的に探す方法」の手順をそのまま使い、検索語を"haiden""shrine hall""torii shrine building""japanese shrine architecture low poly"等に広げて探す。「本殿一式」でなくても、屋根パーツ+柱+高欄を別々のフリーモデルで組み合わせるキットバッシュも視野に入れる(実在の神社建築も梁・柱・屋根の組み合わせなので、キットバッシュ自体は不自然にならない)。
3. **GitHub経由の代替チャネル**: 今回`github.com`/`raw.githubusercontent.com`は到達可能と確認できた。CC0アセットをまとめたGitHubリポジトリ(例: `madjin/awesome-cc0`等のawesome-listや、glTFサンプルを配布しているリポジトリ)を経由すれば、直接poly.pizza等にアクセスできないセッションでも一部の素材は入手できる可能性がある(今回ざっと見た範囲では和風建築の直接ヒットはなかったが、探索の余地は残っている)。

いずれの案を採るにしても、**実装時に必ずthree.jsでの目視プレビューを行い、「塔のように何層にも重なっていないか」「平屋の切妻/流造シルエットになっているか」を確認してから採用する**——これが今回のミスの再発防止策になる。

## 2.7. プロの神社好き目線での全体レビュー(第3弾、新設)

本殿問題ほど致命的ではないが、実装前に直しておきたい細部。

| # | 対象 | 現状の計画 | プロ目線での指摘 | 対応方針 |
|---|---|---|---|---|
| 1 | 石像(キツネ) | `Kurama`を「狛犬代替」として使い、コード上も"狛犬代替"と呼んでいる | 実在の稲荷系神社では、あの位置に立つのは正式には**神狐(しんこ)像**や眷属像であり「狛犬」ではない。コードのコメントや変数名で「狛犬代替」と呼び続けると、知っている人ほど違和感が募る。また本物の神狐像は口に鍵・玉・巻物・稲穂などを咥えていることが多い(今回のモデルにその意匠があるかは実機確認が必要) | 変数名・コメント・UI文言があれば「狛犬」ではなく「神狐像」「キツネの神使像」に統一する。咥え物の有無は実装時の目視確認項目に追加(§検証方法) |
| 2 | 手水舎 | 「Bowl」だけを水盤として置き、屋根は無しで簡略化する前提 | 本来の手水舎(ちょうずや/てみずや)は、水盤の上に小さな切妻屋根+4本柱の覆いが付くのが標準形。屋根なしの水盤だけだと「ただの噴水/水鉢」に見えてしまう可能性が高い | §2.5(B)で探す「展望台/東屋(あずまや)」用の小さな屋根付きパビリオン系モデルが見つかれば、それを縮小して手水舎の屋根に転用する(展望台と手水舎で同じ「柱+切妻屋根」パーツを使い回せば、新規ダウンロード無しで2箇所を同時に解決できる) |
| 3 | しめ縄境界 | 縄(rope/vine)+杭(stake)の組み合わせで表現、紙垂(しで)への言及なし | 実物のしめ縄で最も目を引くのは、縄自体より**紙垂**(白いジグザグの紙垂れ)。紙垂が無いと「ただのロープの柵」にしか見えず、神域を示す境界として機能しない | 紙垂は薄い白色の`MeshBasicMaterial`平面を数枚、縄に等間隔でぶら下げる方式で表現する。これは既存コードの黒い引き戸平面と同じ「単純な装飾ディテールとしての平面」の扱いであり、「オブジェクト本体をプリミティブで作らない」という方針とは矛盾しない(あくまで縄本体はフリーモデル/転用素材を使う前提) |
| 4 | 賽銭箱(仮) | `pedestal_free`(石の台座)を木箱代わりに転用する暫定案 | 実物の賽銭箱は**格子状の板を天面に張った木箱**で、石の台座とは形状・素材感がまったく違う。台座を賽銭箱と言い張るのはかなり無理があり、目視確認で違和感が出る可能性が高い | 優先度を上げて§2.5(B)の`Chest`(Quaternius)系候補、またはGitHub経由での探索を先に試す。見つからない場合は「賽銭箱を置かない」選択肢も検討する(無理に見た目の悪い代替を置くより、無い方がまだ自然) |
| 5 | 御神木 | `pine_tree_free`拡大+根元に`stake_free`を環状配置でしめ縄の代替表現 | 実物の御神木の注連縄は、たいてい**幹の胸の高さ(人の目線あたり)に巻き付ける**もので、根元を杭で囲う形式ではない(根元の柵は一般の樹木保護柵に近く、御神木の神聖さの表現としては弱い) | 幹の途中(1.2〜1.5m相当の高さ)に紐/縄系の転用素材を巻き付け、上記#3と同じ紙垂平面を垂らす形に変更する。根元の杭は不要(または別用途の低い境界柵に転用) |
| 6 | 門松 | 入口・本殿前・各Tierゲート付近に2〜3組、常設のアクセントとして配置 | 門松は本来**正月(12/26頃〜1/7頃)限定**の飾りで、一年中出しっぱなしは実在の神社としては不自然(季節感のズレに気づく人は気づく)。ゴーストコントローラーに季節/期間イベントの概念が無い限り、常設は違和感の元 | 「常設の神社らしい飾り」としては門松より**看板(社号標・案内看板)**や**常夜灯**の方が一年中自然。門松は優先度を下げ、置くとしても1組(入口)に留めるか、そもそも省略して案内看板等に差し替える |
| 7 | 手水舎の位置 | 明確な位置指定なし(Tier1境内のどこかに配置予定) | 手水で身を清めるのは本来「鳥居をくぐった直後、本殿に向かう前」に行う作法。境内の奥や脇に置くと作法上おかしい | Tier1(境内)に入ってすぐ、鳥居ゲート直後の位置に固定配置するよう§9のレイアウトに明記する |
| 8 | 参道の歩き方(フレーバー) | 特に言及なし | 参道の中央は「正中(せいちゅう)」と呼ばれ、神様の通り道とされるため参拝者は端を歩くのが作法、というのは比較的知られた豆知識 | 必須の実装ではないが、config画面やロード画面のヒント文言に一言添えると「わかっている」感が出る(低コストな加点要素、実装は任意) |

上記のうち#1・#3・#5・#6は**モデル追加不要でコード側の扱い方だけで直せる**ため優先度が高い。#2・#4は新規モデル探索(§2.5(B))と連動するため、探索の優先順位を上げる根拠として扱う。

## 2.8. 第4弾での確定調査結果(ネットワーク復旧後、実ダウンロード・目視確認済み)

userが環境のネットワーク設定を修正し、poly.pizzaへの直接アクセスが復旧したため、§2.5(B)の未検証項目を中心に再調査し、実際にダウンロード→`curl -sSI`でサイズ確認→three.jsプレビューで目視確認まで完了した。**このセクションの内容が現時点で最も信頼度が高い**(poly.pizza以外の主要な検討先として`quaternius.com`のパック一覧、`opengameart.org`も確認済み)。

**下記(A)で採用確定したモデルは、既にこのセッションで`models/`配下にダウンロード済み**(`stairs_free.glb`/`steps_free.glb`/`bridge_free.glb`/`fence_free.glb`/`fence_center_free.glb`/`signpost_free.glb`/`shoji_door_free.glb`/`torii_alt_free.glb`/`lantern_alt_free.glb`)。旧`pagoda_free.glb`(撤回済み、§2.6)は削除済み。コード側(`controller.html`)への組み込みはまだ行っていない(マップ分離アーキテクチャの章の通り、実装は別セッション/別コミットで行う想定)。

### (A) 採用確定(見た目確認済み、そのまま使える)

| カテゴリ | モデル名 | 作者 | ライセンス | サイズ | URL | 確認結果 |
|---|---|---|---|---|---|---|
| 石段(踏面装飾) | Stairs | Kenney | CC0 | 39KB | poly.pizza/m/lZFtRSTeKR | シンプルで無地(単色オレンジ)な直進階段ブロック。石色にHSL調整すれば石段として自然。§9.5のスロープ区間の見た目装飾として3箇所(Tier0→1、Tier1→2、Tier2→1.5)に使う |
| 石段(短い区間用) | Steps | Kenney | CC0 | 14KB | poly.pizza/m/2cghwnOqEX | 4段程度の短い階段、小さい段差(祠の入口等)向け |
| 太鼓橋 | Small Bridge | Quaternius | CC0 | 73KB | poly.pizza/m/j4KsIuJYnq | 反り+左右の欄干という理想的な太鼓橋シルエット。§9のTier1東側支道(池)にそのまま使える、探していたものがそのまま見つかった好例 |
| 玉垣(precinct fence) | Fence(直線)+Fence Center(交点) | Quaternius | CC0 | 13KB/11KB | poly.pizza/m/r0n40F7FKx, poly.pizza/m/I7D7zk30rv | 柱+2段の横木という素朴な木柵。本殿(Tier2)を囲う玉垣として直線区間+四つ角に使うモジュール構成。同シリーズにFence Corner/Fence End等もあり、四角く囲うレイアウトが組める |
| 案内看板 | Signpost | Kenney | CC0 | 6.3KB | poly.pizza/m/3U2lj1gpeH | 矢羽根型の案内板2枚+支柱。分岐点(摂社への脇道等)の道しるべに最適 |
| 案内看板(横長タイプ、代替) | Wooden Sign | iPoly3D | CC0 | 27KB | poly.pizza/m/TzjiI94FC5 | 横板3枚の掲示板タイプ、根元に草。掲示板然とした見た目が欲しい場合はこちら |
| **本殿正面の引き戸(既存の黒い平面を置き換え)** | Japanese Door | Quaternius | CC0 | 13.6KB | poly.pizza/m/t4otyljz8K | 障子格子がそのまま再現された本物の引き戸モデル。現状`buildMainHallProcedural`/`placeMainHall`内にある「黒い`MeshBasicMaterial`平面」を、本殿がプロシージャル暫定実装のままでもこれに差し替えるだけで、正面まわりの説得力が大きく上がる。**§3の実装方針に追記** |
| 鳥居(バックアップ) | Torii Gate | Quaternius | CC0 | 16KB | poly.pizza/m/7SyXZ62xR5 | 現行採用中の「torii」(sugamo)が万一読み込み不調の場合の同価格帯な代替。素朴な茶色の鳥居 |
| 石灯籠(バリエーション用、バックアップ) | Japanese Stone Lamp | Flopsi | CC-BY 3.0 | 26KB | poly.pizza/m/5gZfOZIW92k | 現行の「Toro」と少し違う笠のシルエット。灯籠を12〜16基並べる際、全部同じモデルだと単調なので一部をこちらに差し替えると変化が出る(要クレジット追加) |

### (B) 探したが確保できなかった、または見た目が合わず不採用

| カテゴリ | 試した検索・確認内容 | 結論 |
|---|---|---|
| 本殿/拝殿そのもの | §2.6参照。「temple」「shrine hall」「haiden」「japanese temple」等で多数確認、全て西洋教会/城/ロシア正教建築 | **見つからず**。§2.6の代替案1(プロシージャル、平屋・切妻/流造シルエット)を当面の本命とする |
| 賽銭箱 | 「Chest」で作者違いを10種以上確認(Quaternius・Kenney・J-Toastie・Anastasiia Ku等) | **全て蓋が丸くドーム状の「海賊の宝箱」型で、賽銭箱の平らな格子蓋とは似ても似つかない**。§2.7#4の懸念通り。無理に採用せず、賽銭箱は置かない案を正式に採用する |
| 篝火/常夜灯 | 「Torch」(Quaternius)を確認 | **懐中電灯(フラッシュライト)であり火の松明ではない**。不採用、探索継続 |
| 鐘楼/鈴 | 「Bell Tower」(Quaternius、Fabio Hofnik)、「Bell」(Poly by Google、Quaternius)を確認 | 鐘楼は**西洋の石造り時計塔**で不採用。手鈴(ハンドベル)は形状が鈴/鈴緒とかなり違い弱い候補(採用は保留、他の検索語で継続) |
| 展望台/東屋、手水舎の屋根 | 「Gazebo」(Quaternius、Poly by Google、Zsky)を確認 | **西洋公園のあずまや(八角形+円錐屋根、または急勾配のコテージ風切妻)で和風ではない**。代替が無ければ妥協して使う程度の弱い候補 |
| 絵馬掛け/おみくじ結び所 | 専用モデルは見つからず | (A)の「Wooden Sign」を小型化して代用する案は視覚的に弱い。今回は見送り、次回「wooden rack」「votive plaque」等で再探索 |
| 崩れた祠/廃屋 | poly.pizza上では見つからず。`quaternius.com`の`ultimatemodularruins`パック(ZIP配布)に可能性あり | 未確認(ZIPを展開して中身を見る作業が必要、今回は着手せず次回の課題とする) |

**総括**: 石段・太鼓橋・玉垣・案内看板・鳥居/灯籠のバックアップ、そして本殿の引き戸アップグレードという、当初§2.5(B)で「未発見」だったものの過半数が今回で解決した。残っているのは本殿本体・賽銭箱(→置かない方針で決着)・篝火・鐘/鈴・展望台・絵馬掛け・崩れた祠で、このうち賽銭箱は方針決定済み、本殿はプロシージャル案が本命に格上げされたため、**実質的に次回探すべきものは篝火・鈴・展望台・絵馬掛け・崩れた祠(ZIPパック内)の5点に絞られた**。

**(第5弾で更新)** 上記の「本殿はプロシージャル案が本命」という結論は、§2.9の発見により**覆った**。本殿は実物フリーモデル(ライセンス確認完了後)を本命とし、プロシージャル案は「ライセンス確認が取れなかった場合のフォールバック」に格下げする。

## 2.9. 第5弾での発見: itch.ioの「Mini Japan」パックが本殿問題を解決(ライセンス確認待ち)

### 発見の経緯
`itch.io`の無料ゲームアセット一覧(`japan`タグ)を確認したところ、`Ely Dev`による「Mini Japan」(Japanese Village - Tiny Low-Poly Pack)という**941KBのZIPパック**がヒットした。`.fbx`/`.gltf`/`.obj`の3形式で25以上のアセットが同梱されており、**`.gltf`形式がそのまま`GLTFLoader`で読み込める**。price=$0(投げ銭ではなく完全無料)、ログイン不要でダウンロード可能(itch.ioの`download_url`→`file/<id>`のAPIを直接叩けば、ブラウザなしでも一時署名付きURLを取得できることを確認済み)。

ZIPを展開してthree.jsで全アセットを目視確認した結果、**まさに探していた「神社建築」がそのまま入っていた**:

| アセット | 内容 | 評価 |
|---|---|---|
| `House-a.gltf` | 12m×13m、高さ8.2m。入母屋(寄棟に近い)瓦屋根+朱塗りの柱+白壁+高床、四方吹き放し | **本殿/拝殿の理想形そのもの**。§2.6で要求した「平屋・切妻/入母屋・横長シルエット・朱塗り+白壁」を完全に満たす。三重塔の失敗を今度こそ解消できる |
| `House-b.gltf` | 12m×10m、高さ8.6m。House-aに近い様式だが壁面に格子窓、正面に引き戸風の意匠あり | House-aとの2択、またはHouse-aを本殿、House-bを摂社/末社(§8)に使い分ける案も可 |
| `Torii.gltf` | 朱塗り、反りのある笠木、円柱の柱に石の台座 | 現行の鳥居セットと差し替え、またはHouse-a/bと統一感を出すために本殿前だけこちらに変更、という選択肢もある |
| `bridge-a.gltf`/`bridge-b.gltf` | 朱塗りの欄干+反り橋、板張りの床 | §2.8(A)で採用したQuaternius版(茶色)よりも**この一式の配色統一感を優先するなら差し替え候補**になる |
| `Toro-a.gltf`/`Toro-b.gltf` | 朱塗り+格子の灯籠(置き型)、火袋が発光しているように見えるマテリアル | 石灯籠のバリエーション、または本殿前の目立つ位置に1基だけこちらを使うのも良い |
| `water-fountain.gltf` | 石の水盤(手水鉢そのものの形状) | §6手水舎の水盤を、現行のGoogle「Bowl」からこちらに差し替え候補 |
| `koi-pond-water.gltf`(名前は誤りで実体は鹿威し) | 竹の樋+受け筒、いわゆる鹿威し(ししおどし) | 池のほとりに置く小道具として§2.5(B)の池+太鼓橋エリアに追加できる新規ディテール |
| `sales-stall.gltf` | 屋根付きの出店/屋台、看板付き | 社務所やお守り授与所の簡易表現、または祭りの屋台として境内の賑わい演出に転用可 |
| `lamp-post-a/b`, `bamboo`, `rock-a/b/c`, `water-lilly`, `vending-machine`, `store-sign-*`, `signs` | その他の小物 | 神社の世界観からは外れるもの(自販機等)もあるが、竹・岩・水草は境内の緑化に転用できる |

全アセットが同じ配色パレット(色分けされたテクスチャ1枚を全メッシュで共有、"color palete.png")を参照しているため、**採用したものは自動的に統一感のあるビジュアルになる**という利点もある。

### ライセンス上の注意(重要、実装前に必ず解消すること)
**このitch.ioページには、poly.pizzaの全アイテムのような明示的なライセンス表記(CC0/CC-BY等)が一切無い**。ページ本文・作者プロフィールいずれにも使用許諾条件の記載が見当たらず、価格が$0であること・「free」「free-assets」タグが付いていることだけが確認できた事実で、**「無料でダウンロードできる」ことと「商用/改変/再配布してよい」ことは法的には別**である。同じitch.ioの別パック(「Sengoku Japan Props」)には明確な利用規約(商用可・NFT等不可・クレジット任意)が書かれていたのと対照的で、本パックにはそれが無い。

このため、**実装に着手する前に以下のいずれかを満たすことを必須の前提条件とする**:
1. itch.io経由で作者Ely Devに連絡し、本プロジェクト(商用/非商用いずれか要明記)での利用・改変・再配布(モデルファイルをリポジトリにコミットすること)について許諾を得る。
2. 許諾が得られない/連絡が取れない場合は、§2.6の代替案1(プロシージャル実装)に戻す、または他のサイトでの再探索を続ける。
3. 上記いずれも整うまでは、**このパックのファイルを`models/`にコミットしない**(現時点でコミットしていない。ダウンロード先は`/tmp`の作業ディレクトリに留めてある)。

ライセンスさえ解消できれば、§3(本殿)・§6(手水舎)・§2(鳥居)・§7 隣接の太鼓橋・灯籠のいずれについても、このパック一式への切り替えで一気に完成度を上げられる非常に有力な発見であるため、**次回セッションの最優先タスクはこのライセンス確認**とする。

**(第6弾追記)** 以下§2.10の発見により、本殿についてはこちらよりもさらに有力な候補(§2.10)が見つかった。Ely Dev版は「アカウント不要ですぐ試せる」という利点は残るため、§2.10の候補が何らかの理由で使えない場合の次点候補として位置づけを変更する。

## 2.10. 第6弾での発見: BOOTH/ニコニ立体で見つかった高品質な日本製神社モデル

### BOOTH(pixiv運営、同人3Dモデルマーケット)

`https://booth.pm/ja/search/<キーワード>` は検索結果がクライアントサイドレンダリングでcurlでは取得できなかったため、WebSearchツールで実在の商品ページURLを特定し、各商品ページ(`https://booth.pm/ja/items/<id>`)を個別にcurlで確認する方式に切り替えた。商品ページのプレビュー画像(`og:image`)は直接ダウンロードでき、three.jsを使わずとも実物の見た目を確認できた。

| 商品名 | 作者 | 価格 | 形式 | 確認結果 |
|---|---|---|---|---|
| 神社（本殿） | ktgazer.org | ¥0(無料版)/¥1,000(有料版、内容同一) | Blender/FBX/UnityPackage/テクスチャ・ノーマルマップ(11MB) | **プレビュー画像を確認、非常に高品質**。無塗装の檜(白木)、一間社流造(明治～大正期の資料を参考)、千木・鰹木、精緻な組物・階段・高欄付き。§2.6で説明した「白木系」の本殿として理想的。**利用規約が本文に明記**: 「どなたでもご自由に、様々な用途にご利用ください。改変可、商用可。有料版はクレジット表記不要、無料版はクレジット表記をお願いします」——事実上CC-BY相当の明確な許諾。poly.pizza同等の安心感で採用できる |
| 神社ステージ | プリメロ工房 | ¥0(無料版、¥500は応援用で中身同一) | Blender/glb/pmx/UnityPackage(68MB、生成AI不使用と明記) | **Sketchfab埋め込みプレビューで確認、one of the best finds**。本殿+石畳の参道+朱塗りの灯籠列+しめ縄と紙垂を巻いた御神木+小さな絵馬掛け風の構造物+脇の建物まで、**単体の建物ではなく境内一式のシーン**。§9.6で計画した石畳ゾーニングと酷似した構図が最初から実現されている。**glb形式が直接含まれる**ため`GLTFLoader`にそのまま渡せる。利用規約に「個人利用だけでなく法人・集団制作OK、動画等での収益化OK、ただし配布モデル自体や改造・流用パーツの再配布/転売は禁止」と明記 |
| 神社（拝殿） | プリメロ工房 | ¥0 | FBX/Blender/テクスチャ(png)/利用規約txt | プレビュー画像確認、経年劣化した木部+緑がかった瓦の入母屋屋根+高欄付き縁側、**正面に賽銭箱が造形として最初から含まれている**(§2.8(B)で「Chest系は使えない」と結論した問題を一挙に解決)。利用規約は「本3Dモデル自体の再配布・販売」「誹謗中傷目的」「政治的利用」「宗教的利用」を禁止と明記——**「宗教的利用」の禁止条項は、本物の宗教施設として掲げる用途を指すと考えられ、ゲーム背景としての使用は該当しない可能性が高いが、解釈に迷う場合は作者に確認するのが確実** |

### ニコニ立体(ニコニコ運営、MMD/VRM向け投稿サイト)
`https://3d.nicovideo.jp/search?word=<キーワード>`(検索パラメータは`word`、`keyword`ではない点に注意)で検索でき、結果一覧はサーバーサイドレンダリングされているためcurlでも取得できた。以下がヒットしたが、**個別ページのプレビュー画像取得は今回できず(ログイン必須のため)、タイトルのみの確認**:
- 「博麗神社」「博麗神社mk2」「シンプルな博麗神社」(複数投稿者): 東方Projectの博麗神社(反魂蝶で有名な神社)を再現したモデル、複数バージョンあり
- 「烏丸神社」(烏丸ゆん、外観のみ/内装和室のみ/秘湯のみの分割版あり)、「鳥居参道」(同作者)
- 「【樹神社】鳥居【御利益有】」「【樹神社】灯籠【御利益有】」: 鳥居・灯籠単体のセット
- MMD向けモデルのため、ライセンス欄は投稿者ごとに個別設定(改変/再配布/商用利用の可否が投稿ごとに異なる)。ダウンロードにはniconicoアカウントでのログインが必須

### 共通の制約: いずれもダウンロードにアカウントが必要
**BOOTH(pixiv/BOOTHアカウント)・ニコニ立体(niconicoアカウント)とも、¥0のモデルであってもダウンロードにはサイトへのアカウント登録・ログインが必要**で、このセッションでは自動化ツール(`curl`)だけではアカウント作成(メール認証等が絡む)ができないため、**実際のダウンロード・three.jsでの目視プレビュー・サイズ計測までは完了していない**(プレビュー画像とページ本文の利用規約テキストの確認までが限界)。itch.io(§2.9)がAPI経由でアカウント無しでも直接ダウンロードできたのとは対照的。

### 次のアクション
1. **user側でBOOTHアカウント(pixivアカウントで代用可)を用意し、上記3点(特に「神社ステージ」「神社（本殿）」)を実際にダウンロードして共有していただくのが最短**。それが難しい場合は、この設計書のURLを頼りに次回セッション側でアカウント作成から行う。
2. ダウンロードできたら、通常の手順(three.jsプレビュー→バウンディングボックス確認→`models/`への配置→クレジット表記の追加)を実施する。
3. 「神社ステージ」はglb同梱なので変換作業が不要、「神社（本殿）」「神社（拝殿）」はFBXなので`GLTFLoader`ではなく別途FBXLoaderを使うか、Blenderでglb書き出しをしてから組み込む一手間が必要になる点に注意。

## 実装方針

### 1. フォールバック全廃
`buildToriiProcedural`/`buildMainHallProcedural`/`buildStatueProcedural`をすべて削除。`placeTorii`/`placeMainHall`/`placeStatues`は「フリーモデル読み込み→配置」のみとし、`.catch()`はconsole警告のみ(見た目のフォールバックは作らない)。`buildLantern`/`buildBasin`/`buildForbiddenBoundary`もプリミティブ生成をやめ、対応するフリーモデルの読み込み+配置に置き換える(関数名は`placeLantern`/`placeBasin`等に統一)。

### 2. 鳥居
- 入口(参道の起点付近)に「Japanese Torii」(Jacques Fourie)を1基、目玉として設置。
- そこから奥へ向けて「torii」(sugamo)を6〜9基、間隔を空けて連続配置し千本鳥居風の通り道を演出(`scatterProps`ではなく等間隔配置なので専用の`placeToriiRow(startZ,count,spacing)`のような関数を新設)。
- **(第2弾追加)** 各Tierの石段上り口に「torii」(sugamo)をもう1基ずつ追加(`placeTierGateTorii(tierIndex)`のような小関数で、既存の`placeToriiRow`とは別に1基だけ置くラッパーにする)。
- 回転補正: three.jsプレビューで両モデルとも正面(角度0)からアーチ越しに見通せることを確認済みだが、ゲーム内の実際のカメラ/歩行方向(`charYaw=0`が`+Z`)で最終確認し、必要なら`rotation.y`を調整する。

### 3. 本殿
**(第3弾で変更、第4弾で確定、第5〜6弾で候補比較、第7弾で最終決定)** 「Pagoda」の採用は撤回(§2.6参照、五重塔は仏教寺院建築であり神社に使えない)。§2.6(itch.io「Mini Japan」)・§2.10(BOOTHの「神社ステージ」「神社（本殿）」)を比較検討した結果、**§2.10(第7弾)の通り「nojinja01」(kisaraginonono、BOOTH item 1141801)を正式採用**した。

- **採用モデル**: `models/mainhall_free.glb`(このセッションで`nojinja01.fbx`を`assimp`でglb変換してコミット済み、492KB)。
- **ライセンス**: 「規約はなし、よしなに」(kisaraginonono表記そのまま)——改変・商用利用ともに事実上制限なし。クレジット表記は必須ではないが、`FREE_MODEL_CREDITS`には礼儀として作者名・URLを記載する。
- **見た目**: 高床式・切妻屋根の素朴な建物で、鳥居やしめ縄のような装飾は含まれない(§2.6で理想とした朱塗り+白壁の意匠ではなく、暗めの木材色)。装飾面での完成度より「軽量」「ライセンス完全に明確」を優先した判断。
- `placeMainHall(z)`は`mainhall_free.glb`の読み込み+配置のみとし、既存の当たり判定box・黒い引き戸(`MeshBasicMaterial`平面、このモデル自体に建付けの引き戸が無いため引き続き平面で表現)・人感センサーライトはそのまま流用する(バウンディングボックスは§2.9調査時点の実測値、幅×高さ×奥行 ≈ 8.2m×7.6m×10.3m相当を参考に`normalizeToHeight`後の座標を調整)。
- 本殿は最高地点のTier2に設置し、正面に鈴/鈴緒(§2.5、賽銭箱は§2.8(B)の結論により置かない方針)、周囲に玉垣(§2.8(A)のQuaternius Fenceセットで確保済み)を配置して「一番格式高い場所」であることを視覚的に明示する。
- §2.9(Ely Dev)・§2.10(BOOTHの他2点)は今回不採用となったが、密度アップ用の摂社/末社(§8)や、将来的な別マップでの再利用候補として設計書には残しておく。

### 4. 石像(神狐+台座)
**(第3弾で呼称変更)** §2.7#1の指摘により、コード上の呼称を「狛犬代替」から**神狐(しんこ)像**/「キツネの神使像」に統一する(`placeStatues`という関数名自体は狛犬/神狐どちらでも使う一般名として変更不要だが、変数名・コメント・クレジット表記(`FREE_MODEL_CREDITS`の`name`欄、現状"Statue(狛犬代替)"表記になっている箇所)は「神狐像」に直す)。「Pebble Square」(台座)+「Kurama」(キツネ)の2モデル combo は変更なし。台座を`normalizeToHeight`で低め(0.3〜0.4m程度)にスケールし地面に設置、その上面高さぶんキツネのY座標をオフセットして「台座の上に乗っている」状態を作る。左右対称に2体配置、当たり判定は台座の円コライダーを使用。実装時、モデルが何かを咥えている意匠かどうかを目視確認し(§2.7#1)、咥えていなければ無理に鍵/玉を追加造形する必要はない(神使像として成立していれば十分)。Tier1→Tier2の石段を上りきった直後(本殿参道、旧z=110〜125相当)に配置するのは変更なしだが、Y座標オフセットは§9.5の`groundHeightAt`の戻り値を基準にする(平坦地面のY=0決め打ちをやめる)。

### 5. 石灯籠
`buildLantern`を`placeLantern(x,z,mode)`に置き換え、「Toro」モデルを使用。`mode`(lit/cold/dark)による光源のON/OFF・色は現状のロジックをそのまま踏襲(モデルの色を必要ならHSL調整)。

### 6. 手水舎
「Bowl」(Poly by Google)を水盤として使用。**(第3弾追加)** §2.7#2の指摘通り、水盤だけでは「ただの水鉢」に見えるため、§2.5(B)で探す展望台/東屋(あずまや)用の「柱+切妻屋根」パビリオン系モデルが確保できたら、それを縮小して手水舎の屋根として流用する(展望台と手水舎で同じパーツを使い回すことで新規ダウンロードを1種類に抑える)。屋根付きモデルが確保できない場合のみ、水盤単体の簡略化案にフォールバックする。吊り電球+スイングアニメーションは、屋根がある場合はその軒下に、無い場合はBowlの上に浮かせる形で維持する。**(第3弾追加、§2.7#7)** 配置位置はTier1(境内)のどこでもよいわけではなく、鳥居ゲート(Tier0→Tier1の石段上り口)を抜けた直後、本殿へ向かう前の位置に固定する(参拝の作法として「清めてから向かう」順序を再現)。**(第5弾追加)** §2.9の「Mini Japan」のライセンスが解消できた場合、水盤は`water-fountain.gltf`(石造りの手水鉢そのものの形状)に差し替えるとより自然になる。ついでに同パックの`koi-pond-water.gltf`(実体は竹の鹿威し)を手水舎の脇に添えると、水回りのディテールが一段と充実する。

### 7. しめ縄境界
**(第4弾で追記)** ネットワーク復旧後に"rope"で再調査したところ「Rope」(J-Toastie、CC-BY 3.0、poly.pizza/m/dkNzi6oDQj)がヒットしたが、**地面に置いた渦巻き状(コイル状)のロープ**で、支柱間に一直線に渡す用途には形状が合わない(three.js側でジオメトリを作り替えるのは「プリミティブで作らない」方針に反するため不可)。専用の「shimenawa」も引き続き見つからず。実装時にもう一段調査(wooden post/telephone pole等)。見つかった素材の組み合わせで、既存の`buildForbiddenBoundary`と同じ「杭を並べて縄を渡す」配置ロジック(既存コードのx座標割り出し部分)を流用し、個々のプリミティブメッシュ生成だけをフリーモデルの読み込み+配置に差し替える。**(第3弾追加、§2.7#3)** 縄(フリーモデル/転用素材)だけでは「ただのロープ柵」にしか見えないため、紙垂(しで)を追加する: 薄い白色`MeshBasicMaterial`平面を数枚、縄に等間隔でぶら下げる。これは既存コードの黒い引き戸平面と同じ「単純な装飾ディテールとしての平面」の扱いであり、「オブジェクト本体をプリミティブで作らない」方針とは矛盾しない(縄本体・杭本体はフリーモデル/転用素材のまま)。同じ紙垂パーツは§8の御神木の注連縄(下記)にも使い回す。

### 8. オブジェクト密度アップ+建物の種類を増やす(第2弾で拡充)
- 石灯籠を現状6基→12〜16基程度に増量、間隔を詰めすぎない程度に参道沿いに配置。
- Kay Lousbergの小さな祠2種を、単体の目印としてだけでなく**§2.5(A)の摂社/末社セット**(祠+ミニ鳥居+ミニ灯籠2基)として2〜3箇所にまとめて配置。木立の陰・Tier1の脇広場・Tier1.5の窪地付近など、それぞれ異なる雰囲気の場所に置いて個性を出す。
- **(新規)御神木**を境内(Tier1)の広場中央付近にランドマークとして1本設置(§2.5(A))。**(第3弾で修正、§2.7#5)** しめ縄相当の表現は根元の杭リングではなく、幹の胸の高さ(1.2〜1.5m相当)に巻き付ける形に変更し、§7で追加する紙垂パーツを一緒に垂らす(根元の杭は使わないか、別用途の低い境界柵に転用する)。
- **(新規)鳥居ゲート**を各Tier間の石段の上り口に追加し、「登るたびに鳥居をくぐる」多層感を演出(§2、実質コストはtorii_row_freeの追加配置のみ)。
- **(第3弾で優先度を下げる、§2.7#6)** kadomatsu(門松)は本来正月(12/26頃〜1/7頃)限定の飾りで、常設だと季節感が不自然になる。ゲームに季節/期間イベントの概念が無い前提では入口1組程度に留めるか、そもそも省略して案内看板(§2.5(B))や常夜灯に差し替える。既存計画の「2〜3組に増量」は撤回。
- 岩・茂みの`scatterProps`呼び出し個数を、拡張後の面積に比例して増やす(下記マップ拡張とセットで再計算、面積比に比例させすぎるとオブジェクト数が過大になり負荷が上がるため、密度は現状と同程度〜1.5倍程度に留め、単純に面積拡張分は「歩く距離が伸びる」効果を主として狙う)。
- **(第4弾で確定)** §2.8(A)で確保できた石段装飾(Stairs/Steps by Kenney)・太鼓橋(Small Bridge by Quaternius)・玉垣(Fence各種 by Quaternius)・案内看板(Signpost by Kenney)は、下記§9のレイアウトに沿って各Tier・各支道にそのまま配置してよい状態(要ダウンロード・実装組み込みのみ)。展望台/東屋・鈴/鈴緒・崩れた祠・篝火は§2.8(B)の通りまだ見た目の合う候補が無いため、実装セッションで再調査(次点候補または§2.6と同様のプロシージャル妥協案)してから配置する。**「同じアセットの複製で物量を稼ぐ」よりも「その場所だけの一点物を増やす」ことを優先する**——ユーザー指摘の"物量が少なく寂しい"は、単純な密度不足だけでなく建物・オブジェクトの**種類の少なさ**が主因と判断したため。

### 9. マップ面積の拡張+高低差の導入(現状比 面積約8〜10倍、Y方向に3段の高低差)
現状: 参道z=-3〜43(全長46m)× 参道幅15m(`CORRIDOR_HALF_W=7.5`、東側小広場のみ14m張り出し) ≒ 約740㎡、高低差なし(全域Y=0)。

**第2弾での変更点**: 前回計画は「一直線の参道を伸ばして幅も広げる」だけだったため、ユーザー評価では「広いのに空虚」という印象になった。今回は同程度の面積拡張を維持しつつ、**Y方向に3段の高低差**を追加し、各Tierの入口・脇道に上記§2.5/§8の新規オブジェクトを配置することで「歩くたびに景色と地形が変わる」構成にする。

新レイアウト(全体はz軸に沿って奥へ進む一本道構成を維持しつつ、Y座標(標高)が段階的に変化する):

```
z = -5             参道開始(境界外扱いの余白)                          Y=0.0  (Tier0)
z = 0 〜 55         千本鳥居風の連続する小鳥居ゾーン(sugamoの鳥居 x6〜9)     Y=0.0  (Tier0)
z = 60             目玉の鳥居(Japanese Torii、豪華な一の鳥居)              Y=0.0  (Tier0)
z = 63 〜 68        石段1: Tier0→Tier1へ上る(石段モデル+上り口に鳥居ゲート)   Y=0.0→1.8 (スロープ)
z = 68 〜 100       境内(石灯籠列・手水舎・御神木・摂社セット1〜2箇所)         Y=1.8  (Tier1)
  └ z=80〜92の東側支道  小さな池+太鼓橋(§2.5B)。境内から少し外れた寄り道、水面すれすれの低い区画  Y=1.4 (Tier1内の窪み)
z = 100 〜 103      石段2: Tier1→Tier2へ上る(石段+鳥居ゲート)               Y=1.8→3.3 (スロープ)
z = 103 〜 118      本殿への参道(玉垣で囲われた区画、キツネ像+台座を対で配置)   Y=3.3  (Tier2)
z = 120             本殿(§2.6/§2.8の代替案1をプロシージャルで本採用、引き戸のみJapanese Door流用)+正面に鈴/鈴緒(賽銭箱は置かない)  Y=3.3  (Tier2)
  └ 西側支道        展望台/東屋への短い脇道(高台からの見下ろし、行き止まりのごほうびスポット) Y=3.3〜3.6
z = 123 〜 128      石段3: Tier2→Tier1.5へ下る(あえて下らせて不穏さを演出)     Y=3.3→1.3 (スロープ)
z = 128 〜 150       奥の院手前の窪地(疎らな枯れ木・崩れた祠を1つだけ、無灯)      Y=1.3  (Tier1.5)
z = 150 〜 178      奥の院(無灯・木立を疎らに・張り詰めた空気)                 Y=1.3  (Tier1.5)
z = 181             しめ縄+木杭の禁足地境界(現状通りハード境界)                Y=1.3
```

全長 ≈ 186m(現状46mの約4倍)。`CORRIDOR_HALF_W`を7.5→16(全幅32m、現状15mの約2.1倍)に拡張、東西の支道(池+太鼓橋、展望台)は一時的にさらに5〜8m張り出す。主要footprint面積 ≈ 186×32 ≈ 5,952㎡ ≒ 現状の約8.0倍。支道2本の追加分(それぞれ概算150〜250㎡)を加えると約8.4倍相当。「10倍くらい」の指示に対し概算で妥当な範囲(前回計画からの純増分は主に支道と各Tierの脇スペース)。

高低差の設計意図:
- 本殿(Tier2)を全体の最高地点にすることで、「奥に進むほど格式が上がり、地形も登っていく」という実在の神社の体感(伏見稲荷・多くの山間の神社)を再現する。
- 本殿から奥の院にかけては逆にY座標を下げ(Tier2→Tier1.5)、「格式の頂点を過ぎたら、急に不穏な窪地に入っていく」という高低差そのものをホラー演出(ゴーストコントローラーのトーンに合致)として使う。
- 池+太鼓橋、展望台の2支道は「本道から外れないと見られない景色」を作ることで、ただ広いだけでなく探索の動機になる寄り道を用意する。

`corridorBoundsAt(z)`の東側張り出し小広場は、上記の池+太鼓橋支道(z=80〜92)の座標に合わせてスライドし、西側には新たに展望台支道(本殿付近)を追加して、東西1箇所ずつの支道にする(前回計画の「東側のみ2箇所」から「東西1箇所ずつ」に変更、地形演出の観点でも東西に振れ幅があった方が単調にならない)。

`PATH_MIN_Z`/`PATH_MAX_Z`/`MAP_RADIUS`等の定数、`groundGeo`(現状100x120のPlaneGeometry)のサイズも新しい全長・全幅に合わせて拡大する。

### 9.5 高低差の実装方式(コード対応方針、新設)
現状のコードはY方向の変化を一切前提にしていないため、以下の対応が必要:

1. **地面ジオメトリ**: 単一の平坦`PlaneGeometry`をやめ、十分な分割数(segments)を持たせた`PlaneGeometry`の頂点を走査して`vertex.y = groundHeightAt(vertex.x, vertex.z)`で変位させ、`computeVertexNormals()`で法線を再計算する方式を推奨(継ぎ目のない1枚の地形になり、Tierごとに別メッシュを作って繋ぎ合わせるより実装・保守が楽)。
2. **`groundHeightAt(x,z)`関数(新設)**: 基本はzのみで決まる区分関数(上記§9の表のY値)で、Tier間は`smoothstep`等でスロープ状に線形/滑らかに補間する。池+太鼓橋・展望台の2支道だけはx座標も見て、支道のバウンディングボックス内にいる場合のみ個別のオフセット(池は少し低く、展望台は少し高く)を返す特殊分岐を追加する。
3. **キャラクターのY追従**: `charPos`(`THREE.Vector3`)は既にY成分を持つが常に0固定になっている(`charPos.set(x,0,z)`/`characterRoot.position.set(charPos.x,0,charPos.z)`)。`resolveCollision(charPos)`でx/zを補正した後に`charPos.y=groundHeightAt(charPos.x,charPos.z)`を追加し、`characterRoot.position.set(charPos.x,charPos.y,charPos.z)`に変更する。カメラターゲット(`_camTarget.set(charPos.x, charPos.y+charCenterY+charHeight*FRAME_BIAS_FRAC, charPos.z)`)は既に`charPos.y`を加算する式になっているため、この変更だけで自然に追従する。
4. **設置物のY座標**: `placeTorii`/`placeLantern`/`placeMainHall`等、全ての`place*`関数は現状「y=0の地面」を前提にローカルなY量(灯籠の高さオフセット等)を加算している。これを`const baseY=groundHeightAt(x,z); obj.position.set(x,baseY+localOffset,z)`のパターンに統一する。
5. **当たり判定**: `colliders`/`resolveCollision`/`corridorBoundsAt`はx/z平面のみで判定する現行方式を維持してよい(Y方向は見た目の追従のみで、ゲームプレイ上の当たり判定は従来通り2D平面で十分——スロープ上を含め、あるz/x位置にいるかどうかで衝突を判定する設計は変えない)。
6. **石段の3Dモデル(装飾)とスロープ(歩行面)の関係**: 実際に段差のあるジオメトリの上を1段ずつ歩かせる実装(足のIKや段差ごとの当たり判定)は過剰実装になるため行わない。**見た目は石段モデルを配置しつつ、実際にキャラクターが乗る歩行面は滑らかなスロープ**(`groundHeightAt`のsmoothstep補間)にする、という「見た目と当たり判定を分離する」よくある簡略化パターンを採用する。石段モデルはスロープにぴったり沿うよう、上り口・上り切った位置のYが両端のTierのYと一致するようスケール・配置する。
7. **カメラオクルージョン**: `_camRaycaster`によるカメラ-キャラクター間の遮蔽判定はワールド座標のレイキャストなのでY方向の変化にも自然に対応できるはずだが、坂を上りながらのカメラ距離感は実機確認が必要(急に地面が迫るような見え方になっていないか等)。

### 9.6 地面素材(石畳/玉砂利/土、第3弾で新設)
現状の地面(`groundGeo`+`groundMat`)は`makeGroundTexture()`という**プロシージャルなcanvas生成テクスチャ**(暗いベースカラーに明暗の斑点を散らしただけの簡易な砂利風テクスチャ)を1枚、地面全体に単純リピートしているだけで、ゾーンによる素材の違いが一切ない。userの「石畳とか砂利とか普通の地面とか場所に応じて」という指摘はこの点を指している。

**方針**: 新規テクスチャ画像のダウンロードには頼らない(前述の通りネットワーク越しの素材サイトが不安定なため)。幸い、既存の`makeGroundTexture()`自体が外部画像に依存しないプロシージャルなcanvas生成なので、**同じ手法を踏襲して素材違いのバリエーションを複数用意する**のが最も低リスク:

1. **`makeGravelTexture()`(玉砂利、境内Tier1全般のデフォルト)**: 現行の`makeGroundTexture()`をベースに、ベースカラーを明るいグレー(`#8a8478`前後、白っぽい砂利色)にし、斑点の密度・粒サイズを上げて「敷き詰めた砂利」らしい荒さを出す。
2. **`makeStonePathTexture()`(石畳、参道Tier0の中央帯+石段の踏面)**: canvas上に矩形(正方形〜長方形)の敷石をグリッド状に描き、目地(すきま)を暗い線で表現する簡易パターン(`ctx.strokeRect`の繰り返しで十分)。中央の敷石帯は実際の参道作法(正中を石畳、左右を玉砂利にする形式)に合わせ、参道(Tier0)は**中央の狭い帯だけ`makeStonePathTexture()`、その両脇は`makeGravelTexture()`**という2テクスチャ構成にする。
3. **`makeDirtMossTexture()`(土/苔混じり、奥の院Tier1.5)**: ベースカラーを茶色〜暗緑(`#3a3428`前後)にし、苔を思わせる緑がかった斑点と、土のひび割れ風の暗い線をランダムに混ぜる。「格式の頂点(本殿)を過ぎて、手入れが行き届かなくなっていく」という§9の高低差の演出意図(不穏さの強調)と自然に合致する。

**実装方式(ゾーニング)**: §9.5で地面を「頂点変位で一枚に繋がったジオメトリ」にする方針を既に決めているため、素材のゾーニングは**地面メッシュをz(またはx/z)の範囲でいくつかのパーツに分割し、パーツごとに上記のテクスチャを割り当てたマテリアルを使う**方式にする(1枚のメッシュに対して複数マテリアルを頂点グループで塗り分ける高度な手法は使わず、単純に「参道用の細長いPlaneGeometry」「境内用の広いPlaneGeometry」「奥の院用のPlaneGeometry」のように地面自体を数枚に分けて、それぞれ独立に`groundHeightAt`で頂点変位する)。境目はぴったり隙間なく並べる(隣接するメッシュの端の頂点Yが一致するよう、共通の`groundHeightAt`関数から計算する)ほか、境目上に灯籠・祠・岩などの既存の散布オブジェクトを意図的に配置して視覚的にごまかす(これは`scatterProps`が既にやっていることの応用)。

ゾーニング対応表(§9のレイアウトに対応):
| ゾーン | z範囲(目安) | 地面テクスチャ |
|---|---|---|
| Tier0 参道(中央帯) | z=-5〜63 | 石畳(`makeStonePathTexture`) |
| Tier0 参道(左右) | z=-5〜63 | 玉砂利(`makeGravelTexture`) |
| 石段1〜3(踏面) | 各スロープ区間 | 石畳(`makeStonePathTexture`、段差装飾モデルと合わせて使用) |
| Tier1 境内 | z=68〜100 | 玉砂利(`makeGravelTexture`) |
| Tier2 本殿周辺 | z=103〜128 | 玉砂利(`makeGravelTexture`)、本殿真正面のみ石畳を敷いてもよい(実物の拝殿前によくある形式) |
| Tier1.5 奥の院手前の窪地・奥の院 | z=128〜181 | 土/苔混じり(`makeDirtMossTexture`)、手入れの行き届かなさを演出 |
| 池+太鼓橋・展望台の支道 | 各支道 | 玉砂利(境内側の地続きとして自然になるよう本道と合わせる) |

### 10. パフォーマンス配慮
面積・オブジェクト総数が増えるため、前回対応した軽量化(ライト数7灯維持、PIPのレンダーターゲットキャッシュ、キャラクター非ライティング化)は崩さない。新規オブジェクトの光源(灯籠の炎、祠のろうそく、篝火等)は現状同様「本当に光らせるのは一部だけ」の方針を維持し、ライト総数が大きく増えないよう調整する(篝火を追加するなら、灯籠の常時点灯数を1〜2基減らす等でバランスを取ってもよい)。木・岩と同様、大きな新オブジェクト(灯籠・祠・キツネ像・御神木・展望台等)にも当たり判定を付け、カメラの遮蔽レイキャスト(`shrineGroup`に対する既存のカメラオクルージョン処理)がそのまま機能することを確認する。地面ジオメトリを頂点変位方式にする場合、分割数を上げすぎるとポリゴン数が増えるため、視認できる起伏の滑らかさとのバランスを見て分割数を決める(区分ごとのY変化はなだらかなスロープのみなので、極端に高い分割数は不要なはず)。

## マップ分離アーキテクチャ(複数マップ対応、第3弾で新設・計画のみ)

userから新規要望:「マップはコントローラーhtmlに埋め込むんじゃなくて、別ファイルにして呼び出すようにしてね。将来的に複数マップ対応するつもりです」。これは神社の見た目とは独立した、独立したエンジン側の要件のため、独立した章として計画する。**今回はこの章の設計のみ行い、実装(実際のファイル分割)はまだ行わない。**

### 現状の結合度合い(なぜ分離が必要か)
`controller.html`は現在、非モジュールのクラシック`<script>`(`js/vendor/three.min.js`→`js/vendor/GLTFLoader.js`→`js/idb.js`→本体の無名関数IIFE1本)という構成で、神社固有のコード(`buildToriiProcedural`〜`updateShrineEffects`、`groundGeo`/`groundMat`/`makeGroundTexture`、`shrineGroup`、`colliders`、`corridorBoundsAt`、`FREE_MODEL_CREDITS`への直接push等、§「現状の実装」に列挙した一式)が、キャラクター読み込み・移動・カメラ・PIP・configパネルといった**マップに依存しない汎用ロジック**と同じ1つのIIFE・同じ1つのファイルの中で密結合している。このままだと「別マップを追加する」= 「同じ巨大ファイルの中に神社用コードと並べて別マップ用コードを書き足し、if分岐で切り替える」形になり、マップが増えるたびにファイルが肥大化し、かつ一度に全マップ分のコード・扱いによっては将来のマップ専用アセットまで読み込むことになりかねない。

### 方針: マップモジュールのインターフェースを定義し、controller.htmlを「汎用ホスト」にする
- 新規ディレクトリ`maps/`を作り、神社は`maps/shrine.js`として切り出す(既存の`models/`(GLBアセット)、`js/`(共通ヘルパー)と並ぶ位置づけ)。
- プロジェクトが現状ESモジュールを使わずクラシックスクリプト+グローバル名前空間パターン(`js/idb.js`の`global.P3D=...`と同じ書き方)を採っているのに合わせ、`maps/shrine.js`も同じ流儀で書く:
  ```js
  (function(global){
    "use strict";
    const GhostMaps = global.GhostMaps = global.GhostMaps || {};
    GhostMaps.shrine = {
      id: 'shrine', title: '神社',
      build(env){
        // env = { THREE, scene, colliders, credits, loadStaticGLB, normalizeToHeight }
        // ここに現状controller.html内にある神社構築コード一式(shrineGroup生成、
        // placeTorii/placeLantern/placeMainHall/placeStatues/placeBasin/
        // buildForbiddenBoundary、§9.6の地面ゾーニング、scatterProps呼び出し等)を丸ごと移植する。
        // colliders/creditsは呼び出し元から渡された配列に直接pushする(既存コードの
        // `colliders.push(...)`/`FREE_MODEL_CREDITS.push(...)`と書き方は変わらない)。
        return {
          group: shrineGroup,               // 現状のshrineGroupをそのまま返す
          spawnPoint: {x:0, z:-3, yaw:0},    // 参道の入口
          corridorBoundsAt(z){ /* 現状のcorridorBoundsAtの中身 */ },
          groundHeightAt(x,z){ /* §9.5で新設する関数の中身 */ },
          update(dt, charPos){ /* 現状のupdateShrineEffects(dt)相当、必要ならcharPosも使えるようにする */ },
        };
      }
    };
  })(window);
  ```
- `controller.html`側は「汎用ホスト」として以下だけを持つ: レンダラー/シーン/汎用ライト、キャラクターの読込・移動・アニメーション、3人称/自撮り/POVカメラ、configパネル(霧・明るさ・FPS上限・PIP画質)、`loadStaticGLB`/`normalizeToHeight`(汎用アセットローダー、マップ間で共有)、`colliders`/`credits`の実体配列、`resolveCollision`(x/z当たり判定の解決自体は汎用ロジックなので残す)。
- 現在マップ固有ロジックが直接触っている箇所は、**「現在アクティブなマップの返り値オブジェクト経由」に置き換える**:
  - `resolveCollision(pos)`内の`corridorBoundsAt(pos.z)`呼び出し → `currentMap.corridorBoundsAt(pos.z)`
  - 新設予定の`charPos.y=groundHeightAt(...)`(§9.5) → `currentMap.groundHeightAt(charPos.x,charPos.z)`
  - `updateShrineEffects(dt)`の毎フレーム呼び出し → `currentMap.update(dt,charPos)`
  - カメラオクルージョンの`_camRaycaster.intersectObject(shrineGroup,true)` → `_camRaycaster.intersectObject(currentMap.group,true)`
  - `FREE_MODEL_CREDITS`/`colliders`はホスト側の実体配列をそのまま`env`経由でマップに渡すので、クレジットUI(`refreshCreditsUI`)側の変更は不要。

### 複数マップ対応に向けたロード方式(マニフェスト+動的読み込み)
神社1個だけなら`<script src="maps/shrine.js">`を静的に置くだけで済むが、「将来的に複数マップ対応するつもり」という要望を汲み、**マップが増えても初期ロードで全マップ分のコードを読み込まずに済む**構成を最初から用意しておく:
- `maps/manifest.js`(常に読み込む、軽量): `window.GHOST_MAP_MANIFEST=[{id:'shrine',title:'神社',file:'maps/shrine.js'}, ...]`という配列だけを持つ。マップ選択UIができた際、この一覧を表示に使う(現時点ではマップが1つなので選択UIは不要、神社を決め打ちで読み込む)。
- ホスト側に`loadMapScript(path)`という小さなヘルパーを新設: `document.createElement('script')`でタグを動的挿入し、`onload`でPromise解決する(現状`<script src=...>`を静的に並べているのと違い、「選ばれたマップのファイルだけを必要になった時に取得する」ため)。
- 起動シーケンス: `manifest.js`読込→(現時点ではマップ選択UI無しなので)`GHOST_MAP_MANIFEST`から`id==='shrine'`を探して`loadMapScript(その.file)`→ロード完了後`window.GhostMaps.shrine.build(env)`を呼んで`currentMap`にセット→以降は`currentMap`経由で動かす。
- 2個目以降のマップを足す時の作業は「`maps/<新マップ>.js`を同じインターフェースで書く」+「`maps/manifest.js`に1行追加」だけで済み、`controller.html`側(ホスト)には触れずに済む、という状態を目標にする。

### 移行時の注意
- `groundGeo`/`groundMat`/`makeGroundTexture`(および§9.6で追加する`makeGravelTexture`/`makeStonePathTexture`/`makeDirtMossTexture`)は地面の見た目がマップごとに大きく異なりうるため、**ホスト側ではなくマップモジュール側**に持たせる(マップの`build(env)`が自分の地面メッシュを組み立てて`env.scene`に追加する)。
- 段階的移行が可能な設計にする: 一気に全部切り出すのではなく、まず神社一式を`maps/shrine.js`に移してホストとのインターフェースを固め、動作確認が取れてから新規カテゴリ(§2.5/§2.6/§2.7/§9.6の追加分)をそちらに実装していく、という順番が安全(既存の「神社が動いている状態」を壊さずに移植できたことを先に確認できる)。
- この章の変更は`controller.html`のコード構造全体に関わるため、実装時は神社の見た目の変更(本殿差し替え・地面素材・高低差等)とは別コミットに分けることを推奨(レビューしやすくするため)。

## 参照する既存コード
`controller.html`: `loadStaticGLB`/`normalizeToHeight`(モデル読込・正規化)、`colliders`/`resolveCollision`/`corridorBoundsAt`(当たり判定・境界)、`FREE_MODEL_CREDITS`/`refreshCreditsUI`(クレジット表示)、`_camRaycaster`によるカメラオクルージョン処理、`scatterProps`(散布ヘルパー、鳥居列配置にも同様のパターンを流用)、`charPos`/`characterRoot`/`_camTarget`(高低差対応で修正対象、§9.5参照)。**(第3弾追記)** これらのうち神社固有の部分(`corridorBoundsAt`/`FREE_MODEL_CREDITS`へのpush元/`shrineGroup`関連)は「マップ分離アーキテクチャ」章の方針に従い`maps/shrine.js`に移す対象、`loadStaticGLB`/`normalizeToHeight`/`colliders`実体/`_camRaycaster`/`charPos`/`characterRoot`/`_camTarget`はホスト(`controller.html`)に残る汎用部分。

## フリーモデルを効率的に探す方法(今後のセッション用メモ)

1. **主戦場は poly.pizza**(https://poly.pizza)。ログイン不要、CC0/CC-BYが明記され、`.glb`への直リンクが`https://static.poly.pizza/<uuid>.glb`の形でページのraw HTML内に直接埋め込まれている(`curl`で取得したHTMLから`grep -o 'https://static.poly.pizza/[^"]*\.glb'`で一発抽出可能)。検索は`https://poly.pizza/search/<query>`をcurlで取得し、`grep -oE 'href="/m/[^"]*"'`で候補一覧のID(`/m/<id>`)を列挙→各IDのページをcurlしてタイトル(`og:title`)とライセンス(`CC0`/`CC-BY`の文字列)を確認、というワークフローが機械的に回せる。**ただしセッションのネットワークポリシー次第でpoly.pizzaへの直接アクセスがブロックされる場合がある(第2弾・第3弾のセッションはいずれもこれで、userが環境のネットワーク設定を変えたと認識していた第3弾ですら`curl`・`WebFetch`とも403で拒否された)。その場合はまずWebSearchツール(検索結果の要約のみでページ本文は取れない)でタイトル・作者名の当たりを付け、直接アクセス可能な別セッション/環境で最終確認する**。
2. **ファイルサイズは`curl -sSI`(HEADリクエスト)の`content-length`で、ダウンロード前に確認できる**。
3. **見た目は必ず目視確認する**(タイトルや説明文だけで判断しない)。このセッションでは`/tmp`配下に軽量なthree.jsプレビューHTML(`preview.html`)+ Playwright実行スクリプト(`preview_run.js`)を即席で作り、候補モデルをGridHelper付きシーンにロードしてスクリーンショットを撮る、という手法で多数の候補を効率よく目視スクリーニングした。**モデルの奥行きが極端に薄い/巨大すぎるスケール(数千〜数万単位)の場合があるので、カメラのnear/farをモデルの実測バウンディングボックスに応じて動的に設定する**(固定near/farだと真っ黒/何も映らない画面になることがある)。
4. **プレビュー用HTTPサーバは本番のリポジトリディレクトリを汚さないよう、`/tmp`配下の隔離されたディレクトリで別ポートに立てる**(このセッションでは8934=リポジトリ本体、8935=プレビュー専用の一時モデル置き場、で完全に分離した)。plan modeなど「リポジトリに書き込み禁止」の制約下でも安全に候補評価ができる。
5. **有力な作者を覚えておくと検索が速い**: Quaternius・Kay Lousberg・Poly by Google(Googleの旧Poly資産)は軽量・CC0/CC-BYが多く実績あり。日本語ローマ字名のユーザー(例: sugamo)は和風モチーフの投稿が多い傾向があった。
6. **itch.ioやCGTrader/TurboSquidのアセット"パック"は基本的に有料、かつ数百MB級で「軽量」に反するため、このプロジェクトの用途では基本スキップしてよい**(Sketchfabの無料モデルはライセンスが緩い場合もあるが、ダウンロードにログインが要ることが多く、4Kテクスチャ2種同梱など重いことも多いので個別に要検証)。
7. **検索語は日本語ローマ字/英語両方、かつ対象そのものだけでなく類語・上位/下位概念でも試す**(例: 「komainu」で見つからなければ「guardian dog」「fox statue」、「shrine」で理想のものがなければ「pagoda」「temple」、「shimenawa」がなければ「rope」「stone platform」等)。**ただし§2.6の教訓の通り、上位概念に広げすぎて建築カテゴリが変わってしまう候補(神社→寺院建築のpagoda等)を採用しないよう、横展開した候補は必ず「本来のカテゴリの特徴(神社なら平屋・切妻/流造)を満たしているか」を目視で確認する**。今回はこの横展開で台座(pebble square)を発見できた。**第2弾では同様の横展開を石段・橋・柵・鈴等にも適用する(§2.5(B)の検索キーワード案を参照)**。
8. **(第3弾で追加)`github.com`/`raw.githubusercontent.com`は、poly.pizza等が組織ポリシーでブロックされているセッションでも到達できることがある**(このプロジェクトのGitHub連携範囲内であることが前提)。CC0アセットをまとめたGitHubのawesome-list(例: `madjin/awesome-cc0`)や、glTFサンプルを配布しているリポジトリ経由で素材そのもの、またはより一般的な素材サイトへの間接的な手がかりを得られる場合がある。今回この経路で和風建築の直接ヒットは見つからなかったが、テクスチャ系(gravel/stone等)は今後試す価値がある。
9. **(第4弾で追加)特定の作者が和風モチーフを他にも投稿していないか、投稿者プロフィールページ(`https://poly.pizza/u/<作者名(URLエンコード)>`)を丸ごと確認するのが効率的**。`grep -oE 'href="/m/[^"]*"'`でその作者の全投稿IDを一括抽出できる(検索ワードに引っかからない意外な和風アセットが見つかることがある。ただし今回sugamo・Jacques Fourie・Hattie Stroud・Flopsi・Aimi Sekiguchiを確認した範囲では本殿級の建物は無かった)。
10. **(第4弾で追加)大量の候補を評価する時は「検索結果ページから全IDを収集→`xargs -P <並列数>`でタイトル取得を並列化→タイトルをキーワードでgrep」の3段階に分けると効率がよい**(このセッションでは359個のIDを並列12でタイトル取得→キーワードgrepという流れで数分程度に収まった)。ダウンロード・three.jsプレビューは有望と判断できたものだけに絞って行う(全件プレビューは非効率)。
11. **(第4弾で追加)`quaternius.com`や`kenney.nl`など作者本人のサイトは、poly.pizzaに載っていない新しいパックがある一方、配布形式がZIPのパック単位になっていることが多く、poly.pizzaのような個別モデルのプレビュー/ダウンロードができない**。中身を見るにはZIPを実際にダウンロードして展開する一手間が必要(今回は`quaternius.com`のパック一覧(80種以上)のタイトルだけ確認し、`ultimatemodularruins`(廃墟)や`sushirestaurantkit`(寿司屋)等、名前的に転用できそうなパックを次回候補としてメモするに留めた)。
12. **(第5弾で追加)`itch.io`は「free game assets tagged <キーワード>」(例: `https://itch.io/game-assets/free/tag-japan`)で無料アセット一覧を検索できる。ヒット後の各作品ページに`.gltf`/`.fbx`/`.obj`同梱のZIPが無料公開されていることがあり、poly.pizzaに無い掘り出し物が見つかることがある**(今回の「Mini Japan」がまさにこれで、本殿問題を解決する決定的な発見になった)。ただし**itch.ioの各作品ページにライセンス表記(CC0/CC-BY等)が無いことが多い**(ある場合とない場合があり、無い場合は「無料公開されている」ことと「自由に使ってよい」ことは別問題なので、実装前に作者への確認を必須とする、§2.9参照)。
13. **(第5弾で追加)itch.ioはログイン無しでも、ブラウザを使わず`curl`だけで無料アセットをダウンロードできる**: 作品ページのHTMLから`csrf_token`メタタグの値を取り、`POST https://<sub>.itch.io/<slug>/download_url`に`csrf_token=<値>`を送ると一時ダウンロードページのURLが返る→そのページのHTMLから新しい`csrf_token`を取り直し、`POST https://<sub>.itch.io/<slug>/file/<upload_id>`(`upload_id`はダウンロードボタンの`data-upload_id`属性)に送ると、Cloudflare R2の署名付き直URL(60秒程度で失効)がJSONで返る、という2段階のAPI呼び出しで完結する。
14. **(第5弾で追加)`sketchfab.com`/`www.cgtrader.com`はクライアントサイドレンダリング(SPA)のため、`curl`は空のHTMLしか返さない。Playwrightで実ブラウザを使っても`sketchfab.com`は接続がリセットされ(ボット対策と見られる)、検索結果の取得に失敗した**。`free3d.com`/`renderhub.com`/`turbosquid.com`/`cults3d.com`は`curl`が403で弾かれる。これらのサイトを調査対象にする場合は、実ブラウザでの手動確認、または別の到達経路を検討する必要がある(今回は断念)。ただし個別の商品/モデルページ(検索結果ページではない)は、`sketchfab.com/models/<id>`のように直接URLが分かっていれば`curl`でも取得できることがある(`og:image`からプレビュー画像も直接ダウンロードできた)。
15. **(第6弾で追加)日本語圏のサイトは`WebSearch`で商品名を先に見つけ、個別ページを`curl`で確認する流れが機械的に回せる**。`booth.pm`の検索結果ページ自体はクライアントサイドレンダリングで`curl`が使えない(`https://booth.pm/ja/search/<キーワード>`は空振り)が、`WebSearch`で「booth.pm 神社 3Dモデル 無料」等を検索すると実在の商品ページURL(`https://booth.pm/ja/items/<id>`)が見つかり、そのページ自体は`curl`で取得可能(`og:image`からプレビュー画像、本文からタイトル・価格・利用規約テキストも取得できた)。`3d.nicovideo.jp`(ニコニ立体)は検索パラメータが`word`(`keyword`ではない)で、検索結果一覧はサーバーサイドレンダリングのため`curl`で直接取得できる。
16. **(第6弾で追加、重要な制約)BOOTH・ニコニ立体はいずれも¥0のモデルであっても、実際のダウンロードにはサイトのアカウント登録・ログインが必要**(itch.ioの`download_url`API(§8番参照)のようなログイン不要の直接ダウンロード経路は無い)。メール認証等が絡むアカウント作成は自動化ツールだけでは完結できないため、**この種のサイトでは「商品ページの本文・プレビュー画像までは確認できるが、実ファイルのダウンロード・three.jsでの目視確認まではuser側のアカウントが無いと完了できない」という限界がある**ことを踏まえて調査計画を立てる。

## 検証方法
1. `python3 -m http.server`でリポジトリ配信、Playwrightで`controller.html`サンプルモードを開く。
2. 各新規オブジェクト設置点へ`window.__ghostController.setCharPos(x,z)`でテレポートしスクリーンショット確認(鳥居列、目玉鳥居、Tierゲート鳥居、本殿、キツネ像+台座、灯籠、摂社セット、御神木、池+太鼓橋、展望台、祠、境界)。
3. 鳥居の向き: 参道を実際にWキーで歩いて各鳥居に正面から近づき、アーチをくぐる形で見えるか(横向きになっていないか)をスクリーンショットで確認。
4. 新しい`corridorBoundsAt`の境界・当たり判定(大きな岩・灯籠・祠・キツネ像の台座・御神木・展望台の縁)が機能し、キャラ/カメラがオブジェクトに埋まらないことを確認(前回と同じ手法:カメラオクルージョンレイキャストの動作確認込み)。
5. **(第2弾追加)** 各石段区間(Tier0→1、Tier1→2、Tier2→1.5)を実際に歩いて上り下りし、`charPos.y`が`groundHeightAt`に沿って滑らかに変化すること、キャラクターが地面に沈んだり浮いたりしないこと、カメラが坂の途中で不自然に地面にめり込まないことを確認。
6. **(第2弾追加)** 池+太鼓橋・展望台の2支道を実際に歩いて到達できること、行き止まりで違和感がないこと(展望台からの見晴らしが実際に良い構図になっているか)を確認。
7. 面積拡張後もフレームレート体感が大きく落ちないこと(ライト数・オブジェクト総数の増加が過大でないか)を確認。
8. configパネルのクレジット欄に新規使用モデル(§2.5含む)がすべて表示されること。
9. 読み込み失敗時(意図的にURLを壊すテスト)にフォールバックなしで単にそのオブジェクトが出ないだけであること(procedural fallbackが本当に無くなったこと)を確認。
10. **(第3弾追加)** 本殿の見た目が「塔のように何層にも屋根が重なっていないか」「平屋の切妻/流造シルエットになっているか」を実機スクリーンショットで確認する(§2.6、本殿問題の再発防止チェック)。
11. **(第3弾追加)** 地面の素材が§9.6のゾーニング通りに切り替わっていること(参道中央=石畳、参道左右・境内=玉砂利、奥の院=土/苔)をTier0→Tier1.5まで歩いてスクリーンショットで確認し、ゾーン境界に不自然な隙間・ズレが出ていないことも確認する。
12. **(第3弾追加)** §2.7のプロレビュー是正点を個別確認: しめ縄・御神木の紙垂が見えるか、神狐像まわりのUI/クレジット表記に「狛犬」という語が残っていないか、手水舎が鳥居ゲート直後(本殿より手前)に配置されているか、門松が1組程度に抑えられているか。
13. **(第3弾追加、マップ分離アーキテクチャ実装時)** `maps/shrine.js`を`maps/manifest.js`経由で動的読み込みした後も、既存の全機能(鳥居・本殿・キツネ像・当たり判定・高低差・地面素材・クレジット表示)が分離前と同じ見た目・挙動で動くことを確認する(リグレッションが無いことの確認が最優先、分離作業自体は見た目を変えないリファクタであるべき)。
14. **(第4弾追加)** §2.8(A)で確定した新規モデル(石段装飾・太鼓橋・玉垣・案内看板・鳥居/灯籠バックアップ・本殿の引き戸)が実機で狙い通りの見た目になっているか(特に本殿の引き戸は「Japanese Door」に差し替わっており黒い平面のままになっていないか)を確認。
15. **(第4弾追加)** 本殿正面に賽銭箱が置かれていないこと(§2.8(B)の結論通り、無理な代替を置かない方針が実装にも反映されていること)を確認。
16. **(第5弾追加、最優先)** §2.9の「Mini Japan」パック(Ely Dev)を本殿等に使う前に、**作者からの利用許諾が実際に得られているか**をまず確認する(コミットログや本ドキュメントの更新履歴に許諾のやり取り・確認結果を残す)。許諾が確認できていない状態で`models/`にコミットしない。
17. **(第6弾追加、最優先)** §2.10のBOOTHモデル(「神社ステージ」「神社（本殿）」「神社（拝殿）」)を採用する場合、**実際にダウンロードした実ファイルでthree.jsプレビューを行い、プレビュー画像だけでは分からなかった問題(ポリゴン数過多、意図しないマテリアル、スケール異常等)が無いかを確認**する(このセッションではアカウント未保有のためプレビュー画像・利用規約テキストの確認までしかできていない)。FBX形式のものはBlender等で一度glTF/glbに変換してから`GLTFLoader`に渡す変換手順も忘れずに検証する。
18. **(第7弾追加)** `models/mainhall_free.glb`(採用確定した本殿)を実際に`placeMainHall(z)`で読み込み、`normalizeToHeight`後のスケール・当たり判定box・引き戸平面の位置がモデルの実寸(§3参照)とズレていないことを実機で確認する。

## 第8弾: つっちー本殿/手水舎の採用、controller.html空気感演出

### 経緯
- userからBOOTHのつっちーモデル2点(本殿 `honden.fbx` 2.3MB、手水舎 `chouzuya.fbx` 1.4MB、いずれも「商用利用・改変自由、再配布不可」ライセンス確認済み)がアップロードされた。
- FBXはテクスチャが作者PCのローカルパス参照のまま外部化されており、GLTFLoaderが読み込めない状態だった。glb変換後、JSON chunkを直接書き換えてimages/texturesの参照を除去し、マテリアル名(oni/yane/red/white/stair/hounou/take等)から用途を推定してbaseColorFactorを塗り直すパイプラインを作成(SimplifyModifierの二次誤差削減+崩壊検知時のグリッドクラスタリングfallbackによる軽量化つき)。
- **本殿**: 342KB・21,287 triに軽量化、3方向のスクリーンショットで違和感の無い仕上がりを確認。`models/honden_free.glb`としてコミットし、`placeMainHall(z)`から読み込む(旧`models/shrine_hall_free.glb`参照は実は第7弾で追加した`mainhall_free.glb`に更新されておらず、実際には一度も読み込まれていなかった不具合が判明したため、このタイミングで併せて是正・`mainhall_free.glb`は削除)。
- **手水舎**: 軽量化の有無に関わらず(元の40,000 tri未加工状態でも)屋根と水盤の間に支柱が存在せず宙に浮いた状態であることが判明。全メッシュのワールド座標バウンディングボックスを調査した結果、支柱に相当するメッシュ自体が元データに含まれていないと判断(軽量化パイプラインが壊したのではない)。user承認のもと、ライセンスの改変可規定に基づき4本の簡易な木製支柱(円柱)を追加して隙間を視覚的に解消した。あわせて水盤本体(`oke`マテリアル)が塗り直し対象から漏れて彩度の高いオレンジのままだった点も修正。`models/chouzuya_free.glb`としてコミットし、`buildBasin(x,z)`から読み込む(センサーライトの電球は見た目モデルに関わらず独立して同じ位置に残す、`placeMainHall`の引き戸と同じ扱い)。
- controller.html側の3点も同セッションで対応: (1)境界線の太さを1/10に(0.12→0.012)、(2)フォグ密度のデフォルトを100%→80%に軽減、(3)空気感演出としてキャラクター周囲を漂う微細なホコリパーティクル(`THREE.Points`、220個、範囲外に出たらラップしてキャラに追従)を追加。

### 教訓
- **メッシュのワールドバウンディングボックスが上下に大きく広がっていても、内部に連続した実体があるとは限らない**(屋根の装飾的な垂れ下がり部分だけが範囲を広げているケースがあった)。軽量化前後どちらでも同じ見た目の欠落がある場合は、パイプラインの問題ではなく元データ自体の欠落を疑う。
- **フリーモデルの差し替えは、コード変更(ファイルパス・法線・クレジット表記の更新)まで含めて一括で確認しないと「glbはコミットしたが実際には読み込まれていない」不具合が生埋れる**(今回の`mainhall_free.glb`がまさにこのケース)。

## 第9弾: つっちー本殿/手水舎をOBJ形式で再取得、頂点欠落バグを修正

### 経緯
- 第8弾で採用した`honden_free.glb`/`chouzuya_free.glb`(FBX→glb変換、SimplifyModifierで軽量化)について、userから「頂点が欠けていて穴だらけ」との指摘。userが同じモデルをOBJ+MTL形式(`honden.obj`/`honden.mtl`、`chouzuya.obj`/`chouzuya.mtl`)で再度用意し、置き換えられるか確認依頼があった。
- 調査の結果、**穴の原因は第8弾のFBX→glb変換パイプライン(SimplifyModifierによる軽量化)側の不具合であり、元データ自体には欠落が無かった**ことが判明。OBJ形式は非圧縮・非破壊のテキスト形式で、Blenderのエクスポート時点のジオメトリをそのまま保持しているため、これを直接使うことで穴を回避できると判断した。
- 当初、手水舎(chouzuya)の屋根と水盤の間に支柱が無く浮いているという第8弾からの既知の問題(その回では4本の簡易円柱を追加して対処済み)がOBJ側にも引き継がれていると想定し、同様の支柱追加を検討したが、**実際にthree.jsでレンダリングして確認したところ、OBJ側には最初から朱塗りの支柱(`hounou`マテリアル、台座付き)が正しく含まれていた**。第8弾のFBX変換はこの支柱ジオメトリ自体を欠落させていたことになる(バウンディングボックスの数値だけを見て「支柱が無い」と誤診断しかけたため、必ず目視レンダリングで確認する、という本ドキュメントの既存の教訓を再確認する形になった)。

### 変換パイプライン
- assimp/Blenderはこのセッションでは利用できなかったため、Node.js上でthree.js(r128、`js/vendor/three.min.js`と同バージョン)の`OBJLoader`/`MTLLoader`/`GLTFExporter`を使い、無変換(簡略化なし)でglbに変換するスクリプトを新規作成(このドキュメントには残さない、セッションの一時スクリプト)。
- `.mtl`内の`map_Bump`/`map_Kd`/`map_Ns`行(作者PCのローカルWindowsパス参照で、この環境には存在しないテクスチャファイル)は事前に除去し、`Kd`(拡散色)の数値だけをそのままマテリアルカラーとして採用。第8弾のような「マテリアル名から色を推測して塗り直す」処理は不要になった(OBJ+MTLはテキストなので値がそのまま読める)。
- 変換後、`gltf-transform`(`weld`+`dedup`+`prune`+`quantize`)でロスレスな軽量化を実施。`simplify`(メッシュの間引き)は、このモデルが非常に多数の小さな独立パーツ(瓦1枚ずつ等)で構成されており境界ロックのため実質的にほぼ縮小できず、かつ再び頂点欠落のリスクを持ち込むため**今回は使用しなかった**。`quantize`は`KHR_mesh_quantization`拡張を使うが、`js/vendor/GLTFLoader.js`(r128)がこの拡張をネイティブサポート済み(追加のデコーダ設定不要)であることを確認した上で採用。
- 結果: `models/honden_free.glb`は342KB→2.79MB、`models/chouzuya_free.glb`は222KB→1.58MBに増加した。**軽量さより正しいジオメトリを優先した判断**(頂点欠落の再発を避けるため、境界を壊す簡略化は行っていない)。サイズが気になる場合は、Blenderでの手動リトポロジー(瓦などの細かいパーツをローポリで作り直す)が次の軽量化手段として考えられるが、今回は行っていない。
- 目視確認: three.jsプレビュー(3方向)、および実機(`controller.html`のサンプルモードでキャラクターをテレポートさせて確認)の両方で、本殿・手水舎とも穴・浮遊部品が無いことを確認済み。
- 水盤(`oke`マテリアル)は今回色の変更を行っていない。当初「オレンジのKdはmikage.jpg(石材テクスチャ)不在時の仮色だろう」と推測して灰色に塗り直したが、実際にレンダリングで確認したところ、水盤の外側から見える面の大部分は`oke`ではなく`hounou`(朱塗り、支柱と同じマテリアル)が占めていることが判明し、**水盤ごと支柱と同じ朱塗りで統一されたデザインである可能性が高いと判断して塗り直しを撤回**した。

### controller.html側の追加対応
- 手水舎の屋根瓦(`yane`)は本殿と同じ暗い`Kd`値を共有しているため、本殿(`placeMainHall`)に既にあった「HSLの明度に下限を設けて底上げする」処理を`buildBasin`側にも追加(今までは本殿にしか無く、手水舎の屋根が暗所でほぼ黒く潰れる可能性があった)。
- 別件のuser指摘: 空気感演出のホコリパーティクル(第8弾で追加)が、自撮り/POVのPIPカメラ画面では近距離で重なって見えすぎるとのことで、PIPレンダーパス(`selfieRT`/`fpsRT`への描画)の直前だけ`dustMat`のsize/opacityを一時的に下げ、描画後に元へ戻す対応を追加(メイン画面の見た目・挙動には影響しない)。

### 教訓(追記)
- **バウンディングボックスの数値だけで「ジオメトリが足りない」と判断しない**。今回、手水舎の支柱について数値だけで「無い」と誤診断しかけたが、実際にthree.jsでレンダリングして目視した結果、支柱は最初から存在していた。数値上の疑いは必ずレンダリングで裏取りする。
- **FBXなどのバイナリ形式は、変換パイプライン側の不具合(今回はSimplifyModifierによる軽量化)がジオメトリを壊しても気付きにくい**。OBJ+MTLはテキストで中身が直接読めるため、同じモデルの再取得先としてOBJ形式を選ぶこと自体は有効な問題切り分け手段になった。
- **同じ理由で、`usemtl`が複数ある1オブジェクトの「面グループのfaceカウントが少ない」ことは「見た目への影響が小さい」ことを意味しない**。今回`hounou`はオブジェクト内で18面しか使っていなかったが、その大半が数百頂点規模の巨大なn-gon(Blenderの穴埋め面と見られる)だったため、三角形数・表面積では`oke`(2330面)を大きく上回り、外から見える範囲の大部分を占めていた。
