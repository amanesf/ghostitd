# CLAUDE.md

このリポジトリで作業するAI/開発者向けのガイドです。編集前に必ず目を通してください。

## プロジェクト概要

3DtoolJS — ブラウザ完結(サーバサイド処理なし、素朴な`<script>`読み込みのみ、
ビルドステップなし)のキャラクター生成・閲覧・一人称walking simアプリ。

- **`landmark_tool.html`(ジェネレータ)**: 正面/側面/背面の3枚絵+ランドマーク
  座標から、3Dモデル(GLB)を生成する。彫刻(marching cubes)・スキニング・
  アトラス焼き込みまでを`js/`のパイプラインで行う。
- **`character_3d.html`(ビューア)**: GLBモデルの表示専任。ジェネレータから
  IndexedDB経由で受け取った中間データ(`P3D.loadGeneratedModel()`)を
  `finishFromIntermediate()`でその場で再構築し、ライブパラメータ編集ができる。
- **`controller.html`(コントローラー)**: 神社マップを一人称/自撮り視点で
  歩き回るwalking sim。キャラクターモデルを読み込んで動かすだけで、
  生成ロジックには関与しない。
- **`js/`**: 上記2ツール(ジェネレータ・ビューア)が共有する生成パイプライン。
  `window.P3D`名前空間にぶら下げる素朴な`<script>`読み込み方式(ESモジュール
  不使用)。`common.js`が最初に読まれ、他のファイルはそこに生やされた
  ユーティリティをそのまま使う。
- **`bk/`**: 2026-07-07の深いリファクタリング(carveRegion分割等)着手直前の
  スナップショット。参照・切り戻し用。**通常の開発では触らないこと**。

## 構造上の注意(既知の落とし穴)

3つのHTMLファイル(`controller.html`, `character_3d.html`, `landmark_tool.html`)
は、それぞれ**単一の巨大なIIFEスコープ**に全ロジックが入っている
(モジュール分割はされていない。理由は下記「なぜモジュール化していないか」参照)。
これにより以下のリスクがあるので、変数を編集・リネームする前に**同じ名前が
別の意味で使われていないか、必ずファイル全体をgrepすること**。

### 実際に見つかった衝突の例
`controller.html`では、キャラクター本体を指すグローバル変数`model`
(`THREE.Object3D`)と、サンプル選択用の`SAMPLES`配列のプロパティ
`sample.model`(GLBファイル名の文字列)が同じ識別子`model`を使っている。
単純な単語境界置換(`\bmodel\b`のような正規表現一括置換)をかけると
`sample.model`が`sample.Character.model`のような壊れた参照に化ける。
**短い/汎用的な変数名(`model`, `mode`, `c`, `v`等)は特に注意。**

### なぜモジュール化していないか
2026-07-07のリファクタリングで、3ファイルとも「巨大な関数(Long Method)を
責務ごとの内部関数に分割する」作業は完了させた(`draw()`, `frame()`,
`loadCharacter()`, `loadGLB()`, `carveRegion()`等)。しかし「グローバル状態を
名前空間オブジェクトにまとめる」「ファイルを複数`<script>`に分割する」という
モジュール化そのものは、上記の命名衝突リスクが自動テストなしでは検出しきれない
ため意図的に見送った(ユーザー合意済み)。今後着手する場合は、変数1個ずつ
全参照箇所を確認しながら進めること。

## テスト

`tests/`にPlaywrightベースの回帰テストがある。

```
npm install
npm test
```

- `tests/generation-pipeline.test.js`: サンプルから生成したGLBのSHA-256
  ハッシュを固定値と比較するgolden masterテスト。彫刻/スキニング
  アルゴリズムを意図的に変更した場合はこのテストが失敗するのが正しい
  挙動なので、テスト内の`EXPECTED_*`を新しいハッシュ値で更新すること。
- `tests/generator-ui.test.js` / `viewer-ui.test.js` / `controller-ui.test.js`:
  3ツールのUI操作(モード選択、タブ切替、アクセサリー編集、移動、
  WebGLコンテキストロスト等)の回帰テスト。
- 詳細・既知の注意点は`tests/README.md`参照。

コード変更後は**必ず`npm test`を実行し、全てPASSすることを確認してから
コミットすること**(自動テストがこれしか無いため)。

## 開発時の慣習

- 素の`<script>`読み込み(ESモジュール不使用、ビルドステップなし)。
- コメントは「なぜ」を書く(隠れた制約・過去の不具合の経緯等)。「何を」は
  書かない(コード自体が示すため)。日本語コメントが大半。
- `js/`内の各ファイルは`(function(global){ ... })(window)`で包み、
  `P3D.xxx = xxx;`の形で`window.P3D`名前空間に公開する。
- 3Dtool(Python版)からの移植コードが多く、`pipeline_lib/xxx.py の yyy() の
  JS移植`のようなコメントで元実装への参照がある。

## 参考ドキュメント

- `MASTER_PLAN.md`: 全体のロードマップ・優先度。
- `SHRINE_REDESIGN_PLAN.md`, `GHOST_SCANNER_PLAN.md`: 個別機能の設計ログ。
- `REFACTOR_STATUS.md`: 2026-07-07のリファクタリング作業メモ(完了/未完了事項)。
