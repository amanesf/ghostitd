# ストリーミング彫刻(Y方向タイル化)計画

## 背景・目的

`body_vox`/`body_vox_xz`を両方とも1px相当にすると、`Array buffer allocation
failed`でクラッシュする。原因は`js/carving.js`の`carveUnifiedRegions`が
確保する共有フィールド`field`(Float32Array)/`ownerField`(Int32Array)が、
`nx*ny*nz`セル分(mxBounds/mzBoundsはTポーズの腕先まで含む固定の全体範囲)を
一括確保しているため。実測(サンプルキャラクター、1px/1px設定):

- グリッド: 1100×976×390 ≈ 4.19億セル
- メモリ: 4.19億×8byte(field 4byte+ownerField 4byte) ≈ 3.35GB

これがブラウザタブのArrayBuffer確保上限を超えて失敗する。

一方、彫刻の**最終成果物であるサーフェスメッシュ(頂点+面)は、この巨大な
密グリッドに比べて桁違いに小さい**(同条件での推定頂点数は約67万、V+F+owner
を合わせても数十MB程度)。間引き(SimplifyModifier)は既にこの規模のメッシュを
問題なく扱えている(過去実績: 163,982頂点で完走・安定化済み)。

**したがって、メモリを食っているのはグリッド確保とmarching cubesの実行段階
だけであり、間引き以降(decimateStage/meshFinishStage/atlas_bake)は変更
不要**。Y方向にグリッドを分割(タイル化)しながらmarching cubesを行い、
できあがったサーフェスメッシュ(小さい)だけを蓄積していけば、間引きは
今まで通り最後に1回、蓄積済みの完成メッシュに対して行うだけで良い。

## 全体方針

1. Y軸を「ウィンドウ」(連続する行の区間、行数はメモリ予算から自動計算)に
   分割する。
2. ウィンドウごとに、そのウィンドウの行数分だけの小さいfield/ownerFieldを
   確保し、体+全アクセサリーをそのウィンドウの行範囲だけ彫る。
3. ウィンドウの小さいfield/ownerFieldに対してmarching cubesを実行し、
   得られた頂点・面を「グローバルな(全体を通しての)メッシュ」へ蓄積する。
4. 次のウィンドウへ進む(field/ownerFieldは使い回し、-1で初期化し直す)。
   隣接ウィンドウは境界の行を1行共有し、marching cubesのセルがY方向に
   跨って抜け漏れ・重複なく処理されるようにする。
5. 全ウィンドウを処理し終えたら、蓄積済みのグローバルメッシュ(V,F,owner)を
   `carveUnifiedRegions`の戻り値として返す。**ここから先
   (`dropSmallFragments`/`decimateStage`/`meshFinishStage`/`stageAtlasBake`)
   は現状のコードを一切変更しない。**

グリッドが十分小さい(現状の既定設定程度)場合は、ウィンドウ数=1になるように
自動調整され、実質的に今までと同じ1回のmarching cubesと同じ結果になる
(=挙動を変えない安全弁)。

## 必要な変更(ファイル別)

### `js/marching_cubes.js`

`marchingCubes(field, ny, nx, nz, isolevel, ownerField)`は現状、呼び出す
たびに新しいエッジ→頂点キャッシュ(`cache`)とverts/faces配列をゼロから
作る。ウィンドウをまたいで**境界の行(隣接ウィンドウ同士が共有する行)上の
エッジ**が同じ物理頂点として再利用されないと、境界に頂点が二重生成され、
位置は同じでもindexが別々になる(見た目のクラック/継ぎ目、間引き時の
不整合の原因になりうる)。

対応: `marchingCubes`に以下を追加する(後方互換、省略時は今まで通り):
- `opts.rowOffset`: このウィンドウの最初の行が全体の何行目に当たるか。
  キャッシュのキー(`canonicalEdgeKey`)を、ウィンドウ内のローカル行番号
  ではなく`rowOffset+ローカル行番号`(=グローバル行番号)で作るようにする。
- `opts.externalCache`: 呼び出し側が保持する`Map`を渡せるようにし、
  戻り値としても(更新済みの)cacheを返す。呼び出し側は次のウィンドウを
  処理する前に、**直前ウィンドウの最終行(=次のウィンドウの先頭行)に
  関するキャッシュエントリだけ**を引き継ぐ(古い内部行のエントリは破棄して
  メモリを抑える)。
- `opts.vertBase`: 頂点index(faces配列の中身)をグローバルな通し番号に
  オフセットできるようにする(呼び出し側でV/Fをそのままグローバル配列へ
  結合できるようにするため)。

### `js/carving.js`

- `carveRegion`のメイン行ループ(`for(var iy3=0; iy3<ny; iy3++)`)に、
  処理対象の行範囲(`opts.rowRange = [iyMin, iyMax]`、省略時は全行)を
  絞れるようにする。`field`/`ownerField`の添字計算(`rowOff=iy3*strideY`)も
  ウィンドウ内ローカルな行番号基準に変更する必要がある。
- `carveUnifiedRegions`にウィンドウ処理のオーケストレーションを追加する:
  - グリッド全体のメモリ予算(例: 300MB)から`WINDOW_ROWS`を自動計算する
    (`Math.max(8, Math.floor(BUDGET_BYTES/(nx*nz*8)))`、`ny`以下にクランプ)。
  - `WINDOW_ROWS >= ny`なら現状通り1回で処理(挙動変更なし)。
  - それ以外は、行0から`WINDOW_ROWS-1`刻みで重なりを持たせながら
    (次ウィンドウの先頭行=前ウィンドウの最終行)ループし、各ウィンドウで
    小さいfield/ownerFieldを確保→体+各アクセサリーをそのウィンドウの
    行範囲だけ彫刻→marching cubes→グローバルV/F/ownerへ蓄積、を繰り返す。

### `part_gap_close`/`bone_bridge`との整合

`closeInterPartGaps`(part_gap_close)と`bridgeKFor`(bone_bridge)は、
どちらも「隣接ボクセルを距離R歩まで辿る」BFS/参照を、共有field全体に対して
行っている。ウィンドウ化すると、境界付近ではウィンドウの外側(まだ確保して
いない/既に破棄した行)の情報が必要になり、正しく判定できない可能性がある。

**v1のスコープでは、この2機能とウィンドウ化を同時には検証しない
(part_gap_close/bone_bridgeがONの状態で1px/1px相当の巨大グリッドを使う
組み合わせは、まず動くようにしてから別途検証する)。** 必要であれば、
ウィンドウの重なり行数を「境界処理に必要な最大R」以上に広げることで
正しく扱えるはずだが、これは後続の検証課題とする。

### `js/pipeline.js`

- `runCarvingStages`の進捗報告(`report(...)`)に、ウィンドウ処理の進捗
  (例:「彫刻(ウィンドウ3/12)」)を追加する。1px/1px相当の生成は複数回の
  marching cubes呼び出しに分かれるため、進捗が見えないと固まったように
  見える。
- `estimateOnly`モード(既に実装済み)には変更不要(グリッド確保自体を
  行わない軽量パスなので、ウィンドウ化の恩恵とは無関係)。

## 検証方法

1. **回帰確認**: 既定設定(ウィンドウ数=1に自動収束するはず)で
   golden-masterハッシュが変わらないことを確認する。
2. **境界の水密性確認**: 意図的に小さいメモリ予算(例: 数MB相当)を設定して
   強制的に多数のウィンドウに分割させ、体単体を生成。頂点座標でマージした
   上での真の境界エッジ(1三角形にしか使われない辺)が0件であることを
   確認する(このセッションで既に使った手法の再利用)。
3. **実際のクラッシュケースの再現・解消確認**: 1px/1px設定で実際に生成が
   完走することを確認する(現状クラッシュするケース)。
4. **速度の実測**: ウィンドウ分割によるオーバーヘッド(field再確保・
   marching cubes呼び出し回数増)がどの程度か実測し、許容範囲か確認する。

## 見積もり・リスク

- 変更範囲: `js/marching_cubes.js`(中規模)、`js/carving.js`の
  `carveRegion`/`carveUnifiedRegions`(中〜大規模、彫刻の中核関数)、
  `js/pipeline.js`(小規模、進捗報告のみ)。
- 最大のリスクは`carveUnifiedRegions`が彫刻パイプラインの中核であること
  (bone_bridge/part_gap_close以外にも、他の機能が「共有fieldは1回だけ
  丸ごと確保される」という前提を暗黙に置いていないか、変更時に全体を
  grep して確認する必要がある)。
- 既定設定(体_vox_xzが既定の粗い値)ではウィンドウ数=1に収まるため、
  通常利用への影響は無いはず。1px/1px等の重い設定を使うユーザーだけが
  恩恵を受け、かつ副作用も受ける(バグがあった場合、影響はそのユーザーに
  限定される)。
